# Surface Contract

> The authoritative per-tool, per-surface status registry for the Macro_Copilot frontend.  As of v5 (Phase 0 of the `revamp` branch) the contract also tracks lifecycle-axis status per the [tool lifecycle standard](../03_standards/tool_lifecycle.md).

**Version:** v5 (Phase 0 of `revamp` — lifecycle axes added)
**Last reviewed:** 2026-05-26
**Status:** draft — Phase 0 in progress
**Branch discipline:** as of v5, rows live on the `revamp` branch.  `build` stays at v4 until `revamp` merges back.
**See also:** [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) (lifecycle standard); [`../05_decisions/0015-tool-metadata-db-table.md`](../05_decisions/0015-tool-metadata-db-table.md) (DB metadata architecture).

---

## 0. Purpose

Every backend artifact in this system — a **primitive**, an **operator**, or a **workflow** — can be exposed on up to four frontend surfaces: **Library**, **Ask**, **Build**, **Monitor**. This document is the single source of truth for:

1. Which surface each backend artifact **should** have (eligibility).
2. Which surface each backend artifact **currently has** (status).
3. The **classifications** that pin down what "having a surface" actually means for that artifact + surface combination.
4. **(v5)** Which lifecycle axes (per [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) §2) each tool has shipped — see §10.

No surface code may be added, removed, or modified without updating the corresponding row in this document in the same PR. That is the gate.

---

## 1. THE CONTAINMENT PRINCIPLE (load-bearing)

> A tool may be partially shipped — i.e. one surface built, another not — **if and only if** all the partial state lives entirely inside that tool's module folder at `src/modules/primitives/<tool_name>/` (or `src/modules/workflows/<workflow_id>/` for workflows).

**Concretely:**

- ✅ **Allowed:** module A's Monitor widget is real, its Ask card is real, its Build is just the generic fallback. *All* the per-tool code lives inside the module folder. The central registries (`toolNames.ts`, `monitor/registry.ts`, `widgets/index.ts`, `BuildShell.tsx`, `ConversationCanvas.tsx`, `contextDecoder.ts`, `persistedModelAdapters.ts`) carry zero per-tool entries for this module — they only do dispatch.
- ✅ **Allowed:** module B's bespoke Build canvas is partially polished, the result-renderer for module C is not yet pixel-perfect. As long as the files live inside their module folders and the central files have no exceptions for them.
- ❌ **Disallowed:** a "placeholder" `KNOWN_BACKEND_TOOLS` entry for tool X in `src/lib/toolNames.ts`.
- ❌ **Disallowed:** a `case 'tool_x':` branch inside `src/lib/contextDecoder.ts` to special-case routing for X.
- ❌ **Disallowed:** a stub widget file under `src/components/monitor/widgets/` named after a specific primitive.
- ❌ **Disallowed:** a module that claims a tier (e.g. `monitor_surface`) but ships a placeholder widget that does not deliver the claimed capability. **A module's `tiers` array is a contract.** Claiming a tier you don't deliver is "half-built state for the system," which the principle forbids.

**Enforcement:**

- ESLint `boundaries/no-module-from-shells` (already in place, FP12).
- This document. Every PR must update affected rows; a row marked `built` for surface X requires the corresponding file to exist inside the module folder AND the module's `tiers` array to claim the matching tier.

---

## 2. The 3 backend building blocks

### 2.1 Primitive
A single backend tool with a typed input + typed output.  Today the backend ships **58 total primitives** the frontend must consider, broken down as: **52** in `rates_agent/workflows/__init__.py:_PRIMITIVE_SPECS` (with executable PrimitiveSpec) + **3** in `WORKFLOW_INCOMPATIBLE_TOOLS` (real backend tools whose output shape can't lift to a Series/Panel artifact) + **3** manifest-only / typed-detail tools (have typed-detail endpoints but no PrimitiveSpec entry — e.g. `calculate_butterfly_tool`, `scan_extremes_tool`).

Output kinds (drives Build / Monitor archetype choice):
- `single-series-bps` — time series in basis points (spreads, butterflies)
- `single-series-percent` — time series in percent (yields, rates)
- `single-series-events` — per-event values (CPI surprise, NFP surprise; index is release dates, not trading dates)
- `snapshot-grid` — multi-row table (curves × tenors)
- `scanner-result` — ranked rows
- `regime-state` — single-row classification
- `rich-model` — composite output (PCA loadings + variance + factor scores; regression coefficients; half-life + half-life CI; etc.)
- `panel` — tabular multi-row dataset (e.g. `build_sovereign_yield_panel`)

### 2.2 Operator
Internal computational glue, defined in `shared/operators/`. Today: **12 operators**. By design, operators have NO direct frontend surface. They are not in Library, do not get routed by Ask, do not appear on Build except as nodes in a workflow DAG.

The operators are: `align_series`, `series_arithmetic`, `threshold_events`, `event_windows`, `conditional_aggregate`, `rolling_regression`, `apply_mask`, `select_from_series_set`, `summarize_series`, `construct_trades`, `evaluate_trades`, `summarize_trades`.

Operator outputs only surface when (a) they are the **terminal artifact** of a workflow (rendered in the standard ResultsView), or (b) future enhancement: a workflow's ResultsView opts in to an "intermediate steps" view that surfaces non-terminal operator artifacts.

### 2.3 Workflow
A DAG template chaining primitives + operators, defined in `rates_agent/workflows/<template_id>/template.yaml`. Today: **3 templates** — `event_study`, `regime_conditioned_relationship`, `backtest`.

Workflow surface contract:
- MUST render the DAG in Build via `DagView`.
- MUST render the terminal artifact in Build via `ResultsView` (specialised dashboard if one exists, generic widget grid otherwise).
- MUST render its progress + result inline on Ask via `WorkflowResultCard`.
- MAY surface intermediate operator outputs via the (future) "show intermediate steps" toggle.

---

## 3. The 4 frontend surfaces

### 3.1 Library

**Purpose:** catalogue of every backend-registered tool. The user browses, filters by sub-agent / category, clicks a card → `ToolDetailDrawer` → "Open in Build" deep-link.

**Source of truth:** `/api/v1/library/manifest`. The page is fully auto-derived.

**Status enum (per tool):** `registered` | `hidden`.

This surface has zero per-tool frontend code — it is a generic list+drawer over the manifest. No row in the registry below needs anything beyond `registered` / `hidden`.

### 3.2 Ask

**Purpose:** chat-style answer to a natural-language query. Brief textual answer + a "Show more in Build" handoff button that transfers context to the Build page.

**Render component:**
- Default: `AssistantResearchCard` (7 zones — RoutingStrip, ProseAnswer, ToolTrace, ResultCanvas, DagStrip, ProvenanceRow, ActionRow, FollowUps). Already feature-complete. Renders the terminal artifact chart, the DAG, lineage, follow-ups.
- Per-module override: `MODULE.surfaces.ask` lets a module replace the entire research card with bespoke chrome. A bespoke ask card **MUST add value over the generic** (e.g. a specific framing/header callout above the standard zones); it MUST NOT drop the generic's chart / DAG / provenance / follow-ups.

**Workflow rendering:** `WorkflowResultCard` (renders inline inside the chat bubble for workflow turns).

**Status enum (per tool) — Ask routing stage:**

| Stage | Meaning |
|---|---|
| **1 — `unrouted`** | The Supervisor's system prompt doesn't list this tool's domain as covering relevant queries → "clarify" / "out_of_scope" response. Ask never reaches the tool. |
| **2 — `routed-generic`** | Supervisor routes to the right domain → domain agent calls the tool → `AssistantResearchCard` renders the result. Default for any tool whose domain is already enumerated in the Supervisor prompt. |
| **3 — `routed-bespoke-frontend`** | Stage 2 plus the module ships `MODULE.surfaces.ask` and the card adds real value beyond the generic. |
| **4 — `routed-bespoke-full`** | Stage 3 plus the domain agent prompt has tool-specific framing logic (e.g. NFP-specific "actual vs consensus" structured output). |

A module that claims `MODULE.surfaces.ask` but where the Supervisor doesn't route to it = **stage 1 (unrouted)**. Frontend claim alone is not enough.

### 3.3 Build

**Purpose:** the canonical analysis-anatomy view. The most important page in the product. Three states:

- **Empty** — no slug, no `?context=`, no `?workflow=`, no `?builder=` → polished "What would you like to build?" entry with category tiles + composer.
- **Single-primitive** — `?context=<encoded>` with one decoded tool → `VirtualPrimitiveCanvas` mounts the module's **extended Build view** (`MODULE.surfaces.buildExtended` per the [rendering-density standard](../03_standards/rendering_density.md); legacy modules: `MODULE.surfaces.build` / generic builder fallback).
- **Multi-tool query (ad-hoc DAG)** — `?context=` with ≥2 decoded tools → DAG visualization with each primitive rendered as a node body via the module's **compact Build view** (`MODULE.surfaces.buildCompact` per the rendering-density standard).  Each node carries an expand affordance that opens that primitive's extended view in a modal/drawer overlay with a breadcrumb back to the DAG.  Legacy modules without `buildCompact` fall back to the artifact-type generic card.
- **Workflow template execution** — `/workspace/:slug` (persisted) → `BuildCompleted` with 4 tabs (DAG / Results / Parameters / Notes).  Workflow templates (`event_study`, `regime_conditioned_relationship`, `backtest`) are a SEPARATE backend concern from ad-hoc multi-tool queries; their dashboard rendering will be governed by a future workflow-contract analogous to the primitive contract.  **Provisional rule** until that contract lands: workflow-template DAG nodes also mount each primitive's compact view, never the extended view inline.

**Dual-view dispatch (rendering-density standard).** Per [`../03_standards/rendering_density.md`](../03_standards/rendering_density.md), every new primitive module ships TWO Build-side views — `buildExtended` (full canvas) and `buildCompact` (grid card) — and the Build canvas dispatches between them based on the single-tool vs multi-tool query context above.  Both views are MANDATORY for new modules; legacy modules are explicitly carved out and migrated opportunistically.

**Multi-tool queries vs workflow templates — two different concepts:**

| Concept | What it is | Where the DAG comes from |
|---|---|---|
| **Multi-tool query (ad-hoc DAG)** | The LLM stitches N primitive calls together AT QUERY TIME to answer an open-ended multi-step prompt (e.g. *"compare US 2s10s vs Bund 2s10s vs BTP 2s10s"*).  No backend template defines the DAG — it's emergent in the supervisor turn. | The supervisor turn's tool-call sequence; rendered by ad-hoc DAG infrastructure. |
| **Workflow template execution** | A backend-declared DAG recipe at `rates_agent/workflows/<template_id>/template.yaml`.  The structure is pre-defined; users fill in slot values. | The template file; rendered by the specialised dashboard for that archetype. |

Both render each primitive node via `buildCompact`; the difference is in the surrounding chrome (ad-hoc DAG infrastructure vs archetype-specific dashboard) and the lifecycle (ephemeral chat-turn vs persisted workspace).

**Per-tool Build archetypes:**

| Archetype | Definition | How it ships |
|---|---|---|
| **`generic-builder`** | Default. The page renders a schema-driven form (from backend `ToolCard.input_fields`) + runs the tool + shows the output via `AutoRenderer`. No per-tool frontend code beyond what the manifest provides. | Module claims only `generic_runnable`. No surface claims. |
| **`typed-result-renderer`** | Same controls path as `generic-builder` (or hand-rolled `PrimitiveParamControls` for the 6 Stage-4 typed views), but the **output area** ships a bespoke chart + KPI strip via `MODULE.surfaces.resultRenderer`. | Module claims `manifest_typed_view` OR `custom_build_surface` AND ships `surfaces/ResultRenderer.tsx`. |
| **`custom-canvas`** | The module owns the entire Build page — bespoke controls AND bespoke output (model-builder feel: PCA, regression, half-life). | Module claims `custom_build_surface` AND ships `surfaces/BuildSurface.tsx`. |

**Status enum (per tool):**

| Status | Meaning |
|---|---|
| **`scaffolded`** | Module folder exists. Falls through to `generic-builder`. The default schema-driven form + `AutoRenderer` is what the user sees. |
| **`typed-renderer-built`** | `surfaces.resultRenderer` ships and renders real data via recharts. Editable params via the shared `PrimitiveParamControls` or the generic form. |
| **`custom-canvas-built`** | `surfaces.build` ships and is feature-complete: editable form controls + bespoke output. |
| **`production-validated`** | The above PLUS verified end-to-end against real data, snapshot-tested, no open regressions, parameter changes round-trip via URL. |

### 3.4 Monitor

**Purpose:** desk-glanceable board for state the user tracks throughout the trading day. Pre-aggregated grids + parameterised tiles.

**Eligibility rule (load-bearing):**

> A tool gets a Monitor widget **only** if a desk user would park it on their board and read it through the trading day. Single-fire event reads (CPI / NFP / PPI releases), one-off scenario calculations, and rich-model fits do NOT belong on Monitor. They are Ask / Build affairs.

**Widget type taxonomy:**

| Type | Examples | Data source | Parameterised? |
|---|---|---|---|
| **`snapshot-grid`** | YieldSnapshot, CurveSpreads, CrossMarketSpreads | shared `RatesDataContext` (page-level fetch) | No |
| **`scanner-leaderboard`** | Scanner | shared `RatesDataContext` | No |
| **`time-series-chart`** | YieldLevel, SpreadChart, CrossMarketSpread | per-widget `/api/v1/rates/detail/...` fetch | Yes (curve + tenor + lookback) |
| **`regime-state`** | CurveClassifier | per-widget `/api/v1/rates/detail/regime` | Yes |

**Eligibility enum (per tool):**

| Eligibility | Definition |
|---|---|
| **`required`** | Desk-glanceable; should have a Monitor widget. |
| **`built`** | Eligible AND ships a real, non-stub widget. |
| **`not-required`** | Explicitly does NOT belong on Monitor (event tools, rich-model tools, one-shot calculators). |
| **`deferred`** | Eligible but parked behind an upstream dependency (e.g. typed-detail endpoint not yet shipped). |

**Anti-pattern:** a module that claims `monitor_surface` but ships a placeholder widget violates the containment principle. The claim must match delivery.

---

## 4. The per-primitive registry

> 58 rows. One row per backend primitive. Columns: tool_name | domain | output_kind | library | ask_stage | build_archetype | build_status | monitor_eligibility | monitor_status.

### 4.1 Sovereign bonds (9)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `calculate_curve_spread_tool` | single-series-bps | registered | routed-generic | dual-view-bridge | dual-view-built | required | built (2 widgets: CurveSpreads, SpreadChart) |
| `calculate_cross_market_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | typed-renderer-built | required | built (2 widgets: CrossMarketSpreads, CrossMarketSpread) |
| `calculate_butterfly_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | typed-renderer-built | not-required | n/a |
| `get_yield_levels_tool` | single-series-percent | registered | routed-generic | dual-view-bridge | dual-view-built | required | built (YieldLevel; YieldSnapshot also reads this domain) |
| `classify_curve_move_tool` | regime-state | registered | routed-generic | typed-result-renderer | typed-renderer-built | required | built (CurveClassifier) |
| `scan_extremes_tool` | scanner-result | registered | routed-generic | typed-result-renderer | typed-renderer-built | required | built (Scanner) |
| `calculate_otr_ofr_spread_tool` | single-series-bps | registered | unrouted | dual-view-bridge | dual-view-built | required | built (OtrOfrSpreadWidget) |
| `build_sovereign_yield_panel_tool` | panel | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |
| `get_otr_history_tool` | workflow-incompatible | registered | unrouted | n/a (workflow_incompatible) | n/a | not-required | n/a |

### 4.2 OIS (7)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `calculate_ois_curve_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (OisCurveSpread widget) |
| `calculate_ois_cross_market_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (CrossMarketOis widget) |
| `calculate_ois_forward_rate_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (OisForwardRate widget) |
| `get_ois_rate_level_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (OisRateLevel widget) |
| `calculate_ois_butterfly_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (OisButterfly widget) |
| `calculate_swap_spread_tool` | single-series-bps | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |
| `calculate_wirp_meeting_pricing_tool` | workflow-incompatible | registered | routed-generic (paused) | n/a (workflow_incompatible) | n/a | not-required | n/a |

### 4.3 Inflation-indexed bonds / linkers (10)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `get_real_yield_level_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (RealYieldLevel widget) |
| `calculate_breakeven_inflation_simple_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (BreakevenInflation widget) |
| `calculate_forward_breakeven_simple_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (ForwardBreakeven widget) |
| `calculate_breakeven_curve_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (BreakevenCurveSpread widget) |
| `calculate_cross_country_breakeven_spread_simple_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (CrossCountryBreakevenSpread widget) |
| `calculate_real_yield_curve_spread_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (RealYieldCurveSpread widget) |
| `calculate_cross_country_real_yield_spread_simple_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (CrossCountryRealYieldSpread widget) |
| `calculate_real_yield_butterfly_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (RealYieldButterfly widget) |
| `calculate_breakeven_butterfly_tool` | single-series-bps | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |
| `build_linker_panel_tool` | panel | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |

### 4.4 Inflation swaps (8)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `calculate_inflation_swap_rate_level_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (InflationSwapRateLevel widget) |
| `calculate_inflation_swap_curve_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (InflationSwapCurveSpread widget) |
| `calculate_inflation_swap_forward_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (InflationSwapForward widget) |
| `calculate_cross_market_inflation_swap_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (CrossMarketZcis widget) |
| `calculate_swap_breakeven_basis_simple_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (SwapBreakevenBasis widget) |
| `calculate_inflation_swap_butterfly_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (InflationSwapButterfly widget) |
| `build_zcis_panel_tool` | panel | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |
| `scan_inflation_swaps_extremes_tool` | scanner-rankedlist | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (ZcisScanner widget) |

### 4.5 Policy futures / STIR (8)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `policy_futures_get_futures_price_level_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (PolicyFuturesPriceLevel widget) |
| `policy_futures_get_volume_open_interest_snapshot_tool` | snapshot-grid | registered | routed-generic | generic-builder | scaffolded | required | deferred |
| `policy_futures_get_futures_strip_snapshot_tool` | snapshot-grid | registered | routed-generic | generic-builder | scaffolded | required | deferred |
| `policy_futures_get_futures_calendar_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (PolicyFuturesCalendarSpread widget) |
| `policy_futures_get_futures_cross_market_spread_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (PolicyFuturesCrossMarketSpread widget) |
| `policy_futures_get_futures_butterfly_simple_tool` | single-series-bps | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (PolicyFuturesButterfly widget) |
| `policy_futures_get_futures_pack_average_simple_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (PolicyFuturesPackAverage widget) |
| `get_scan_policy_futures_extremes_tool` | scanner-rankedlist | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (PolicyFuturesScanner widget) |
| `build_policy_futures_strip_panel_tool` | panel | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |

### 4.6 Bond futures (3)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `get_futures_price_level_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (BondFuturesPriceLevel widget) |
| `get_futures_volume_oi_tool` | snapshot-grid | registered | routed-generic | generic-builder | scaffolded | required | deferred |
| `scan_bond_futures_extremes_tool` | scanner-rankedlist | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (BondFuturesScanner widget) |


### 4.7 Inflation-linker scanner (1)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `scan_inflation_linkers_extremes_tool` | scanner-rankedlist | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (LinkerScanner widget) |

### 4.8 OIS scanner — paused (1)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `scan_ois_extremes_tool` | scanner-result | registered | unrouted | n/a (paused) | n/a | required | deferred |

### 4.9 Rich-model / analytical (5)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `calculate_pca_yield_curve_tool` | rich-model | registered | routed-generic | custom-canvas | custom-canvas-built | not-required | n/a |
| `calculate_rolling_regression_tool` | rich-model | registered | routed-generic | custom-canvas | custom-canvas-built | not-required | n/a |
| `calculate_yield_change_attribution_pca_tool` | rich-model | registered | routed-generic | custom-canvas | custom-canvas-built | not-required | n/a |
| `calculate_half_life_tool` | rich-model | registered | routed-generic | custom-canvas | custom-canvas-built | not-required | n/a |
| `calculate_beta_adjusted_spread_tool` | rich-model | registered | routed-generic | custom-canvas | custom-canvas-built | not-required | n/a |

### 4.10 Cross-domain shared (3)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `compute_financing_rate_tool` | single-series-percent | registered | routed-generic | typed-result-renderer | dual-view-built | required | built (FinancingRate widget) |
| `calculate_breakeven_inflation_tool` | single-series-bps | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |
| `calculate_zscore_custom_tool` | single-series-events | registered | routed-generic | generic-builder | scaffolded | not-required | n/a |

### 4.11 Event-release surprises (2)

| tool_name | output_kind | library | ask | build_archetype | build_status | monitor_elig | monitor_status |
|---|---|---|---|---|---|---|---|
| `calculate_cpi_surprise_tool` | single-series-events | registered | unrouted | generic-builder | scaffolded | not-required | n/a |
| `calculate_nfp_surprise_tool` | single-series-events | registered | unrouted | generic-builder | scaffolded | not-required | n/a |

**Resolved in PR (post-contract Stage C):** Modules `calculate_cpi_surprise_tool` and `calculate_nfp_surprise_tool` previously claimed `monitor_surface` + `ask_surface` with stub widgets and a bespoke Ask card that was a regression vs the generic.  Both tier claims were retracted; the stub files were deleted; the modules now claim `generic_runnable` only.  Per the containment principle (§1), a module's `tiers` array must match delivery.

---

## 5. Per-workflow registry

| template_id | nodes (count) | status | ask | build_dag | build_results | notes |
|---|---:|---|---|---|---|---|
| `event_study` | ~10 | active | routed-generic via `WorkflowResultCard` | rendered via `DagView` | rendered via `ResultsView → EventStudyDashboard` (specialised) | **frontend chain verified clean (PR audit):** node-ID role bindings match template (`signal`/`target`/`align`/`events`/`windows`/`aggregate`/`unconditional_aggregate`/`compare`); 5 dashboard sections render via `NodeWidgetCard` with `MissingArtifactCard` fallbacks; `WorkflowResultCard` handles event-relative Series via `EventRelativeStrip`.  User-reported "garbage outputs" are backend payload quality — frontend renders honestly whatever the backend persists. |
| `regime_conditioned_relationship` | ~12 | active | routed-generic via `WorkflowResultCard` | rendered via `DagView` | rendered via `ResultsView → RegimeConditionedDashboard` (specialised) | — |
| `backtest` | ~6 | paused (backend gated) | not routed | n/a | `WorkflowStatusCanvas` shows "paused" card | depends on financing + price-panel data that isn't fully shipped backend-side |

---

## 6. Operator visibility policy

Operators have NO frontend surface and ARE NOT listed in this registry by name. They exist only as nodes inside workflow DAGs.

When an operator's output IS the terminal artifact of a workflow, it surfaces via the standard `ResultsView` widget grid (one of `SeriesWidget` / `SeriesSetWidget` / `EventSetWidget` / `PanelWidget` / `WindowedPanelWidget` / `TradeSetWidget`) — dispatched by artifact type. No per-operator code lives in the frontend.

**Intermediate-step surfacing (shipped):** every workflow's specialised `ResultsView` dashboard ships a "Show intermediate stages (N)" toggle.  When the toggle is on, every NON-terminal per-node artifact (primitives AND operator outputs alike — `align_series`, `threshold_events`, `event_windows`, `conditional_aggregate`, `rolling_regression`, `construct_trades`, `evaluate_trades`, `summarize_trades`, `select_from_series_set`, etc.) renders through the same per-artifact widget registry the terminal output uses (`NodeWidgetCard` → `resolveNodeRenderer`).  No per-operator UI code is required — adding a new operator is automatically covered by the registry the moment its artifact type is recognised.

---

## 7. Cross-cutting status — containment principle today

**RESOLVED (PR Stage C — same PR as this row update):**

1. **`calculate_cpi_surprise_tool`** previously claimed `monitor_surface` + `ask_surface` with stub widget + regression Ask card.  Both claims retracted.  Module now `generic_runnable` only.
2. **`calculate_nfp_surprise_tool`** — same.  Both claims retracted.  Module now `generic_runnable` only.
3. **`calculate_otr_ofr_spread_tool`** previously claimed `monitor_surface` with stub widget.  Claim retracted.  Module now `generic_runnable` only.  Monitor remains eligible per the rule; row marked `deferred` until the typed-detail endpoint ships OTR-OFR series.

Containment is restored.  Every remaining tier claim across the 58 primitives matches a real, polished surface.

---

## 8. Critical end-to-end gaps (require multi-PR work, NOT containment violations)

These are correctly contained today (no central-file leakage) but are functionally incomplete:

1. **Ask routing for event-release queries** is at Stage 1 (`unrouted`) for the 5 event-class tools (CPI / NFP / OTR-OFR + others). Resolved by extending the Supervisor system prompt in `orchestrator/prompts.py` to claim these tools under their owning domain. **Out of scope for this session per user direction; queued for the orchestration session.**
2. **~~`contextDecoder` typed-kind coverage~~** — **RESOLVED (PR Decoder Audit, v3).**  Audit traced `decodeOne` against every module in `ALL_PRIMITIVE_MODULES`: by construction, the `isRunnablePrimitive` branch (step 3) routes every module that claims `generic_runnable` to `kind: 'generic_builder'`, and the module-derived runnable set automatically covers all such modules.  Modules with `manifest_typed_view`/`workflow_incompatible`/`paused` route via their own branches.  A new coverage assertion in `src/components/build/primitive/__tests__/routingCoverage.test.ts` iterates `ALL_PRIMITIVE_MODULES` and fails CI if any module decodes to null — pure regression guard.  The orange "Could not decode" card now only fires for malformed JSON or backend tools not yet registered as frontend modules (zero today per parity check).
3. **~~Workspace fork → re-render with new params loop~~** — **RESOLVED (PR Operator-Surfacing + Fork-Pipeline Audit, v4).**  End-to-end loop traced: `WorkspaceOverridesProvider.apply()` calls `forkWorkspace(slug, {slot_overrides, slot_dict_overrides})` → backend returns the new slug → `navigate('/workspace/${new_slug}')` mounts the forked workspace.  Pre-flight validation (`validation.ok`) blocks malformed submissions; legacy non-forkable workspaces (template_id null) surface a static "Forkable workspaces only" affordance instead of throwing.  Pipeline is already covered by `__tests__/buildE2E.test.ts` (`overridesToServerPatch` round-trip) + `__tests__/regressionLock.test.ts §C` (call-site pinning).  No code change required.
4. **`event_study` / `backtest` backend payload quality** — user reported "garbage numbers". **Frontend chain verified clean for `event_study` (PR Decoder Audit, v3):** template node IDs match role bindings, dashboard renders honest fallbacks, `WorkflowResultCard` handles event-relative Series.  Any garbage numbers originate in the backend payload (slot bindings, operator output values) — fix lives outside this session.  `backtest` is paused on the backend; frontend correctly shows `WorkflowStatusCanvas` "paused" card.
5. **~~Operator intermediate-output surfacing~~** — **RESOLVED (PR Operator-Surfacing + Fork-Pipeline Audit, v4).**  Pre-flight read found the "Show all artifacts" toggle in `ResultsView.SpecialisedShell` already mounts `XAllArtifactsFallback` → `GenericResultsDashboard` rendering every non-terminal node via `NodeWidgetCard` → `resolveNodeRenderer` → per-artifact widget.  Operators dispatch identically to primitives (artifact-type-keyed).  PR renamed the toggle from "Show all artifacts" → "Show intermediate stages (N)" + tightened the description to explicitly mention operator outputs (vocabulary aligned with this §6).  Behavior 100% backwards compatible — same toggle, same state, same per-artifact rendering; only the user-facing copy + count changed.
6. **GenericPrimitiveBuilder output polish for the 32 tools with `monitor_elig=deferred` / scaffolded** — the schema-driven form + `AutoRenderer` works, but per-tool typed-result-renderers would raise the polish floor. This is per-module work; rows for those tools will move from `build_status=scaffolded` → `typed-renderer-built` as renderers land.

---

## 9. PR gates

A PR may land iff:

1. **Every surface code change has a corresponding row update.** Adding a Monitor widget for tool X requires updating the row `monitor_status: required → built`. Removing a stub requires updating `monitor_status: stub-claim → not-required` (or `→ deferred` if there's a planned proper widget).
2. **No central-file leakage.** PRs that add `case`/`if`/entries for a specific tool in any file under `src/components/{build,library,monitor,ask,layout}/` or in `src/lib/{toolNames,contextDecoder,modelRegistry,persistedModelAdapters}.ts` must be rejected unless the change is generic (i.e. applies to all tools symmetrically). Per-tool exceptions go inside the module folder.
3. **`tiers` claim matches delivery.** A module's `MODULE.tiers` array is a contract. Adding `monitor_surface` to the array requires shipping a non-stub widget in the same PR. Removing a tier requires removing the surface file.
4. **Tests track the contract.** `__tests__/module.spec.ts` calls `assertStandardModuleInvariants`, which already cross-checks tier claims against `surfaces.*` files. Strengthening the invariant to also reject stub widgets (e.g. via a minimum-line-count or a manifest of "approved-non-stub" files) is a separate hardening item.
5. **(v5) Lifecycle gates** — per [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) §7.  Tool-touching PRs additionally update the affected tool's lifecycle row in §10; static metadata edits go through the DB (`macro_data.tool_metadata`) with YAML mirrors updated in the same PR; methodology (config.yaml) changes require a parity-fixture regeneration + rationale + per-tool README update.

---

## 10. Lifecycle status (per tool — Phase 0+)

> Tracks the 7 lifecycle axes per tool per [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) §2.
>
> **This §10 is the roll-up summary view.**  The per-tool **granular truth** — every check-markable item across all 8 lifecycle stages — lives in `rates_agent/<domain>/tools/<tool>/LIFECYCLE_CHECKLIST.md` (or the operator / workflow analog), per [`../03_standards/lifecycle_checklist_template.md`](../03_standards/lifecycle_checklist_template.md).  The 4-value axis enum here (`shipped` / `in-progress` / `not-started` / `n/a`) maps to per-tool checklist stage-gate state per that standard's §7.  **Drift between this §10 row and the per-tool checklist is a PR-gate violation.**

### 10.1 Lifecycle axis status enum

For each tool × each of the 7 lifecycle axes, the status is one of:

| Status | Meaning |
|---|---|
| **`not-started`** | Axis not yet shipped for this tool. |
| **`in-progress`** | Work landed but not complete (e.g. pytest exists but SQL ground-truth doesn't). |
| **`shipped`** | Axis is complete per the lifecycle standard's definition. |
| **`n/a`** | Axis is not applicable to this tool (e.g. some tools have no Monitor widget by eligibility rule). |

### 10.2 Policy

- **Pilot tools (Phase 1)** — shown explicitly in §11.3 below.  All 7 axes get tracked.
- **All other 56 primitives** — defaulted to `not-started` for axes 1, 5, 7 (theoretical reference, known limitations, desk narrative — the human-curated DB fields); inherit existing status for axes 2, 3, 4, 6 (methodology, testing, contracts, frontend — most have partial state today).
- Per Phase 3 policy: a tool's lifecycle row gets fully filled only when the tool is touched for any reason.  No mass back-fill pass.

### 10.3 Pilot tools (Phase 1)

Two primitives selected per [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) §6.  Lifecycle rows for these will be populated in Phase 1; Phase 0 records the pilot selection only.

| tool_name | ax1 theoretical_ref | ax2 methodology (config) | ax3 testing (pytest + SQL + source) | ax4 input/output contracts | ax5 known_limitations | ax6 frontend surfaces | ax7 desk_narrative |
|---|---|---|---|---|---|---|---|
| `get_real_yield_level_tool` | shipped (DB tool_metadata.theoretical_reference filled; Tuckman 4e Ch. 22 + per-country primary issuers) | shipped (config.yaml + exposure: blocks for all 13 conventions per methodology_exposure.md) | in-progress (pytest + parity + SQL all green; source-material sign-off pending in Stage 8) | shipped (Pydantic Input + Metrics + Output + Phase-1 methodology-override fields) | shipped (DB tool_metadata.known_limitations filled) | shipped (BuildExtended + BuildCompact per rendering_density.md dual-view contract; Monitor RealYieldLevel widget; standalone typed-detail bridge endpoint) | shipped (DB tool_metadata.desk_narrative filled; manifest one_liner rewritten PM-facing) |
| `calculate_breakeven_inflation_simple_tool` | in-progress (curated in 2026-05-28 migration; live-DB apply + Stage-8 sign-off pending) | shipped (config.yaml + exposure: blocks for all 13 conventions per methodology_exposure.md) | in-progress (pytest incl. TestInputOverrides + TestExposureBlockContract + parity + SQL green; source-material sign-off pending Stage 8) | shipped (Pydantic Input + 3 Phase-1 override fields + Output) | in-progress (curated in migration; live-DB apply pending) | shipped (BuildExtended + BuildCompact per rendering_density.md dual-view; Monitor BreakevenInflation widget; standalone typed-detail bridge `/detail/breakeven`) | in-progress (desk_narrative curated in migration; live-DB apply pending; manifest one_liner rewritten PM-facing) |
| `calculate_real_yield_curve_spread_tool` | in-progress (curated in 2026-05-28 migration; live-DB apply + Stage-8 sign-off pending) | shipped (config.yaml + exposure: blocks for all 15 conventions per methodology_exposure.md) | in-progress (pytest incl. TestInputOverrides + TestExposureBlockContract + parity + SQL green; source-material sign-off pending Stage 8) | shipped (Pydantic Input + 3 Phase-1 override fields + Output) | in-progress (curated in migration; live-DB apply pending) | shipped (BuildExtended + BuildCompact per rendering_density.md dual-view; Monitor RealYieldCurveSpread widget; signed 5-zone regime slider; standalone typed-detail bridge `/detail/real_yield_curve_spread`) | in-progress (desk_narrative curated in migration; live-DB apply pending; manifest one_liner rewritten PM-facing) |

### 10.4 Workflow pilot — deferred

Workflows (`event_study`, `regime_conditioned_relationship`, `backtest`) are **out of scope for the primitive pilot**.  Workflow lifecycle standardisation happens after the primitive standard proves out — see [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) §5 (workflow caveats).

---

## 11. Change log

| Version | Date | Change |
|---|---|---|
| v1 | 2026-05-26 | Initial drafting. Populated all 58 primitive rows + 3 workflow rows. Identified 3 containment violations (CPI / NFP / OTR-OFR stub claims). Defined classifications + PR gates. |
| v2 | 2026-05-26 | Stage C resolution.  Retracted the 3 containment violations from v1: CPI / NFP modules dropped `monitor_surface` + `ask_surface` tier claims (stub widget + regression Ask card); OTR-OFR module dropped `monitor_surface` tier claim (stub widget).  All 3 modules now `generic_runnable` only.  OTR-OFR row updated: Monitor eligibility corrected from `not-required` to `required (deferred)` — it IS desk-glanceable but needs the typed-detail endpoint first. |
| v3 | 2026-05-26 | Decoder + event_study audit.  §8 item 2 (decoder coverage) marked RESOLVED — audit found the `isRunnablePrimitive` fallback already covers every module by construction; added a coverage assertion in `routingCoverage.test.ts` as a permanent CI guard.  §8 item 4 (event_study frontend) split — frontend chain verified clean; "garbage numbers" attributed to backend payload (out of session scope).  §5 `event_study` row notes updated with audit findings.  No production code changes; test + doc only. |
| v4 | 2026-05-26 | Operator-surfacing + fork-pipeline audit.  §8 item 5 (operator intermediate-output surfacing) marked RESOLVED — pre-flight read found the existing "Show all artifacts" toggle in `ResultsView.SpecialisedShell` already renders every non-terminal node through the per-artifact widget registry; PR renamed the toggle to "Show intermediate stages (N)" + tightened descriptions across `ResultsView` + `EventStudyDashboard` + `RegimeRelationshipDashboard` + `BacktestDashboard` for vocabulary alignment with §6.  §8 item 3 (workspace fork loop) marked RESOLVED — end-to-end traced; pipeline fully wired through `WorkspaceOverridesProvider.apply` → `forkWorkspace` API → `navigate` to the new slug; already covered by existing tests (`buildE2E.test.ts`, `regressionLock.test.ts §C`).  §6 operator visibility policy expanded with the shipped intermediate-step surfacing details.  `dashboardContract.test.ts` updated to lock the new toggle copy.  100% backwards compatible — same toggle, same state, same per-artifact rendering; only the user-facing copy + count changed. |
| v5 | 2026-05-26 | **Phase 0 of `revamp` branch.**  Contract extended to track tool lifecycle per the new [`../03_standards/tool_lifecycle.md`](../03_standards/tool_lifecycle.md) standard.  New §10 "Lifecycle status" tracks 7 axes per tool (theoretical_reference, methodology, three-way testing, input/output contracts, known_limitations, frontend surfaces, desk_narrative).  Existing §10 (changelog) renumbered to §11.  §0 + §9 updated to reference the lifecycle doc + the new lifecycle PR gate (#5).  Pilot trio scoped to TWO primitives (`get_real_yield_level_tool` + `calculate_breakeven_inflation_simple_tool`); workflows deferred until primitive standard proves out.  Companion ADR [`../05_decisions/0015-tool-metadata-db-table.md`](../05_decisions/0015-tool-metadata-db-table.md) lands the DB-backed metadata table (`macro_data.tool_metadata`) as the source of truth for units + theoretical reference + known_limitations + desk_narrative.  No code changes outside docs + database/ folders. |
| v6 | 2026-05-28 | **Rails for Phase-1 pilot.**  §10 prefaced with a paragraph clarifying that the section is the roll-up summary view; the **per-tool granular truth** lives in `rates_agent/<domain>/tools/<tool>/LIFECYCLE_CHECKLIST.md` (or operator / workflow analog) per two new sibling standards in `03_standards/`: [`lifecycle_checklist_template.md`](../03_standards/lifecycle_checklist_template.md) (the 8-stage assembly-line template, `☐/☑/⏸/⊘` status, `pending` treatment for source-material verification, per-tool ↔ §10-row mapping) and [`methodology_exposure.md`](../03_standards/methodology_exposure.md) (the per-convention `exposure:` YAML-block decision protocol consumed by Stage 1A + the standalone-module bridge contract — every new module ships its own typed-detail endpoint at `/api/v1/rates/detail/<tool_kind>` and full `BuildSurface.tsx`; the `MODULE.typedView` closed family is frozen for new modules).  Per-tool `LIFECYCLE_CHECKLIST.md` shipped for the first pilot tool `get_real_yield_level_tool`.  No row-level edits to §4 / §5 / §10.3.  No code changes outside `docs_revamped/` and the per-tool checklist file. |
| v7 | 2026-05-28 | **Dual-view rendering-density contract.**  §3.3 (Build states) rewritten: distinguishes single-tool query (mounts extended view) from multi-tool query / ad-hoc DAG (mounts compact view per node + expand-to-extended modal) from workflow-template execution (deferred to future workflow contract, provisional same-rule).  New sibling standard [`../03_standards/rendering_density.md`](../03_standards/rendering_density.md) mandates every new primitive ship BOTH `surfaces.buildExtended` AND `surfaces.buildCompact` — no opt-in, no tier-parsimony exception.  Legacy modules carved out and migrated opportunistically.  Multi-tool queries (LLM-stitched ad-hoc DAGs) explicitly disambiguated from workflow templates (`event_study` / `regime_conditioned_relationship` / `backtest`) — different backend concerns sharing the word "multi-tool".  No row-level edits to §4 / §5 / §10.3.  No code changes outside `docs_revamped/` and the per-tool checklist updates. |
| v8 | 2026-05-28 | **Phase-1 pilot `get_real_yield_level_tool` Stage A closeout.**  §4.3 row updated: `build_archetype: typed-result-renderer`, `build_status: dual-view-built` (new value semantically equivalent to `typed-renderer-built` but signals both `buildExtended` + `buildCompact` ship per rendering_density §1), `monitor_status: built (RealYieldLevel widget)`.  §10.3 row reflects Stages 1-3 (Backend PR) + Stages 4-6 (Frontend PR) + Stage 7 (this row update + per-tool README + manifest mirror) closed; Stage 8 (`source_material_verified`) stays `in-progress`.  Frontend shipped: 18-file shared-shell library at `UI/.../src/components/shared/build/` (finance-blind, reusable); per-tool wrappers `surfaces/{BuildExtended,BuildCompact,monitor/RealYieldLevelWidget}.tsx`; focused-mode infrastructure (collapses sidebar + chat rail when extended view mounts; edge toggles to re-open).  Defensive frontend sanity filter at `±6%` real-yield bounds catches Bloomberg generic-roll artifacts (TD #31).  Per-tool README at `rates_agent/inflation_indexed_bonds/tools/real_yield_level/README.md`.  Multi-tool DAG container (shared infra for hosting compact cards) is the next deliverable in Stages D-E of the closeout plan — out of scope for this v8 row. |
| v39 | 2026-06-10 | **Frontend factory batch-4 tool 32 / 35 — `get_yield_levels_tool` MIGRATION shipped (APPROVED).**  §4.1 row updated: `build_archetype: typed-result-renderer → dual-view-bridge`, `build_status: typed-renderer-built → dual-view-built`; monitor row preserved verbatim (`built (YieldLevel; YieldSnapshot also reads this domain)`) per `MIGRATION_RULES.md` widget-identity-preservation contract.  Second migration-mode tool of the factory (catalog `build_mode: migration`); previously shipped under the legacy typed-result-renderer surface (`surfaces/ResultRenderer.tsx` + `module.ts.typedView: 'yield'`), now converted to the standalone-bridge dual-view contract while keeping the `YieldLevelWidget` monitor widget (`id: 'yield_level'`, paramFields `curve_family` / `tenor` / `lookback_days` with defaults `UST` / `10Y` / `252`) unchanged.  Builder Round 1 performed the dual-view conversion (deleted `surfaces/ResultRenderer.tsx`; added `surfaces/BuildExtended.tsx` + `surfaces/BuildCompact.tsx` + `surfaces/yieldLevelShared.ts`; flipped `module.ts.typedView` to `null`, added `richModel: false`, surfaces flipped to `{ build, buildExtended, buildCompact }`; updated `__tests__/module.spec.ts` to dual-view + monitor-widget backward-compat lock checks per `MIGRATION_RULES.md` §6; updated `THESIS.md` to v3 5-question template with Migration audit section; updated central tests `buildE2E.test.ts` rows 7+14 kind 'yield' → 'generic_builder', `routingCoverage.test.ts` 4 substitutions to `calculate_cross_market_spread_tool` typed-view stand-in, `genericBuilder.test.ts` typed-view middle-entry substitution).  Reviewer Round 2 emitted `APPROVED` with no findings — Section H 12 migration-specific checks all ✓ (`surfaces/ResultRenderer.tsx` deleted ✓, `module.ts.typedView: null` ✓, no `workspaceLabel` / `unsupportedReason` ✓, `YieldLevelWidget` path + id + paramFields preserved ✓, `YieldLevelOutput` appears 1× in `src/types/rates.ts:185` ✓, `fetchDetailYield` appears 1× in `src/services/ratesApi.ts:129` ✓, `/detail/yield` route appears 1× in `api/routes/rates/detail.py:660` ✓, `BuildExtended.tsx` + `BuildCompact.tsx` + `yieldLevelShared.ts` are NEW ✓, THESIS v3 enumerates buildExtended + buildCompact + preserved widget in Q1 ✓, `module.spec.ts` calls `assertStandardModuleInvariants` + dual-view + widget-identity checks ✓, `module.ts.tiers` = `[generic_runnable, custom_build_surface, monitor_surface]` matches catalog `required_tiers` ✓, all three gates exit 0 ✓, `BuildCompact` and `YieldLevelWidget` are two different files with two different exports ✓).  No Dispatch 3 fix pass needed (APPROVED first time — single-round per `SINGLE_REVIEW_ROUND_POLICY.md`).  Standalone typed-detail endpoint at `/api/v1/rates/detail/yield` (pre-existing — bridge reuses the legacy route, sibling to `get_real_yield_level_tool`); `YieldLevelOutput` mirror type in `types/rates.ts:185` reused unchanged.  Compact density: shell-standard (3 KPIs YIELD / 1D CHANGE / Z-SCORE 252D + mini-chart with ±2σ z-score envelope + footer caveat + expand affordance) per Option (c) precedent — the mockup's auxiliary 6-KPI footer strip (5D / 1M / PCTL / 252D HIGH / 252D LOW / OBSERVATIONS) lands in the Extended view's 9-cell KPI strip; not rendered in Compact (BuildCompactShell enforces a 3-KPI contract).  Extended view methodology card documents the z-score model (252-day rolling, ddof=1) via config conventions; the backend `YieldLevelMetrics` Pydantic schema does NOT carry a `methodology_label` field today (verified via grep) — TODO(PR10) marker in `yieldLevelShared.ts` and THESIS Q4 for when the backend ships the field.  Reference frontend module: `get_real_yield_level_tool` (the real-yield sibling — same dual-view shape, same shared-helper pattern, same standalone-bridge convention).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`get_yield_levels_tool: 7 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 32 done / 0 blocked / 3 remaining (next eligible: build_order 33 `sovereign__butterfly__migrate`). |
| v38 | 2026-06-10 | **Frontend factory batch-4 tool 31 / 35 — `calculate_curve_spread_tool` MIGRATION shipped (APPROVED-AFTER-FIX).**  §4.1 row updated: `build_archetype: typed-result-renderer → dual-view-bridge`, `build_status: typed-renderer-built → dual-view-built`; monitor row preserved verbatim (`built (2 widgets: CurveSpreads, SpreadChart)`) per `MIGRATION_RULES.md` widget-identity-preservation contract.  First migration-mode tool of the factory (catalog `build_mode: migration`); previously shipped under the legacy typed-result-renderer surface (`surfaces/ResultRenderer.tsx` + `module.ts.typedView: 'curve_spread'`), now converted to the standalone-bridge dual-view contract while keeping monitor widget identities unchanged.  Builder Round 1 performed the dual-view conversion (deleted `surfaces/ResultRenderer.tsx`; added `surfaces/BuildExtended.tsx` + `surfaces/BuildCompact.tsx` + `surfaces/curveSpreadShared.ts`; flipped `module.ts.typedView` to `null`; updated `__tests__/module.spec.ts` to dual-view+standalone-bridge contract; updated `THESIS.md` to v2 5-question contract with `Mockup-conformance` section).  Reviewer Round 2 emitted `CHANGES REQUIRED` with 9 concrete + actionable + bounded findings: 7 central-test updates (`buildE2E.test.ts` rows 6+13 typedView 'spread' → null + ask 'spread' → 'generic_builder'; `routingCoverage.test.ts` direct curve_spread decode + 2 priority tests + workflow_incompatible decodeList + generic_builder+unsupported decodeList; `genericBuilder.test.ts:349` typed-view sibling substitution) + finding 8 (Extended top-right card 3 'sovereign_caveat' duplicate → 'regime' using `shapeRegimeCaption` × `zScoreCaptionForSpread` with `signedFixed(current_spread_bps, 1) bp` headline) + finding 9 (THESIS Q3 'Documented density deviation' paragraph naming 6-cell mockup strip / 3-KPI implementation gap with Option (c) Batch 1 fdac7d2 precedent citation).  Builder Round 3 applied all 9 findings; orchestrator-side post-fix gate green.  Standalone typed-detail endpoint at `/api/v1/rates/detail/curve-spread` (pre-existing — bridge reuses the legacy route); `CurveSpreadOutput` mirror type in `types/rates.ts` (one-line addition for `methodology_label` field readability); per-leg short/long tenor + bps spread / z-score / methodology_label exactly mirror the backend Pydantic `CurveSpreadOutput` schema.  Methodology disclosure wire-sourced from `current_metrics.methodology_label` via `buildMethodologyRows` (Extended `MethodologyCard` "Disclosure" row in `surfaces/curveSpreadShared.ts`); the `SOVEREIGN_CURVE_SPREAD_COMPACT_CAVEAT` constant was removed from `BuildExtended.tsx` after the finding-8 fix replaced the duplicate caveat card with a regime-classification card.  Monitor widget identities (`CurveSpreads` + `SpreadChart` — 2 widgets) preserved by ID per `MIGRATION_RULES.md` §H1-H7 backward-compat invariant; reviewer Section H ran the 12 migration-specific checks (legacy file deleted ✓, central type / service / route NOT duplicated ✓, monitor widget identity unchanged ✓, `module.ts.typedView` flipped to `null` ✓, tier set includes `custom_build_surface` ✓, shared helper consolidates per-surface duplication ✓, dual-view per-rendering_density §1 ✓, mockups present ✓, FM cites in THESIS ✓, methodology honesty wire-sourced ✓, central tests updated for typed-view drop ✓, `surfaces/ResultRenderer.tsx` not re-introduced ✓).  Reference frontend module: the most-recently-shipped dual-view sovereign bond tool (otr_ofr_spread `dual-view-bridge` pattern; reviewer Section F confirms shared-helper discipline + monitor widget per-leg identity-preserve follows reference precedent).  Shared `curveSpreadShared.ts` consolidates `CURVE_REGISTRY`, `OPTIONS`, `useCurveSpreadData` data hook, `compactKPIs` / `extendedKPIs`, reference-bands / stretch-context / methodology-rows builders, `shapeRegimeCaption` × `zScoreCaptionForSpread` + `signedFixed` regime-classifier shared helpers across BuildExtended + BuildCompact (Monitor widgets continue to read the workflow result envelope unchanged — bridge endpoint optional from the widget's vantage point).  Compact density: shell-standard (3 KPIs SPREAD / 1D CHANGE / Z-SCORE 252D + sparkline with ±2σ z-bands + per-leg / per-curve identity chip footer + per-leg-pair short caveat) per Option (c) precedent (catalog `design_guardrail` #1 — "keep shell-standard density on Compact; mockup's 6-cell auxiliary strip documented as a deviation in THESIS Q3"); the 6-cell mockup strip / 3-KPI implementation gap is documented verbatim in THESIS Q3's `Documented density deviation` paragraph with the Batch 1 fdac7d2 (`calculate_breakeven_butterfly_tool`) precedent citation per the finding-9 fix.  Reviewer (fresh-context Claude) emitted `CHANGES REQUIRED` with concrete findings (no STRUCTURAL / vague / blast-radius-excessive / mockup-contradiction / hallucinated-reference flags per `SINGLE_REVIEW_ROUND_POLICY.md` §3) — routed to Dispatch 3 fix pass per Case D.  Pre-commit gate re-run in orchestrator session post-Dispatch 3 fix: `npm run test:modules` ✓ (`calculate_curve_spread_tool: 8 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 31 done / 0 blocked / 4 remaining (next eligible: build_order 32). |
| v37 | 2026-06-10 | **Frontend factory batch-4 tool 30 / 35 — `calculate_otr_ofr_spread_tool` dual-view shipped (APPROVED).**  §4.1 row updated: `build_archetype: generic-builder → dual-view-bridge`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (OtrOfrSpreadWidget)`.  Final containment-violation remediation from v2 closed — OTR-OFR's monitor surface, dropped at v2 pending the typed-detail endpoint, is now restored at the bridge contract.  Standalone typed-detail endpoint at `/api/v1/rates/detail/otr-ofr-spread` (`api/routes/rates/detail.py` with `response_model=OtrOfrSpreadOutput`); `OtrOfrSpreadOutput` (+ `OtrOfrSpreadCurrentMetrics` + `OtrOfrSpreadTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output; per-leg `otr_yield_pct` / `ofr_yield_pct` PERCENT scalars; spread + z-score / percentile in BPS; `methodology_note` top-level — the load-bearing TD #27b auction-detection-date caveat is sourced from wire not TSX literal).  Mockup deviation (omitted "Days Since Auction" chip + "Auction Context" panel) documented in `THESIS.md` Mockup-conformance section with backend-constraint rationale (Pydantic `OtrOfrSpreadOutput` exposes no `effective_from` / auction-date field; TD #27b's detection-date precision caveat makes the resolver's `effective_from` an unsafe proxy — correct per "don't fabricate"); short-caveat strings ("Liquidity-premium PROXY — sign POSITIVE = OTR cheap to OFR.", "OTR premium — auction-cycle dependent; liquidity-premium proxy.") are tool-design hints in the top-right card / compact fallback, NOT the methodology disclosure, mirroring the reference `breakevenShared.ts` pattern.  Shared `otrOfrSpreadShared.ts` consolidates `COUNTRY_REGISTRY`, `OPTIONS`, `useOtrOfrSpreadData` hook, `compactKPIs` / `extendedKPIs`, reference-bands / stretch-context / methodology-rows builders across all three surfaces (BuildExtended + BuildCompact + OtrOfrSpreadWidget). Reference frontend module: `calculate_breakeven_inflation_simple_tool` (Phase-1 pilot pattern; reviewer Section F confirms shared-helper discipline + monitor-widget thin local fetcher follows reference precedent).  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings; verification scorecard green across dual-view contract, mockup conformance (with documented backend-constraint deviation), standalone bridge, FM1–FM12, methodology honesty, shared cross-surface helper, and gate readiness. Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`calculate_otr_ofr_spread_tool: 8 passed`; all module + registry tests passed) `npm run test:build` ✓ `npm run typecheck` ✓. `graphify update` skipped — not on PATH on this VM. Catalog state: 30 done / 0 blocked / 5 remaining. |
| v36 | 2026-06-10 | **Frontend factory batch-4 tool 29 / 35 — `calculate_cross_country_real_yield_spread_simple_tool` dual-view shipped (APPROVED).**  §4.3 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (CrossCountryRealYieldSpread widget)`.  First tool of the batch-4 catalog extension (build_order 29 of 35; tools 19-28 of batch-3 shipped at v27-v35); same-tenor cross-country linker real-yield spread between two sovereign linker curves (UK-US 10Y real yield default; supports the four cross-country linker pairs USD_TIPS / GBP_LINKER / EUR_OATEI / CAD_RRB) — the desk-canonical read for cross-currency REAL-RATE divergence (distinct from the v34 cross-country BREAKEVEN sibling: that spread isolates inflation-expectations divergence, this spread isolates real-rate divergence).  Standalone typed-detail endpoint at `/api/v1/rates/detail/cross-country-real-yield-spread` (`api/routes/rates/detail.py` with `response_model=CrossCountryRealYieldSpreadSimpleOutput`); `CrossCountryRealYieldSpreadSimpleOutput` (+ `CrossCountryRealYieldSpreadSimpleCurrentMetrics` + `CrossCountryRealYieldSpreadSimpleTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output incl. **load-bearing PERCENT-vs-BPS shape delta vs the breakeven sibling**: `current_spread_pct` / `high_252d_pct` / `low_252d_pct` / `first_curve_real_yield_pct` / `second_curve_real_yield_pct` are PERCENT scalars — real yields are quoted in PERCENT, not BPS — while period changes remain `_bps` for desk convention; also `z_score_252d`, `percentile_252d`, `observation_count`, per-leg index-family identifiers — `first_inflation_index` / `second_inflation_index` — and `methodology_label` on `current_metrics`).  **Two load-bearing caveats surfaced wire-honestly on BOTH views and the Monitor widget**: (1) index-family mismatch (CPI-U vs RPI vs HICPxT vs Canadian CPI are NOT fungible inflation measures — same as cross-country breakeven sibling), and (2) cross-country linker market-structure differences (liquidity / issuance / OTR-OFR — distinct from the breakeven sibling's caveat set).  `cm.methodology_label` flows from wire → `buildMethodologyRows` (Extended `MethodologyCard` "Disclosure" row) and → Monitor widget z-badge `title=` tooltip; Compact footer renders the per-pair short caveat via `shortIndexCaveat()` derived deterministically from the `(first_linker, second_linker)` pair identifiers using the in-helper `CURVE_REGISTRY`; no hardcoded long-form disclosure prose in TSX files.  Reference frontend module: `calculate_cross_country_breakeven_spread_simple_tool` (batch-3 v34; same cross-country two-curve shape — different signal, different units).  Shared `crossCountryRealYieldSpreadSimpleShared.ts` helper consolidates `CURVE_REGISTRY` (USD_TIPS / GBP_LINKER / EUR_OATEI / CAD_RRB linker triplets with per-country short labels + flag glyphs + canonical desk subtitles + nominal-curve + inflation-index identifiers), `CURVE_FAMILY_OPTIONS`, `TENOR_OPTIONS`, `useCrossCountryRealYieldSpread` data hook, `compactKPIs`, `extendedKPIs`, `buildReferenceBands`, `buildStretchContext`, `buildMethodologyRows`, `shortIndexCaveat`, `pairShortLabel`, `linkerCurveFor`, `sanitiseSpreadSeries`, `buildReferenceChips` across all three surfaces (BuildExtended + BuildCompact + CrossCountryRealYieldSpreadWidget) — no duplication; canonical historical series ships in PERCENT (real-yield-spread convention, matching the catalog's `endpoint_slug: cross-country-real-yield-spread`).  Compact density: shell-standard (3 KPIs SPREAD pct / 1D CHANGE bps / Z-SCORE 252D + sparkline with ±2σ z-bands on a PERCENT axis + per-leg flag chips footer + per-pair index-family-mismatch + market-structure short caveat) per Option (c) precedent (catalog `design_guardrail` #4 — "keep shell-standard density on Compact; document deviation in THESIS"); the Compact mockup's secondary KPI row is not rendered by the shared `BuildCompactShell` which enforces a 3-KPI contract — deviation documented verbatim in THESIS Q3 + Mockup-conformance section.  Cross-country invariant enforced at the shared-hook level (`useCrossCountryRealYieldSpread` rejects same-curve pairs with explicit error mirroring the schema-layer check) AND at the controls level (`BuildExtended.handleControlChange` snap-fallback when the user picks identical first/second curves — keeps the dropdown from putting the surface in a bad state).  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2 — Compact obeys NO controls strip, `onExpand` prop, methodology caveat reachable via footer, signed tones via shared shell, `size` prop accepted; Extended ships full canvas with controls strip / 11-cell KPI strip / chart with ±2σ z-band overlay on PERCENT axis / top-right Z-Score / Percentile / Index-Families cards / methodology card / lineage footer), mockup conformance (Extended identity row "UK-US 10Y Real Yield" with 🇬🇧🇺🇸 flag pair + 11-cell KPI strip ordering + top-right Z/Percentile/Index-Families cards + ±2σ z-band overlay on PERCENT axis + methodology card + lineage footer all mirror Extended.png; Compact 3-KPI strip + sparkline with ±2σ bands + footer caveat + expand affordance follows Option (c) shell-density precedent), standalone bridge (methodology_exposure.md §5; `typedView: null` at `module.ts:46`; per-tool detail endpoint at `/api/v1/rates/detail/cross-country-real-yield-spread`; `fetchDetailCrossCountryRealYieldSpread` added to `ratesApi.ts`), FM1–FM12 (FM1 folder == MCP `tool.name`; FM3 tier set claims `generic_runnable` + `custom_build_surface` + `monitor_surface`; FM7 pure-value `module.ts` export — no side effects; FM8 all claimed tiers wired at canonical paths; FM10 THESIS Q1/Q2/Q3/Q5 substantive with per-surface specificity in Q2, alternatives-rejected reasoning for Compact's 3 KPIs in Q3, and FM1/FM3/FM4/FM5/FM7/FM8/FM9/FM10/FM11/FM12 + P5 cites in Q5; FM11 `__tests__/module.spec.ts` calls `assertStandardModuleInvariants` + dual-view + monitor-widget + mockup-folder checks; FM12 loader entry alphabetical at `src/modules/index.ts:51` between the breakeven sibling and `calculate_cross_market_inflation_swap_spread_tool`), methodology honesty (PR10 / P5: `methodology_label` row sourced from `cm.methodology_label` not hardcoded; per-pair short caveat via deterministic `shortIndexCaveat()` helper derived from curve-family identifiers in the registry — no duplicated prose), shared cross-surface helper (`BuildExtended.tsx` + `BuildCompact.tsx` + `CrossCountryRealYieldSpreadWidget.tsx` all import from shared — no duplication; Widget imports `linkerCurveFor`, `pairShortLabel`, `shortIndexCaveat`).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`calculate_cross_country_real_yield_spread_simple_tool` per-folder spec passed; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 29 done / 0 blocked / 6 remaining (next eligible: build_order 30 `sovereign__otr_ofr_spread`). |
| v35 | 2026-06-08 | **Frontend factory batch-3 tool 27 / 28 — `compute_financing_rate_tool` dual-view shipped (APPROVED).**  §4.10 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (FinancingRate widget)`.  Ninth tool of the batch-3 catalog extension (tools 19–26 shipped at v27–v34); per-tenor repo / specials financing rate (UST 10Y financing default; supports the six OIS-proxy curve families USD_SOFR / EUR_ESTR / GBP_SONIA / JPY_TONA / AUD_AONIA / CAD_CORRA) — the desk-canonical read for repo-market dislocations and bond-specific scarcity drift.  **First-of-its-kind architectural deviation: ROUTE-SIDE SYNTHESIS** (Option (a) chosen by human at `9acaa31`).  `FinancingRateOutput` (`rates_agent/ois/tools/financing_rate/schemas.py:148-187`) returns a Panel-shaped result (method, as_of_start/end, n_observations, mean_rate_pct scalar, methodology_disclosures List[str], panel Optional[Panel]) — does NOT carry `current_metrics` or `time_series` like every other snapshot tool.  Backend Output preserved as-is (Panel-based for the `evaluate_trades` workflow consumer; the MCP layer drops the panel for LLM serialisation); the route handler at `/api/v1/rates/detail/financing-rate` synthesizes the standard snapshot shape (`FinancingRateCurrentMetrics` + `FinancingRateTimeSeriesRow` + `FinancingRateDetailResponse`) from `result.panel.payload`: extracts the single-column DataFrame as a Series (PERCENT), computes latest / daily change bps / 252d rolling mean+std → z_score / max+min → high_252d_pct / low_252d_pct / rank → percentile_252d, joins the `methodology_disclosures` plural list with `' '` into a singular `methodology_disclosure` string.  Frontend module is structurally identical to other snapshots — the architectural deviation is invisible above the typed-detail boundary.  `THESIS.md` carries a `## Backend shape note` subsection documenting (1) why `FinancingRateOutput` diverges, (2) where synthesis happens (route, not backend), (3) why the frontend is structurally identical, and the rejected alternatives (Option b: modify backend schema → would break `evaluate_trades`; Option c: ship Panel-only frontend → would force a one-off frontend pattern).  Standalone typed-detail endpoint at `/api/v1/rates/detail/financing-rate` (`api/routes/rates/detail.py` with `response_model=FinancingRateDetailResponse` + file-header architectural-deviation comment immediately before the route); `FinancingRateCurrentMetrics` + `FinancingRateTimeSeriesRow` + `FinancingRateTimeSeries` + `FinancingRateDetailResponse` mirror types in `types/rates.ts` (snake_case preserved; mirror the SYNTHESIZED shape, not the backend Output); `fetchDetailFinancingRate` + `FinancingRateDetailParams` service helper in `services/ratesApi.ts`.  Shared `financingRateShared.ts` helper consolidates `PROXY_CURVE_REGISTRY` (USD_SOFR / EUR_ESTR / GBP_SONIA / JPY_TONA / AUD_AONIA / CAD_CORRA proxy-curve triplets with short labels + bond-family + canonical OIS-index identifiers), `useFinancingRate` data hook, `compactKPIs`, `extendedKPIs`, `buildReferenceBands`, `buildStretchContext`, `buildMethodologyRows`, `buildReferenceChips`, `compactCaveatText` across BuildExtended + BuildCompact + FinancingRateWidget — no duplication; canonical historical series ships in PERCENT (rate convention, matching `endpoint_slug: financing-rate`).  Compact density: shell-standard (3 KPIs FINANCING RATE / 1D CHANGE / Z-SCORE 252D + mini-chart with reference bands + one-line caveat footer + expand affordance) per Option (c) precedent (catalog `design_guardrail` #9 — "keep shell-standard density on Compact; the mockup's auxiliary 6-KPI strip lands in the Extended view's 9-cell strip — same data, different surface"); the deviation is documented verbatim in THESIS Q3 + Mockup-conformance section.  Methodology disclosure wire-honest: `data.methodology_disclosure` (joined backend `methodology_disclosures: List[str]`) drives the Extended `MethodologyCard` "Disclosure" row + the Compact caveat footer (first sentence) — never a TSX literal in either surface file; auxiliary "Caveat" row in `buildMethodologyRows` ("OIS proxy approximates true overnight repo; does NOT reflect CUSIP-level specials...") preserves the reference-module pattern.  Identity: primary uses bond-family short label ("UST"); secondary reads "Financing · SOFR" with the proxy-curve descriptor — the mockup's "10Y" sub-identifier was omitted because the catalog architectural override explicitly removed `tenor` from the V1 input contract ("no tenor — financing is an overnight quantity").  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2 — Compact obeys NO controls strip, `onExpand` prop, methodology disclosure reachable via footer caveat, signed tones via shared shell, `size` prop accepted; Extended ships full canvas with controls strip / 9-cell KPI strip / main chart with ±2σ z-bands / stretch-context panel / methodology card / lineage footer), mockup conformance (Extended identity row "UST · Financing · SOFR" with as-of + top-right Z-Score / Percentile / Method cards + controls strip method · proxy_curve · lookback + 9-cell KPI strip + main chart with z-bands + stretch-context panel + methodology card + lineage footer; Compact title row "OIS · SNAPSHOT" + identity "UST · Financing" + three headline KPIs in mockup order + sparkline with reference bands + one-line caveat footer + expand affordance), standalone bridge (methodology_exposure.md §5; `typedView: null`; per-tool detail endpoint at `/api/v1/rates/detail/financing-rate` with `response_model=FinancingRateDetailResponse`), FM1–FM12 (FM1 folder == MCP `tool.name` `compute_financing_rate_tool`; FM3 tier set claims `generic_runnable` + `custom_build_surface` + `monitor_surface`; FM7 pure-value `module.ts` export — no side effects; FM8 all claimed tiers wired at canonical paths; FM10 THESIS Q1/Q2/Q3/Q5 substantive with per-surface specificity in Q2, alternatives-rejected reasoning for Compact's 3 KPIs in Q3, and FM IDs + P5 + methodology_exposure §5 + rendering_density §1 cites in Q5; FM11 `__tests__/module.spec.ts` calls `assertStandardModuleInvariants` + 7 checks asserting `custom_build_surface` / `monitor_surface` / `buildExtended` / `buildCompact` / `typedView == null` / monitor widget id present / mockups exist; FM12 loader entry pre-existing at `src/modules/index.ts:77` (import) + `:150` (array) alphabetical — no edit required), methodology honesty (PR10 / P5: `methodology_disclosure` row sourced from `data.methodology_disclosure` at `financingRateShared.ts:398`, not hardcoded; compact caveat from same backend field at `financingRateShared.ts:431-444`), shared cross-surface helper (`BuildExtended.tsx` + `BuildCompact.tsx` + `FinancingRateWidget.tsx` all import from shared — no duplication; Monitor widget defines its own data-fetch hook locally, matching reference module `get_real_yield_level_tool`'s pattern).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`compute_financing_rate_tool: 7 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed; 23 bundles, 27 e2e, ~500 individual checks) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 27 done / 0 blocked / 1 remaining (next eligible: build_order 28 `ois__swap_spread`). |
| v34 | 2026-06-08 | **Frontend factory batch-3 tool 26 / 28 — `calculate_cross_country_breakeven_spread_simple_tool` dual-view shipped (APPROVED).**  §4.3 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (CrossCountryBreakevenSpread widget)`.  Eighth tool of the batch-3 catalog extension (tools 19–25 shipped at v27/v28/v29/v30/v31/v32/v33); cross-country breakeven inflation spread at a common tenor (UK-US 10Y BE default; supports UK/US, EUR-US, CA-US, EUR-UK ordered pairs across CPI-U / HICPxT / RPI / Canadian CPI index families) — the desk-canonical read for cross-currency inflation-expectations divergence, exposing same-tenor breakeven differentials independent of any one country's nominal level.  Standalone typed-detail endpoint at `/api/v1/rates/detail/cross-country-breakeven-spread` (`api/routes/rates/detail.py` with `response_model=CrossCountryBreakevenSpreadSimpleOutput`); `CrossCountryBreakevenSpreadSimpleOutput` (+ `CrossCountryBreakevenSpreadSimpleCurrentMetrics` + `CrossCountryBreakevenSpreadSimpleTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output incl. signed `spread_bps`, per-leg BE A/BE B values, change_1d/5d/1m_bps, z_score_252d, percentile_252d, observation_count, 252d HI/LO bounds, per-leg index-family identifiers — `country_a_inflation_index` / `country_b_inflation_index` — for the load-bearing index-family-mismatch caveat, and `methodology_label` on `current_metrics`).  **Load-bearing index-family-mismatch caveat surfaced wire-honestly on BOTH views and the Monitor widget**: the cross-country variant nearly always has mismatched inflation indices (RPI vs CPI-U; HICPxT vs CPI-U; Canadian CPI vs CPI-U) — the caveat is folded into `methodology_label` at the backend (schema does NOT expose a separate `index_family_caveat` field, unlike the cross-market ZCIS sibling), and `crossCountryBreakevenSpreadSimpleShared.ts:buildMethodologyRows` threads `cm.methodology_label` verbatim into the Extended `MethodologyCard` Disclosure row; Compact footer renders the per-pair short caveat via `shortIndexCaveat()` derived deterministically from the `(nominal, linker)` pair identifiers using the in-helper `PAIR_REGISTRY`; Monitor widget z-badge `title={m.methodology_label}` surfaces the wire's full disclosure on hover.  Reference frontend module: `calculate_cross_market_inflation_swap_spread_tool` (Batch 1 `302a388`; same cross-market spread shape — different curve family).  Shared `crossCountryBreakevenSpreadSimpleShared.ts` helper consolidates `PAIR_REGISTRY` (UK-US / EUR-US / CA-US / EUR-UK ordered country-pair triplets with per-leg short labels + flag glyphs + canonical desk subtitles), `COUNTRY_PAIR_OPTIONS`, `TENOR_OPTIONS`, `useCrossCountryBreakevenSpread` data hook, `compactKPIs`, `extendedKPIs`, `buildReferenceBands`, `buildStretchContext`, `buildMethodologyRows`, `shortIndexCaveat`, `pairShortLabel`, `identitySubtitle`, `countryPairFor`, `sanitiseSpreadSeries`, `buildReferenceChips` across all three surfaces (BuildExtended + BuildCompact + CrossCountryBreakevenSpreadWidget) — no duplication; canonical historical series ships in BPS (spread convention, matching the catalog's `endpoint_slug: cross-country-breakeven-spread`).  Compact density: shell-standard (3 KPIs SPREAD bps / 1D CHANGE bps / Z-SCORE 252D + sparkline with ±2σ z-bands + per-leg flag chips footer + RPI vs CPI-U short caveat) per Option (c) precedent (catalog `design_guardrail` #4 — "keep shell-standard density on Compact; document any deviation in THESIS"); the mockup's denser 9-KPI Compact layout was elided in favour of the shell-standard 3-KPI density per the guardrail and the deviation is documented verbatim in THESIS Q3 + Mockup-conformance section.  Cross-country invariant enforced both at the shared-hook level (`useCrossCountryBreakevenSpread` rejects same-nominal / same-linker pairs with explicit error mirroring the schema-layer check) AND at the controls level (`BuildExtended.handleControlChange` snap-fallback when the user picks identical Country A/B — keeps the dropdown from putting the surface in a bad state).  `defaultParams.lookback_days: '365'` (calendar-day) vs Monitor widget `defaultValue: '252'` (trading-day) — identical pattern to the reference sibling `calculate_cross_market_inflation_swap_spread_tool` (Batch 1); reviewer judged this not a regression.  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2 — Compact obeys NO controls strip, `onExpand` prop, methodology disclosure reachable via footer caveat, signed tones via shared shell, `size` prop accepted; Extended ships full canvas with controls strip / KPI strip / chart / methodology card / lineage footer), mockup conformance (Extended identity row "UK-US 10Y BE" with 🇬🇧🇺🇸 flag pair + per-country subtitle "UK 10Y BE (RPI) minus US 10Y BE (CPI-U)" + 11-cell bps KPI strip + spread-history chart with ±2σ/±1.5σ z-bands + stretch-context panel + methodology card + lineage footer; Compact title row "Cross-Country Bond Breakeven Spread · SNAPSHOT" + identity "UK-US 10Y BE" + per-country subtitle + three headline KPIs in mockup order + sparkline with z-band overlays + footer caveat "RPI vs CPI-U · not fungible inflation measures" with per-country flag chips), standalone bridge (methodology_exposure.md §5; `typedView: null` at `module.ts:46`; per-tool detail endpoint at `api/routes/rates/detail.py:3947-4054` with `response_model=CrossCountryBreakevenSpreadSimpleOutput`), FM1–FM12 (FM1 folder == MCP `tool.name`; FM3 tier set claims `generic_runnable` + `custom_build_surface` + `monitor_surface`; FM7 pure-value `module.ts` export — no side effects; FM8 all claimed tiers wired at canonical paths; FM10 THESIS Q1/Q2/Q3/Q5 substantive with per-surface specificity in Q2, alternatives-rejected reasoning for Compact's 3 KPIs in Q3, and FM IDs + P5 + methodology_exposure §5 + rendering_density §1 cites in Q5; FM11 `__tests__/module.spec.ts` calls `assertStandardModuleInvariants` + 8 checks asserting `custom_build_surface` / `monitor_surface` / `buildExtended` / `buildCompact` / `typedView == null` / monitor widget id present / mockups exist; FM12 loader entry pre-existing at `src/modules/index.ts:50` (import) + `:123` (array) alphabetical between `..._butterfly_simple_tool` and `..._real_yield_spread_simple_tool` — no edit required), methodology honesty (PR10 / P5: `methodology_label` row sourced from `cm.methodology_label` at `crossCountryBreakevenSpreadSimpleShared.ts:563-566`, not hardcoded; per-leg caveat via deterministic `shortIndexCaveat()` helper derived from `(nominal, linker)` identifiers, not duplicated prose), shared cross-surface helper (`BuildExtended.tsx:38-52` + `BuildCompact.tsx:19-28` + `CrossCountryBreakevenSpreadWidget.tsx:18-22` all import from shared — no duplication).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`calculate_cross_country_breakeven_spread_simple_tool: 8 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 26 done / 0 blocked / 2 remaining (next eligible: build_order 27 `ois__financing_rate`). |
| v33 | 2026-06-08 | **Frontend factory batch-3 tool 25 / 28 — `calculate_swap_breakeven_basis_simple_tool` dual-view shipped (APPROVED).**  §4.4 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (SwapBreakevenBasis widget)`.  Seventh tool of the batch-3 catalog extension (tools 19/20/21/22/23/24 shipped at v27/v28/v29/v30/v31/v32); swap-breakeven basis at a single tenor (ZCIS rate minus bond-implied breakeven, signed; positive = ZCIS rich vs bond breakeven) — desk-canonical RV read on inflation-market dislocations across the ZCIS / nominal / linker triplet (USD_ZCIS+UST+USD_TIPS at 10Y default; EUR_ZCIS+EUR_BUND+EUR_OAT_LINKER; GBP_ZCIS+UKT+GBP_LINKER).  Standalone typed-detail endpoint at `/api/v1/rates/detail/swap-breakeven-basis` (`api/routes/rates/detail.py` with `response_model=SwapBreakevenBasisSimpleOutput`); `SwapBreakevenBasisSimpleOutput` (+ `SwapBreakevenBasisSimpleCurrentMetrics` + `SwapBreakevenBasisSimpleTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output incl. signed `basis_pct` + `basis_bps`, ZCIS / bond / nominal-yield / real-yield decomposition, change_1d/1w/1m_bps, z_score_252d, percentile_252d, observation_count, ZCIS index-family triple — `zcis_inflation_index_family` + `zcis_index_lag` + `zcis_interpolation` + `zcis_underlying_index`, linker mirror fields, load-bearing `index_families_match: bool` + `index_family_caveat: Optional[str]` wire fields, and `methodology_label` on `current_metrics`).  **Wire-honesty load-bearing caveat surfaced on BOTH views and the Monitor widget**: when `index_families_match=False` the structured `index_family_caveat` is consumed verbatim by `swapBreakevenBasisShared.ts:buildMethodologyRows` (Extended `MethodologyCard` "Caveat" row) + `BuildCompact.tsx` footer + `SwapBreakevenBasisWidget.tsx` `title=` tooltip — never as a TSX literal; the static `fallbackBasisCaveat()` / `SWAP_BREAKEVEN_BASIS_COMPACT_CAVEAT` fire only in the loading state or when the wire returns null (matching families); methodology disclosure `methodology_label` is wire-sourced via `data.current_metrics.methodology_label` (NOT `methodology_disclosure` on top-level Output — schema names this field on `current_metrics`).  Reference frontend module: `calculate_breakeven_inflation_simple_tool` (Phase-1 pilot; same "single-tenor cross-market spread between ZCIS and bond-implied breakeven" KPI shape).  Shared `swapBreakevenBasisShared.ts` helper consolidates the pair registry (USD/EUR/GBP triplets), tenor maps, `useSwapBreakevenBasis` data hook, `compactKPIs` / `extendedKPIs` builders, reference bands, stretch context, methodology rows, caveats across all three surfaces (BuildExtended + BuildCompact + SwapBreakevenBasisWidget) — no duplication; canonical historical series ships in BPS (spread convention; per `time_series_basis`'s closed-enum `TimeSeriesUnits.BPS`, not PERCENT).  Compact density: shell-standard (3 KPIs BASIS bps / 1D CHANGE bps / Z-SCORE 252D + sparkline with ±2σ z-bands + caveat footer) per Option (c) precedent (catalog `design_guardrail` #4); secondary KPI row in mockup (5D/1M/PCT/HIGH/LOW/OBS) elided per shell hard-truncation to 3 KPIs (`BuildCompactShell.tsx:48-54` spec); deferral documented verbatim in THESIS Q3 + Mockup-conformance section.  Identity primary uses `pair.country` ("US"/"UK"/"EUR/FR") rather than mockup's currency codes ("USD"/"GBP") — minor cosmetic carry-over from the Phase-1 pilot precedent (reviewer judged acceptable; not a finding).  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (Extended title row "USD 10Y SWAP-BE BASIS" + subtitle "ZCIS CPI-U • 10Y minus UST/TIPS 10Y breakeven" + three top-right cards Z-Score/Percentile/Liquidity-Premium-Proxy + Country-Pair dropdown + tenor/lookback/field controls + bps-scale KPI strip with ZCIS-leg + bond-BE decomposition + basis-history chart in BPS with ±2σ/±1.5σ z-bands + methodology card with index-family triple + lineage footer; Compact category+status pill + identity row "USD · CPI-U SWAP-BE BASIS" + three KPI tiles in mockup order + BPS sparkline + footer caveat + fresh-status badge + expand affordance), standalone bridge (methodology_exposure.md §5; `typedView: null`; per-tool detail endpoint at `/api/v1/rates/detail/swap-breakeven-basis`), FM1–FM12 (FM1 folder == MCP `tool.name`; FM3 tier set claims `generic_runnable` + `custom_build_surface` + `monitor_surface`; FM7 pure-value `module.ts` export — no side effects; FM8 all claimed tiers wired at canonical paths; FM10 THESIS Q1/Q2/Q3/Q5 substantive with FM IDs + standards cites in Q5; FM11 `__tests__/module.spec.ts` calls `assertStandardModuleInvariants` + dual-view + monitor + mockup-presence checks; FM12 loader entry at `src/modules/index.ts:71` (import) + `:144` (array) alphabetical between `calculate_rolling_regression_tool` and `calculate_swap_spread_tool` — pre-existing, no edit required), methodology honesty (PR10 / P5: `methodology_label` row sourced from `cm.methodology_label` at `swapBreakevenBasisShared.ts:576`, not hardcoded; `index_family_caveat` structured wire field is canonical source for per-pair caveat on all three surfaces), shared cross-surface helper (BuildExtended + BuildCompact + Monitor widget all import from `swapBreakevenBasisShared.ts`).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`calculate_swap_breakeven_basis_simple_tool: 8 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 25 done / 0 blocked / 3 remaining (next eligible: build_order 26 `inflation_indexed_bonds__cross_country_breakeven_spread`). |
| v32 | 2026-06-08 | **Frontend factory batch-3 tool 24 / 28 — `calculate_inflation_swap_forward_tool` dual-view shipped (APPROVED-AFTER-FIX).**  §4.4 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (InflationSwapForward widget)`.  Sixth tool of the batch-3 catalog extension (tools 19/20/21/22/23 shipped at v27/v28/v29/v30/v31); ZCIS forward rate spanning two pillars (e.g. US 5Y5Y ZCIS) — implied forward inflation-swap rate, the canonical long-run market read on forward inflation compensation, distinct from bond-implied forward breakeven.  Standalone typed-detail endpoint at `/api/v1/rates/detail/inflation-swap-forward` (`api/routes/rates/detail.py` with `response_model=InflationSwapForwardOutput`); `InflationSwapForwardOutput` (+ `InflationSwapForwardCurrentMetrics` + `InflationSwapForwardTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output incl. PERCENT/BPS-axis forward rate, percentile, z-score, 252d range, observations count, methodology_disclosure wire field).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row consumes `buildMethodologyRows` at `inflationSwapForwardShared.ts`; Monitor widget exposes via `title=` tooltip; no TSX literal of the disclosure prose).  Reference frontend module: `calculate_ois_forward_rate_tool` (same snapshot/forward shape; different curve family — OIS forward cousin).  Shared `inflationSwapForwardShared.ts` helper consolidates `FAMILY_REGISTRY` (CPI-U / HICPxT / RPI canonical desk casing), `ZCIS_FORWARD_PAIRS`, KPI builders, reference bands, methodology rows, caveat text, and the `useInflationSwapForward` hook across all three surfaces (BuildExtended + BuildCompact + InflationSwapForwardWidget).  Compact density: shell-standard (3 KPIs FORWARD ZCIS / 1D CHANGE / Z-SCORE 252D + sparkline with z-bands + caveat) per Option (c) precedent (catalog `design_guardrail` #3).  Reviewer (fresh-context Claude) emitted `CHANGES REQUIRED` with TWO concrete + actionable + bounded findings (both inside the module folder): (1) `surfaces/monitor/InflationSwapForwardWidget.tsx:103` — kicker template `${shortLabel.toUpperCase()}` destroyed canonical lowercase 'x' in `HICPxT` (HICP excluding Tobacco), rendering as `HICPXT`; OIS-sibling pattern copied was safe because OIS `indexShort` values (`USD SOFR` etc.) are already all-caps but inflation family short labels store their canonical desk casing (`CPI-U`, `HICPxT`, `RPI`) and uppercasing corrupts the financial-domain meaning (FM10 / methodology honesty / labelling fidelity violation); (2) `surfaces/BuildCompact.tsx:77-78` — compact identity rendered `primary="EUR ZCIS"`, `secondary="5Y5Y"`, dropping the inflation-index family short label; `mockups/Compact.png` clearly shows three-segment title `EUR ZCIS · 5Y5Y · HICPxT` (mockup-conformance / BUILD_GUIDE Stage 6 6A violation undocumented in THESIS).  Finding-hygiene check (per `SINGLE_REVIEW_ROUND_POLICY.md §3` + Case D step 1) all green (no STRUCTURAL: prefix, no vague language, two files all in module folder, mockup-aligned, real-file references); proceeded to Dispatch 3 (fix pass).  Fix-pass builder removed `.toUpperCase()` from the `shortLabel` token in the Monitor widget kicker (kept `forwardLabel.toUpperCase()` since pair labels like `5y5y` need uppercasing for desk readability) → kicker now renders `CPI-U · 5Y5Y 🇺🇸` / `HICPxT · 5Y5Y 🇪🇺` / `RPI · 5Y5Y 🇬🇧`; extended Compact identity wiring so secondary becomes `${forwardSeg} · ${meta.indexShort}` when family meta resolves (falling back to bare forward-pair label otherwise) → compact title now reads `EUR ZCIS / 5Y5Y · HICPxT` (three segments per mockup); also extended THESIS Mockup-conformance Compact bullet to document the three-segment identity (market+stem primary; forward-pair · index-short secondary).  No other surfaces / module.ts / types / services / detail.py / tests touched (three-file fix, well under 4-file blast-radius).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`calculate_inflation_swap_forward_tool: 11 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 24 done / 0 blocked / 4 remaining (next eligible: build_order 25 `inflation_swaps__swap_breakeven_basis`). |
| v31 | 2026-06-08 | **Frontend factory batch-3 tool 23 / 28 — `get_futures_price_level_tool` (bond_futures family) dual-view shipped (APPROVED).**  §4.6 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (BondFuturesPriceLevel widget)` (monitor_elig was already `required`).  Fifth tool of the batch-3 catalog extension (tools 19/20/21/22 shipped at v27/v28/v29/v30); bond futures front-contract price level (TY/RX/G/JB etc.) — the sovereign-rates counterpart to the Phase-1 STIR price-level tool.  Note: this folder is **`get_futures_price_level_tool`** (bond_futures sub-agent MCP server) — distinct from the policy_futures cousin shipped at v17 under folder `policy_futures_get_futures_price_level_tool` (KNOWN_TOOL_ALIASES bridge); the two share the bare MCP function name but live in different sub-agent MCP servers (per catalog `design_guardrails`).  Standalone typed-detail endpoint at `/api/v1/rates/detail/bond-futures-price` (`api/routes/rates/detail.py` with `response_model=BondFuturesPriceLevelOutput`); `BondFuturesPriceLevelOutput` (+ `BondFuturesPriceLevelCurrentMetrics` + `BondFuturesPriceLevelTimeSeriesRow`) mirror types in `types/rates.ts` — namespaced to avoid collision with the policy_futures cousin; snake_case preserved; exact field-by-field match to backend `FuturesPriceLevelOutput` Pydantic schema (PRICE-axis: `current_metrics.current_price` + `daily_change_price` + `current_z_score_252` + 252d range + `quote_units` + per-contract notation context).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row consumes `buildMethodologyRows` at `bondFuturesPriceShared.ts:543`; Monitor widget exposes a short curated `BOND_FUTURES_PRICE_COMPACT_CAVEAT` via `title=` tooltip on the price tile — sibling-tool pattern matches commits `fcc5381` / `be2595b` / `ad4a6fa` / `abe834b`; no TSX literal of the authoritative wire disclosure).  Reference frontend module: `policy_futures_get_futures_price_level_tool` (Phase-1 pilot Batch 1 `d8e8233` — same snapshot/forward dual-view shape; different rates family).  No KNOWN_TOOL_ALIASES change needed — `src/modules/index.ts` already imports + arrays `get_futures_price_level_tool` under the bare name at line 78,151; the loader entry now resolves to the new dual-view spec (no alias hazard versus the policy_futures cousin which keeps its own prefixed folder + alias).  Shared `bondFuturesPriceShared.ts` helper owns `CONTRACT_REGISTRY` (per-contract flag/tenor/quote_units/notation/decimals), enum options, `useBondFuturesPrice` data hook, `compactKPIs`, `extendedKPIs`, formatters (`format32nds`, `formatPrice`, `formatPriceChange`, `priceChangePercentSubtext`), `sanitisePriceSeries`, `buildReferenceBands`, `buildStretchContext`, `buildMethodologyRows`, `buildReferenceChips` across all three surfaces (BuildExtended + BuildCompact + BondFuturesPriceLevelWidget); 32nds notation isolated to UST family via the per-contract registry, decimal notation for the rest with per-contract `decimals`; `toneForChange(-delta)` sign-inversion (positive price = rally → mint; opposite of the cousin's implied-rate axis where positive = hike → coral) documented inline.  `as_of_date` deliberately NOT exposed as a control — bond_futures Pydantic Input does not accept it, surfacing would silently no-op (documented in THESIS).  Compact density: shell-standard (3 KPIs PRICE + 1D CHANGE + Z-SCORE 252D + sparkline + caveat) per Option (c) precedent (catalog `design_guardrail` #4) — the mockup's secondary 6-cell KPI row (5D / 1M / pctl / high / low / N) would require extending `BuildCompactShell` with a `secondaryKpis` slot (cross-tool refactor); deferred and documented verbatim in THESIS `## Mockup conformance`.  32nds notation IS rendered in the primary cell per the mockup (e.g. `129'08` + `129 and 8/32nds`), including for price change (e.g. `+0'06`).  Monitor widget id `bond_futures_price_level` is globally unique with a positive spec assertion that it doesn't collide with the cousin `policy_futures_price_level`.  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (Extended identity row "TY1 · CME 10-Year U.S. Treasury Note Futures 🇺🇸" + 9-cell PRICE KPI strip + price-history chart with ±2σ z-bands + methodology card + lineage footer all matched; Compact shell-standard density with documented Option-(c) deferral), standalone bridge (methodology_exposure.md §5; `typedView: null`; per-tool detail endpoint at `/api/v1/rates/detail/bond-futures-price`), FM1–FM12 (folder == MCP `tool.name` FM1; tier set valid FM3; `module.ts` pure value export FM7; all claimed tiers wired FM8; THESIS five-question with per-surface specificity in Q2, curated-headline rationale in Q3, ID citations in Q5 — FM10; spec tests assert standard invariants + dual-view + monitor-widget-id collision watch — FM11; loader entry pre-existing at `src/modules/index.ts:78` alphabetical — FM12), methodology honesty (wire-sourced; no TSX literal of the disclosure prose), shared cross-surface helper (all three surfaces import from `bondFuturesPriceShared.ts`).  Pre-commit gate green: `npm run test:modules` ✓ (`get_futures_price_level_tool: 8 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 23 done / 0 blocked / 5 remaining (next eligible: build_order 24 `inflation_swaps__forward`). |
| v30 | 2026-06-08 | **Frontend factory batch-3 tool 22 / 27 — `policy_futures_get_futures_pack_average_simple_tool` dual-view shipped (APPROVED).**  §4.5 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (PolicyFuturesPackAverage widget)`.  Fourth tool of the batch-3 catalog extension (tools 19/20/21 shipped at v27/v28/v29); STIR pack average across four consecutive quarterly policy-futures contracts (Whites=Y1, Reds=Y2, Greens=Y3, Blues=Y4) for a single market (e.g. SFR Whites Pack Average), smoothing quarter-by-quarter noise into a year-anchored read of the implied policy path.  Standalone typed-detail endpoint at `/api/v1/rates/detail/policy-futures-pack-average` (`api/routes/rates/detail.py` with `response_model=FuturesPackAverageSimpleOutput`); `FuturesPackAverageSimpleOutput` (+ `FuturesPackAverageSimpleCurrentMetrics` + `FuturesPackAverageSimpleTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output incl. PERCENT-scale pack implied rate, per-leg implied rates for SFR1..SFR4, central-bank-context regime label, and the `methodology_disclosure` wire field).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row consumes `buildMethodologyRows` at `futuresPackAverageSimpleShared.ts`; Monitor widget exposes via `title=` tooltip on `PolicyFuturesPackAverageWidget.tsx`; no TSX literal of the disclosure prose).  Reference frontend module: `calculate_ois_forward_rate_tool` (snapshot/forward shape; different curve family — OIS forward cousin).  Folder-vs-MCP alias bridged via `KNOWN_TOOL_ALIASES` in `src/lib/toolNames.ts` (`get_futures_pack_average_simple_tool → policy_futures_get_futures_pack_average_simple_tool`), mirroring tools 19/20/21 precedent.  Shared `futuresPackAverageSimpleShared.ts` helper consolidates CURVE_REGISTRY / POLICY_FUTURES_CURVE_OPTIONS / PACK_OPTIONS (Whites/Reds/Greens/Blues) / `pctToBps` / `usePolicyFuturesPackAverage` data hook / `compactKPIs` / `extendedKPIs` / `decompositionKPIs` / `sanitisePackSeries` / `buildReferenceBands` / `buildStretchContext` / `buildMethodologyRows` / `buildReferenceChips` / `compactCaveatFor` across all three surfaces (BuildExtended + BuildCompact + PolicyFuturesPackAverage widget); single PERCENT-scale source-of-truth with shared `pctToBps` conversion.  Compact density: shell-standard (3 KPIs PACK IMPLIED RATE % / 1D CHANGE bps / Z-SCORE 252D + sparkline with ±2σ z-bands + caveat) per Option (c) precedent (catalog `design_guardrail` #5).  Documented mockup deviations (judged honest by reviewer): Compact secondary KPI row (5D / 252D PCTL / 252D HIGH / 252D LOW / OBSERVATIONS) elided in favour of shell-standard 3-KPI density per design_guardrail #5 — the secondary-row data remains reachable via the expand affordance and the Monitor tile's L/H range strip.  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (Compact identity + caveat + sparkline + freshness pill + expand affordance + tone cues all matched), standalone bridge (methodology_exposure.md §5; `typedView=null` at `module.ts:67`; per-tool detail endpoint at `api/routes/rates/detail.py:3420`), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface specific with FM IDs + P5 + ADR 0013 cites; FM11 test calls `assertStandardModuleInvariants` + 10 dual-view + standalone-bridge specific checks; FM12 alphabetical loader entry at `src/modules/index.ts:88`), methodology honesty (`data.methodology_disclosure` wire-sourced; no TSX literal of the disclosure prose), shared cross-surface helper (BuildExtended + BuildCompact + Monitor widget all import from `futuresPackAverageSimpleShared.ts`), naming-divergence alias.  Pre-commit gate green: `npm run test:modules` ✓ (`policy_futures_get_futures_pack_average_simple_tool: 11 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 22 done / 0 blocked / 6 remaining (next eligible: build_order 23 `bond_futures__price_level`). |
| v29 | 2026-06-08 | **Frontend factory batch-3 tool 21 / 27 — `policy_futures_get_futures_cross_market_spread_tool` dual-view shipped (APPROVED-AFTER-FIX).**  §4.5 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (PolicyFuturesCrossMarketSpread widget)`.  Third tool of the batch-3 catalog extension (tools 19/20 shipped at v27/v28); cross-market STIR spread between two policy-futures markets at common matched-strip expiry (e.g. SFR1−ER1 Fed-vs-ECB), surfacing front-end cross-currency policy-path divergence.  Standalone typed-detail endpoint at `/api/v1/rates/detail/policy-futures-cross-market` (`api/routes/rates/detail.py` with `response_model=FuturesCrossMarketSpreadOutput`); `FuturesCrossMarketSpreadOutput` (+ `FuturesCrossMarketSpreadCurrentMetrics` + `FuturesCrossMarketSpreadTimeSeriesRow`) mirror types in `types/rates.ts` (snake_case preserved; exact field-by-field match to backend Pydantic Output incl. per-leg implied rates, per-leg short-rate regime labels, and the mixed-regime call-out).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row consumes `buildMethodologyRows` at `futuresCrossMarketSpreadShared.ts`; Monitor widget exposes via `title=` tooltip; no TSX literal of the disclosure prose).  Reference frontend module: `calculate_cross_market_inflation_swap_spread_tool` (same cross-market 2-leg differential shape; different curve family — inflation swaps vs STIR).  Folder-vs-MCP alias bridged via `KNOWN_TOOL_ALIASES` in `src/lib/toolNames.ts` (`get_futures_cross_market_spread_tool → policy_futures_get_futures_cross_market_spread_tool`), mirroring tools 19/20 precedent.  Shared `futuresCrossMarketSpreadShared.ts` helper consolidates CURVE_REGISTRY / cross-CB pair registry / `crossCBLabel` / `compactCaveatFor` (CB-pair-templated wire-honest caveat) / data hook / KPI builders / band builder / methodology rows / sign-convention coding across all three surfaces (BuildExtended + BuildCompact + PolicyFuturesCrossMarketSpread widget); single PERCENT-POINTS → bps conversion point.  Compact density: shell-standard (3 KPIs SPREAD bps / 1D CHANGE bps / Z-SCORE 252D + sparkline with z-bands + caveat) per Option (c) precedent (catalog `design_guardrail` #4).  Documented mockup deviations (judged honest by reviewer): Compact tertiary 1D-change sub-row inside the SPREAD cell promoted to the primary 1D-CHANGE KPI rather than a sub-row — promoting it would require extending `BuildCompactShell` with a `secondaryKpis` slot touching 10+ shipped compact views.  Reviewer (fresh-context Claude) emitted `CHANGES REQUIRED` with FOUR concrete + actionable + bounded findings (all inside the three module folder files): (a) Compact identity primary used `aMeta.shortLabel`/`bMeta.shortLabel` ("SOFR-Euribor") instead of `aMeta.stripStemPrefix`/`bMeta.stripStemPrefix` ("SFR-ER") — mockup + THESIS commitment violation; (b) Compact secondary line built `${aMeta.cbShort}-${bMeta.cbShort} Cross-CB Spread (Implied Rate)` instead of the mockup-verbatim literal `'Front-Quarter Cross-CB Spread (Implied Rate)'`; (c) Extended `identityPrimary` was `${pairLabel} Cross-CB Spread` ("SFR1-ER1 Cross-CB Spread") instead of mockup-faithful `${stripStemPrefix}-${stripStemPrefix} Pos${n} Cross-CB Spread` ("SFR-ER Pos1 Cross-CB Spread"); (d) compact caveat constant `POLICY_FUTURES_CROSS_MARKET_COMPACT_CAVEAT` hardcoded "Fed vs ECB" — wire-dishonest when selecting Fed-vs-BOE / ECB-vs-BOE pairs (P5 + STANDARD_MODULE_RULES.md no-hardcoded-CB-labels violation).  Finding-hygiene check (per `SINGLE_REVIEW_ROUND_POLICY.md §3` + Case D step 1) all green (no STRUCTURAL: prefix, no vague language, three files all in module folder, mockup-aligned, real-file references); proceeded to Dispatch 3 (fix pass).  Fix-pass builder swapped `stripStemPrefix` into Compact primary, replaced Compact secondary with the mockup literal, rebuilt Extended `identityPrimary` with `Pos${n}` segment, and converted the caveat constant to `compactCaveatFor(curveFamilyA, curveFamilyB)` templating the CB pair via the existing `crossCBLabel` helper; also updated the Extended lineage row's lone other consumer of the deleted constant (within the same three files; same wire-honesty fix).  No other surfaces / module.ts / types / services / detail.py / THESIS / tests touched.  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`policy_futures_get_futures_cross_market_spread_tool: 6 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 21 done / 0 blocked / 6 remaining (next eligible: build_order 22 `policy_futures__pack_average`). |
| v28 | 2026-06-07 | **Frontend factory batch-3 tool 20 / 27 — `policy_futures_get_futures_calendar_spread_tool` dual-view shipped (APPROVED).**  §4.5 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (PolicyFuturesCalendarSpread widget)`.  Second tool of the batch-3 catalog extension (tool 19 `futures_butterfly` shipped at v27 / `fcc5381`); STIR calendar spread between two quarterly policy-futures contracts (e.g. SFR1-SFR2 OIS Calendar Spread), surfacing front-end policy-path slope.  Standalone typed-detail endpoint at `/api/v1/rates/detail/policy-futures-calendar` (`api/routes/rates/detail.py:3132` with `response_model=FuturesCalendarSpreadOutput`); `FuturesCalendarSpreadOutput` (+ `FuturesCalendarSpreadCurrentMetrics` + `FuturesCalendarSpreadTimeSeriesRow`) mirror types in `types/rates.ts:1959` (snake_case preserved; exact field-by-field match to backend `FuturesCalendarSpreadOutput` Pydantic schema).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row consumes `buildMethodologyRows` → `data.methodology_disclosure` at `futuresCalendarSpreadShared.ts:783`; Monitor widget exposes via `title=` tooltip at `PolicyFuturesCalendarSpreadWidget.tsx:192`; no TSX literal of the disclosure prose).  Reference frontend module: `calculate_ois_curve_spread_tool` (same 2-leg spread KPI shape; different curve family — OIS calendar cousin).  Folder-vs-MCP alias bridged via `KNOWN_TOOL_ALIASES` in `src/lib/toolNames.ts` (`get_futures_calendar_spread_tool → policy_futures_get_futures_calendar_spread_tool`), mirroring tool 19's and `policy_futures_get_futures_price_level_tool`'s precedent.  Shared `futuresCalendarSpreadShared.ts` helper consolidates CURVE_REGISTRY / calendar-pair registry / sign-flip `wirePctToDisplayBps` (single PERCENT-POINTS → bps conversion point — no drift risk) / `usePolicyFuturesCalendar` data hook / `compactKPIs` / `extendedKPIs` / decomposition row / band builder / stretch builder / `buildMethodologyRows` across all three surfaces (BuildExtended + BuildCompact + PolicyFuturesCalendarSpread widget).  Compact density: shell-standard (3 KPIs SPREAD / 1D CHANGE / Z-SCORE + sparkline with z-bands + caveat) per Option (c) precedent (catalog `design_guardrail` #5); mockup z-cell phrasing "Bull Flattening" generalised to desk-vocabulary via shared `zScoreCaptionForSpread` helper (`Elevated Steeper` / `Elevated Flatter` / `Extreme …`) — documented in THESIS `## Mockup conformance` per design_guardrail #5.  Documented mockup deviations (judged honest by reviewer): third top-right Extended card is `STRIP CONTEXT` (curve identity + regime caveat) rather than the mockup's `Federal Reserve (FOMC)` meeting-metadata card — backend has no FOMC meeting metadata; documented in THESIS §1 + §3.  Reviewer (fresh-context Claude) emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (Compact identity "SFR · 1-3 · WHITES" + caveat verbatim "Calendar spread on implied rates; underlying contracts quote inverse."), standalone bridge (methodology_exposure.md §5; `typedView=null` at `module.ts:90`; per-tool detail endpoint), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface specific with FM ID cites; FM11 test calls `assertStandardModuleInvariants` + dual-view + mockups + monitor-widget-id; FM12 alphabetical loader entry at `src/modules/index.ts:86` between `butterfly_simple` and `cross_market_spread`), methodology honesty, shared cross-surface helper, naming-divergence alias.  Pre-commit gate green: `npm run test:modules` ✓ (`policy_futures_get_futures_calendar_spread_tool: 8 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 20 done / 0 blocked / 7 remaining (next eligible: build_order 21 `policy_futures__cross_market_spread`). |
| v27 | 2026-06-07 | **Frontend factory batch-3 tool 19 / 27 — `policy_futures_get_futures_butterfly_simple_tool` dual-view shipped (APPROVED).**  §4.5 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (PolicyFuturesButterfly widget)`.  First tool of the batch-3 catalog extension (build_order 19-27 appended after the v26 batch-2 catalog-exhaustion); STIR 3-point butterfly between three quarterly policy-futures contracts (e.g. SFR 1-2-3 OIS Butterfly), surfacing curvature of the policy-futures strip independent of overall level.  Standalone typed-detail endpoint at `/api/v1/rates/detail/policy-futures-butterfly`; `FuturesButterflySimpleOutput` (+ `FuturesButterflySimpleCurrentMetrics` + `FuturesButterflySimpleTimeSeriesRow`) mirror types (snake_case preserved; exact field-by-field match to backend `FuturesButterflySimpleOutput` Pydantic schema — `current_metrics.fly_bps` / `daily_change_bps` / `current_z_score`, plus `time_series_fly` + `time_series_zscore` + per-leg context in `triplet_positions`).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row consumes `buildMethodologyRows` → `data.methodology_disclosure || '—'`; no TSX literal replaces the wire string per PR10/P5).  Reference frontend module: `calculate_ois_butterfly_tool` (OIS butterfly cousin — same 3-point fly KPI shape, different curve family).  Folder-vs-MCP alias bridged via `KNOWN_TOOL_ALIASES` in `src/lib/toolNames.ts` (`get_futures_butterfly_simple_tool → policy_futures_get_futures_butterfly_simple_tool`), mirroring the `policy_futures_get_futures_price_level_tool` precedent.  Shared `futuresButterflySimpleShared.ts` helper consolidates `CURVE_REGISTRY` / `BUTTERFLY_TRIPLETS_BY_CURVE` / `usePolicyFuturesButterfly` / `compactKPIs` / `extendedKPIs` / `decompositionKPIs` / `buildMethodologyRows` / `sanitiseButterflySeries` / `buildReferenceBands` / `pctToBps` across all three surfaces (BuildExtended + BuildCompact + PolicyFuturesButterflyWidget) — single PERCENT-POINTS → bps conversion point; no drift risk.  Documented mockup deviations (judged honest by reviewer): (a) third top-right Extended card is `STRIP CONTEXT` (curve identity + regime caveat) rather than the mockup's `Federal Reserve (FOMC)` meeting-metadata card — backend has no FOMC meeting metadata; (b) mockup `5D` / `1M` change cells absent — documented in THESIS §4 as missing wire fields.  `useEffect` hook in `usePolicyFuturesButterfly` defensively short-circuits + clears state when the triplet ordering is invalid (front-end guard so backend's `_strip_positions_must_be_ordered` validator never sees a bad request).  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance, standalone bridge (methodology_exposure.md §5; `typedView=null`; per-tool detail endpoint), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface specific with FM ID cites; FM12 alphabetical loader entry at `src/modules/index.ts:85,158`), methodology honesty, shared cross-surface helper, naming-divergence alias.  Pre-commit gate green: `npm run test:modules` ✓ (`policy_futures_get_futures_butterfly_simple_tool: 8 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  Catalog state: 19 done / 0 blocked / 8 remaining (next eligible: build_order 20 `policy_futures__calendar_spread`). |
| v26 | 2026-06-07 | **Frontend factory batch-2 tool 18 / 18 — `get_scan_policy_futures_extremes_tool` dual-view shipped (APPROVED-AFTER-FIX). Catalog exhausted (18/18 done, 0 blocked).**  §4.5 row updated: `output_kind: scanner-result → scanner-rankedlist`, `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (PolicyFuturesScanner widget)` (monitor_elig was already `required`).  Fourth scanner-shape tool of the batch (after `scan_inflation_swaps_extremes_tool` v13 + `scan_inflation_linkers_extremes_tool` v24 + `scan_bond_futures_extremes_tool` v25) and the final tool of the 18-tool initial batch.  Compact view is a top-N ranked table with a 7-column grid (`# | CONTRACT | PACK | SCOPE | VALUE | 1D Δ | Z`) per `mockups/Compact.png`.  Standalone typed-detail endpoint at `/api/v1/rates/detail/policy-futures-scanner`; `ScanPolicyFuturesExtremesOutput` mirror types (snake_case preserved, exact field-by-field match to backend Pydantic schemas).  Methodology honesty wire-sourced via `data.methodology_disclosure` (Extended `MethodologyCard` "Disclosure" row + Compact footer + Monitor widget `title=` tooltip).  Dispatch 1 (builder) emitted a full diff (~13m, gates green in-sandbox); Dispatch 2 (reviewer) emitted `CHANGES REQUIRED` with one concrete + actionable + bounded finding on `surfaces/BuildCompact.tsx` (Compact grid was collapsed to 6 columns; `1D Δ` was being concatenated into the Value sub-line, breaking mockup conformance + `rendering_density.md §2.2`).  Finding-hygiene check (per `SINGLE_REVIEW_ROUND_POLICY.md §3` + Case D step 1) all green (no STRUCTURAL: prefix, no vague language, single file in module folder, mockup-aligned, real-file references); proceeded to Dispatch 3 (fix pass).  Fix-pass builder widened header grid + data-row grid + `CompactSkeleton` from 6 → 7 columns (`grid-cols-[20px_1fr_auto_auto_auto_auto] → grid-cols-[20px_1fr_auto_auto_auto_auto_auto]`), inserted the dedicated `1D Δ` cell between Value and Z (with the OI / VOL / IR per-metric narrative rendering rules called out by the reviewer), and stripped the inline concat from the Value sub-line.  Reviewer's two observation-only notes (`IR+Δ+V+O` literal kicker; Monitor widget data-fetch boilerplate) left intact per explicit "do NOT dispatch a fix" instruction.  No other surfaces / module.ts / types / services / detail.py / THESIS / tests touched (single-file fix, well under 4-file blast-radius).  Pre-commit gate re-run in orchestrator session: `npm run test:modules` ✓ (`get_scan_policy_futures_extremes_tool: 7 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓ (`tsc -p tsconfig.app.json --noEmit` clean).  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback).  **Catalog exhausted**: 18 / 18 tools shipped (0 blocked); orchestrator transitions to `catalog_exhausted` per `ORCHESTRATOR_PROMPT.md` Catalog-exhaustion rule. |
| v25 | 2026-06-07 | **Frontend factory batch-2 tool 17 / 18 — `scan_bond_futures_extremes_tool` dual-view shipped (APPROVED).**  §4.6 row updated: `output_kind: scanner-result → scanner-rankedlist`, `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (BondFuturesScanner widget)` (monitor_elig was already `required`).  Third scanner-shape tool in the batch (after `scan_inflation_swaps_extremes_tool` v13 + `scan_inflation_linkers_extremes_tool` v24); compact view is a top-N ranked table with SCOPE chip per row + value-with-narrative dual-line cells, NOT a sparkline.  Standalone typed-detail endpoint at `/api/v1/rates/detail/bond-futures-scanner`; `ScanBondFuturesExtremesOutput` + `ScanBondFuturesExtremesResultRow` + `ScanBondFuturesMetric` mirror types (snake_case preserved, exact field-by-field match to backend Pydantic schemas). Methodology honesty wire-sourced via `data.methodology_disclosure` — Extended `MethodologyCard` "Disclosure" row consumes the wire field verbatim via `buildMethodologyRows`; Compact footer + Monitor widget surface the same wire field via the `title=` tooltip; short-form `BOND_FUTURES_SCANNER_COMPACT_CAVEAT` used only as the visible footer label (P5 / PR10 satisfied — no TSX literal replaces the wire string). Per-row drill-down from the Extended ranked table to `futures_price_level_tool` matches THESIS Q2's deep-link claim. Reviewer (fresh-context Claude) emitted bare `APPROVED` with one non-blocking observation (`instrumentCountry` import unused in `BuildExtended.tsx` — kept exported for parity with the shared identity helper triple); verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (Compact + Extended both match `Compact.png` / `Extended.png` — header / kicker / SCAN SUMMARY + DISTRIBUTION + LIMITATIONS cards / histogram / ranked detail table / methodology card / lineage footer), standalone bridge (methodology_exposure.md §5; `typedView=null`), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface specific with FM ID cites; FM12 alphabetical loader at `src/modules/index.ts:92`), methodology honesty, shared `bondFuturesScannerShared.ts` helper (`useBondFuturesScanner` consolidated across all three surfaces).  Pre-commit gate green: `npm run test:modules` ✓ (`scan_bond_futures_extremes_tool: 7 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v24 | 2026-06-07 | **Frontend factory batch-2 tool 16 / 18 — `scan_inflation_linkers_extremes_tool` dual-view shipped (APPROVED).**  §4.7 row updated: `output_kind: scanner-result → scanner-rankedlist`, `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (LinkerScanner widget)` (monitor_elig was already `required`).  Second scanner-shape tool (after `scan_inflation_swaps_extremes_tool` v13); compact view is a top-N ranked table per the scanner pattern (NOT a sparkline; distinct from the snapshot / spread / forward shapes).  Standalone typed-detail endpoint at `/api/v1/rates/detail/linkers-scanner`; `ScanInflationLinkersExtremesOutput` + `ScanInflationLinkersExtremesResultRow` mirror types (snake_case preserved, exact field-by-field match to backend `ScanInflationLinkersExtremesOutput` Pydantic schema — `signal` Literal enum closed identically, `methodology_disclosure` required at both row and response level, nullable `maturity_date` / `country` / `vendor_ticker`).  Methodology honesty wire-sourced via `data.methodology_disclosure` — Extended methodology card's "Disclosure" row consumes the wire field verbatim via `buildMethodologyRows`; Compact footer + Monitor widget surface the same wire field as the `title=` tooltip; the short `LINKER_SCANNER_COMPACT_CAVEAT` is desk-canonical visible text (PR10/P5 satisfied — no TSX literal replaces the wire string).  Per-row drill-down from the Extended ranked table to `get_real_yield_level_tool` matches THESIS Q2's deep-link claim.  Documented mockup deviation (honest): committed mockups envision a composite **RY + BE (real-yield + breakeven) scanner** with a per-row `SCOPE` column tagging each row; the current backend (`schemas.py:ScanInflationLinkersExtremesOutput`) is **single-metric** — ranks only on `z_score_real_yield` per the catalog's literal wording ("ranked by absolute 252-day z-score of real yields"). The backend's `planned_extensions` block in `config.yaml` notes a multi-metric (RY + BE) ranking is contingent on a catalog amendment.  Builder built faithful to the backend wire (single-metric RY) rather than invent breakeven rankings the wire doesn't produce; THESIS §4 documents this as future-state.  Reviewer (fresh-context Claude) judged the deviation honest per the reviewer policy ("the FM contract / dual-view / bridge contracts are satisfied for the as-built single-metric scanner"); emitted bare `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (judged against backend-wire shape, not aspirational scope), standalone bridge (methodology_exposure.md §5; endpoint at `/api/v1/rates/detail/linkers-scanner`; `typedView=null`), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface specific with FM ID cites; FM12 alphabetical loader at index.ts:94 between `calculate_ois_cross_market_spread_tool` and `scan_inflation_swaps_extremes_tool`), methodology honesty, shared `scanInflationLinkersExtremesShared.ts` helper (`useLinkersScanner` consolidated across all three surfaces).  Pre-commit gate green: `npm run test:modules` ✓ (`scan_inflation_linkers_extremes_tool: 7 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v23 | 2026-06-07 | **Frontend factory batch-2 tool 15 / 18 — `calculate_forward_breakeven_simple_tool` dual-view shipped (APPROVED).**  §4.3 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (ForwardBreakeven widget)` (monitor_elig was already `required`).  Forward-shape breakeven implied between two pillars (e.g. US 5Y5Y, EUR 5Y5Y, GBP 5Y5Y) — bond-implied forward inflation compensation; the linker analog of the just-shipped `calculate_ois_forward_rate_tool` forward shape.  Standalone typed-detail endpoint at `/api/v1/rates/detail/forward-breakeven`; `ForwardBreakevenSimpleOutput` mirror type (snake_case preserved; exact field-by-field match to backend `ForwardBreakevenSimpleCurrentMetrics` + `ForwardBreakevenSimpleOutput` Pydantic schemas — `current_forward_bps`, `daily_change_bps`, `current_z_score`, `start_breakeven_bps`/`end_breakeven_bps`, `start_years`/`end_years`, `methodology_label`, `time_series_forward` + `time_series_zscore`).  Methodology honesty wire-sourced via `current_metrics.methodology_label` (no TSX literal); fallback string only fires when the wire field is empty (P5 honest disclosure preserved).  Pair/forward-pair model uses single Country-Pair linker-keyed dropdown with same-country invariant + per-pair forward-pair filtering via `forwardPairOptionsForLinker` against `BREAKEVEN_TENORS_BY_PAIR` (prevents 422 round-trips); defaultParams `forward_pair: '5Y5Y'` / `start_tenor: '5Y'` / `end_tenor: '10Y'` satisfies the backend's `_start_must_precede_end` validator on first render.  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings; verification scorecard green across dual-view contract (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (Compact + Extended both match `Compact.png` / `Extended.png`), standalone bridge (methodology_exposure.md §5; endpoint at `/api/v1/rates/detail/forward-breakeven`; `typedView=null`), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface specific; FM12 alphabetical loader entry between `calculate_curve_spread_tool` and `calculate_half_life_tool`), methodology honesty, shared `forwardBreakevenShared.ts` helper, and `useForwardBreakeven` consolidation across Build views (widget uses local fetcher per the reference `BreakevenInflationWidget` paramsKey de-dup pattern).  Pre-commit gate green: `npm run test:modules` ✓ (`calculate_forward_breakeven_simple_tool: 10 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v22 | 2026-06-07 | **Frontend factory batch-2 tool 14 / 18 — `calculate_ois_cross_market_spread_tool` dual-view shipped (APPROVED).**  §4.2 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (CrossMarketOis widget)` (monitor_elig was already `required`).  Cross-currency policy-path divergence (SOFR-ESTR / SOFR-SONIA / ESTR-SONIA / etc.) at a common tenor — sibling to the just-shipped `calculate_cross_market_inflation_swap_spread_tool` cross-market shape (same compact + extended layout; different curve family).  Standalone typed-detail endpoint at `/api/v1/rates/detail/ois-cross-market-spread`; `OisCrossMarketSpreadOutput` mirror type (snake_case preserved, exact field-by-field match to backend `OISCrossMarketSpread*` Pydantic schemas — `current_metrics` with `spread_bps` / `daily_change_bps` / `current_z_score`, per-leg `short_rate_pct` / `long_rate_pct`, `time_series` + `time_series_zscore`).  Methodology disclosure sourced from `OIS_CROSS_MARKET_COMPACT_CAVEAT` in `crossMarketOisShared.ts` with explicit `TODO(PR10)` marker — `OISCrossMarketSpreadCurrentMetrics` does NOT carry `methodology_label`, mirroring the OIS sub-domain siblings (rate_level / curve_spread / butterfly / forward_rate); single-line swap when the backend ships the field.  Cross-curve invariant guarded by `curveFamily1 === curveFamily2` snap-back logic in `BuildExtended.tsx` + the shared hook.  Per-currency overnight-index identity (SOFR / ESTR / SONIA / TONA / AONIA / CORRA) cited at the methodology card + Policy-Regime top-right card.  No tool-name alias bridge needed — frontend folder name matches the MCP function + workflow registry name (`calculate_ois_cross_market_spread_tool`).  Reviewer (fresh-context Claude) emitted `APPROVED` with no findings; verification scorecard green across dual-view contract, mockup conformance (Compact + Extended), standalone bridge, FM1–FM12, methodology honesty (acknowledged-gap), shared helper, and cross-curve invariant.  Pre-commit gate green: `npm run test:modules` ✓ (`calculate_ois_cross_market_spread_tool: 6 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v21 | 2026-06-07 | **Frontend factory batch-2 tool 13 / 18 — `calculate_inflation_swap_curve_spread_tool` dual-view shipped (APPROVED).**  §4.4 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (InflationSwapCurveSpread widget)` (monitor_elig was already `required`).  Same-country ZCIS tenor spread (e.g. EUR 2s10s ZCIS, US 2s10s ZCIS); mirrors the just-shipped `calculate_breakeven_curve_spread_tool` shape-twin (curve-spread of ZCIS rather than bond-implied breakeven).  Standalone typed-detail endpoint at `/api/v1/rates/detail/inflation-swap-curve-spread`; `InflationSwapCurveSpreadOutput` mirror type (snake_case preserved, exact field-by-field match to backend `InflationSwapCurveSpreadCurrentMetrics` + `InflationSwapCurveSpreadOutput` Pydantic schemas — `current_spread_bps`, `short_swap_rate_pct`/`long_swap_rate_pct`, `short_tenor`/`long_tenor`, `methodology_label`, `time_series` + `time_series_zscore`).  Methodology honesty wire-sourced via `current_metrics.methodology_label` (no TSX literal); compact-card OTC-ZCIS caveat is a curated short-form footer (same pattern as the breakeven-curve-spread reference's `BREAKEVEN_CURVE_SPREAD_COMPACT_CAVEAT`).  Per-currency reference-index disclosure (HICPxT EUR / CPI-U NSA USD / RPI GBP) surfaced via the methodology card's reference-index/lag/interpolation triple.  No tool-name alias bridge needed — frontend folder name matches the MCP function + workflow registry name (`calculate_inflation_swap_curve_spread_tool`).  Reviewer (fresh-context Claude) emitted `# APPROVED` with "No findings"; the orchestrator's parser (which expects bare `APPROVED` on a line by itself) returned `heading: null` and the verdict was confirmed by orchestrator-side visual inspection of the reviewer log — same Case-C treatment.  Pre-commit gate green: `npm run test:modules` ✓ (`calculate_inflation_swap_curve_spread_tool: 6 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v20 | 2026-06-07 | **Frontend factory batch-2 tool 12 / 18 — `calculate_ois_forward_rate_tool` dual-view shipped (APPROVED).**  §4.2 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (OisForwardRate widget)` (monitor_elig was already `required`).  Forward-shape OIS implied forward overnight rate spanning two pillars (e.g. SOFR 1Y1Y / 2Y1Y / 5Y5Y) — risk-neutral implied policy-path nowcast.  Standalone typed-detail endpoint at `/api/v1/rates/detail/ois-forward-rate`; `OisForwardRateOutput` mirror type (snake_case preserved; mirrors backend `OISForwardRateCurrentMetrics` field-for-field including `forward_rate_pct`, `daily_change_bps`, `current_z_score`, `start_spot_rate_pct`, `end_spot_rate_pct`, plus `time_series` / `time_series_forward` / `time_series_zscore`).  Methodology label sourced via per-family `FAMILY_REGISTRY` in `oisForwardRateShared.ts` with explicit `TODO(PR10)` marker — same OIS sub-domain pattern as siblings (`OISForwardRateCurrentMetrics` does NOT carry `methodology_label`, mirroring OIS rate_level / curve_spread / butterfly).  Documented mockup deviation: Extended 9-cell KPI strip's "5D CHANGE" and "1M CHANGE" cells substituted with START SPOT / END SPOT because backend lacks `weekly_change_bps` / `monthly_change_bps` (config.yaml `methodology.planned_extensions` — deferred to follow-up PR so the wire shape stays stable in this migration).  Deviation documented in THESIS §"Known mockup/backend deviation", `oisForwardRateShared.ts:359-371`, and `BuildExtended.tsx:24-28`.  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings.  Pre-commit gate green: `npm run test:modules` ✓ (`calculate_ois_forward_rate_tool: 10 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v19 | 2026-06-06 | **Frontend factory batch-2 tool 11 / 18 — `calculate_inflation_swap_rate_level_tool` dual-view shipped (APPROVED).**  §4.4 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (InflationSwapRateLevel widget)` (monitor_elig was already `required`).  Snapshot-shape ZCIS rate level (level + 1D change + 252d range/percentile + rolling z-score); mirrors the just-shipped `get_ois_rate_level_tool` snapshot pattern and the Phase-1 `get_real_yield_level_tool` pilot.  Standalone typed-detail endpoint at `/api/v1/rates/detail/inflation-swap-rate-level`; `InflationSwapRateLevelOutput` + `InflationSwapRateLevelMetrics` mirror types (snake_case preserved, exact field-by-field match to backend Pydantic schemas).  Methodology label sourced from the wire (`current_metrics.methodology_label`, threaded from backend `config.yaml:methodology.what_it_does`) — NOT a TSX literal.  Per-currency reference-index disclosure (HICPxT EUR / CPI-U NSA USD / RPI GBP / CPI Canada) cited per the catalog guardrail.  No tool-name alias bridge needed — frontend folder name matches the MCP function + workflow registry name (`calculate_inflation_swap_rate_level_tool`).  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings.  Pre-commit gate green: `npm run test:modules` ✓ (all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v18 | 2026-06-06 | **Frontend factory batch-2 tool 10 / 18 — `get_ois_rate_level_tool` dual-view shipped (APPROVED).**  §4.2 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (OisRateLevel widget)` (monitor_elig was already `required`).  First snapshot-shape tool of the batch-2 catalog; mirrors `get_real_yield_level_tool`'s Phase-1 snapshot pilot (level + change + z-score + range strip + sparkline) for the OIS curve family.  Standalone typed-detail endpoint at `/api/v1/rates/detail/ois-rate-level`; `OisRateLevelOutput` mirror type (snake_case preserved, `current_rate_pct` field-by-field with backend `OISRateLevelOutput` Pydantic schema).  Folder-vs-MCP alias bridged via `KNOWN_TOOL_ALIASES` in `src/lib/toolNames.ts` (already present pre-build per the v18 alias note); frontend folder is `get_ois_rate_level_tool`, MCP function + workflow registry use `calculate_ois_rate_level_tool`.  Per-family caveat registry (USD_SOFR_OIS / EUR_ESTR_OIS / GBP_SONIA_OIS / JPY_OIS / AUD_OIS / CAD_OIS) hand-rolled in `oisRateLevelShared.ts` (sibling OIS sub-domain pattern: backend `OISRateLevelMetrics` does NOT carry `methodology_label`, mirroring the OIS curve_spread / butterfly siblings).  THESIS Q4 documents the one-line swap to wire-sourced once OIS PR10 ships.  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings.  Pre-commit gate green: `npm run test:modules` ✓ (`get_ois_rate_level_tool: 9 passed, 0 failed`; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v17 | 2026-05-30 | **Frontend factory tool 9 / 9 — `policy_futures_get_futures_price_level_tool` dual-view shipped (APPROVED).**  §4.5 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (PolicyFuturesPriceLevel widget)` (monitor_elig was already `required`).  Final tool of the 9-tool initial batch; catalog now fully shipped (9 done / 0 blocked).  Resumed after prior structural-finding block (`reviewer_findings_structural_no_fix_attempt` from round-1: shell-density vs. mockup-density mismatch — Compact.png demanded a 2nd identity line + 6 secondary KPIs the shared `BuildCompactShell` does not expose).  Human applied `option_c_shell_density_acceptance` (the same precedent set by `calculate_breakeven_butterfly_tool` at commit `fdac7d2`): keep shell-standard density (single identity line + 3 KPIs) and document the deviation in THESIS `## Mockup conformance` rather than cross-tool refactor the shell.  Catalog design guardrails were updated to encode (a) the Option-(c) shell-density acceptance and (b) the identity-row hazard (do NOT prepend `· ` to `secondary` — the `IdentityBlock` already inserts the glyph; pass bare labels).  Snapshot shape: STIR contract price level + daily change + 252-day z-score + range strip; quote convention `100 - implied rate` (an UP price ⇒ DOWN implied rate / dovish) called out in methodology disclosure.  Standalone typed-detail endpoint at `/api/v1/rates/detail/policy-futures-price`; `PolicyFuturesPriceLevelOutput` mirror type (snake_case preserved, exact field-by-field match to backend `FuturesPriceLevelOutput` Pydantic schema).  Methodology honesty wire-sourced via `data.methodology_disclosure` (no TSX literal).  Both Build views consume the shared `usePolicyFuturesPrice` hook; Monitor widget uses its own local fetcher matching the linker analog pattern.  Reviewer (fresh-context Claude) emitted `APPROVED` confirming all three round-1 findings (#1 + #2 double-`·` bugs + #3 STRUCTURAL density) addressed; no blocking findings on the round-2 builder diff.  Pre-commit gate green: `npm run test:modules` ✓ (`policy_futures_get_futures_price_level_tool: 8 passed, 0 failed`; all 58 modules + registry pass) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v16 | 2026-05-30 | **Frontend factory tool 2 / 9 — `calculate_breakeven_curve_spread_tool` dual-view shipped (APPROVED).**  §4.3 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (BreakevenCurveSpread widget)` (monitor_elig was already `required`).  Resumed after the human re-authored `mockups/{Compact,Extended}.png` to depict the correct 2-leg breakeven curve spread (intake commit `0b1eb86`); prior block (`mockup_content_mismatch`: mockups were byte-identical to tool 1's 3-leg butterfly mockups) cleared.  Same-curve breakeven tenor spread (US 2s10s BE, EUR 2s10s BE, GBP 2s10s BE, etc.); mirrors `calculate_real_yield_curve_spread_tool`'s design (same shape, different concept — inflation compensation, not real yield).  Standalone typed-detail endpoint at `/api/v1/rates/detail/breakeven-curve-spread`; `BreakevenCurveSpreadOutput` mirror type (snake_case preserved, exact field-by-field match to backend `BreakevenCurveSpreadCurrentMetrics` + `BreakevenCurveSpreadOutput` Pydantic schemas — `current_spread_bps`, `short_breakeven_bps`/`long_breakeven_bps`, `short_years`/`long_years`, `spread_label`, `methodology_label`, both `time_series_spread`/`time_series_zscore`).  Methodology honesty wire-sourced via `current_metrics.methodology_label` (no TSX literal).  Mockup deviation (justified, documented in THESIS): slope/regime captions use `Upward Sloping`/`Inverted`/`Flat` instead of the mockup's `Bull Flattening`/`Bear Steepening` because bull/bear/flattening/steepening describe directional MOVES not LEVELS — labelling a snapshot upward-sloping spread "Bull Flattening" would be finance-incorrect.  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings (one explicitly-justified deviation acknowledged).  Pre-commit gate green: `npm run test:modules` ✓ (`calculate_breakeven_curve_spread_tool` covered; all module + registry tests passed) `npm run test:build` ✓ (all build-folder tests passed) `npm run typecheck` ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v15 | 2026-05-29 | **Frontend factory tool 8 / 9 — `calculate_ois_curve_spread_tool` dual-view shipped (APPROVED).**  §4.2 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (OisCurveSpread widget)` (monitor_elig was already `required`).  Second OIS-family tool under the dual-view + standalone-bridge contracts (tool 7 `calculate_ois_butterfly_tool` was the first).  Same-curve 2-leg OIS tenor spread (USD-SOFR 2s10s, EUR-ESTR 2s10s, etc.); mirrors `calculate_real_yield_curve_spread_tool`'s design (same shape, different curve family).  Standalone typed-detail endpoint at `/api/v1/rates/detail/ois-curve-spread`; `OisCurveSpreadOutput` mirror type (snake_case preserved, exact field-by-field match to backend `OISCurveSpreadCurrentMetrics` + `OISCurveSpreadOutput` Pydantic schemas).  Methodology honesty carve-out (sub-domain consistent with tool 7): `OISCurveSpreadCurrentMetrics` does NOT carry a `methodology_label` field, so the disclosure is sourced from `OIS_CURVE_SPREAD_COMPACT_CAVEAT` in `oisCurveSpreadShared.ts` with a `TODO(PR10)` marker — flip to wire-sourced will be a one-line edit once the OIS sub-domain catches up.  Mockup deviations (bull/bear flattening regime substituted with steeper/flatter cues; top-right card #3 substituted with identity + caveat; decomposition row folded into headline strip) forced by wire constraints (bull/bear requires rate-direction history not in Output) or shell-level layout choices, following the just-approved OIS butterfly precedent.  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings.  Pre-commit gate green: test:modules ✓ (`calculate_ois_curve_spread_tool: 6 passed, 0 failed`; all module + registry tests passed) test:build ✓ (all build-folder tests passed) typecheck ✓.  `graphify update` skipped — not on PATH on this VM (recorded per `ORCHESTRATOR_PROMPT` Mirrors §2 fallback). |
| v14 | 2026-05-29 | **Frontend factory tool 7 / 9 — `calculate_ois_butterfly_tool` dual-view shipped (APPROVED).**  §4.2 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (OisButterfly widget)`.  First OIS-family tool brought under the dual-view + standalone-bridge contracts (sibling pattern to the three inflation_swaps + linker butterflies shipped earlier in this wake — same 3-point same-curve fly, different curve family).  Standalone typed-detail endpoint at `/api/v1/rates/detail/ois-butterfly`; `OisButterflyOutput` mirror type (snake_case preserved; exact field-by-field match to backend `OISButterflyCurrentMetrics` + `OISButterflyOutput` Pydantic schemas).  Methodology honesty carve-out: `OISButterflyCurrentMetrics` does NOT carry a `methodology_label` field (distinct from linker/ZCIS sibling Outputs), so the disclosure is sourced from `OIS_BUTTERFLY_COMPACT_CAVEAT` in `oisButterflyShared.ts` with a `TODO(PR10)` marker — flip to wire-sourced will be a one-line edit once the OIS sub-domain catches up.  Family registry honestly uses the playbook's actual short-form curve_family identifiers (USD_SOFR / EUR_ESTR / GBP_SONIA / JPY_OIS / AUD_OIS / CAD_OIS) while labelling the overnight index (SOFR / ESTR / SONIA / TONA / AONIA / CORRA) on the surface — matches the backend schema's "honest disclosure" comment.  Reviewer (fresh-context Claude) emitted `APPROVED` with no blocking findings (one minor non-blocking THESIS prose self-inconsistency: "Mockup conformance" mentions 2 cards while Q2 enumerates all 3 — implementation matches mockup, prose only).  Pre-commit gate green: test:modules ✓ (6 passing per-module checks) test:build ✓ typecheck ✓.  Builder produced 1-byte stdout log (transient claude --print buffering — same pattern as tool 5 reviewer dispatch 2 first attempt) but all 8 artifact slots populated on-disk; no retry needed since the diff was complete. |
| v13 | 2026-05-29 | **Frontend factory tool 6 / 9 — `scan_inflation_swaps_extremes_tool` dual-view shipped (APPROVED).**  §4.4 row updated: `output_kind: scanner-result → scanner-rankedlist`, `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_status: deferred → built (ZcisScanner widget)`.  First scanner-shape tool brought under the dual-view + standalone-bridge contracts (compact view is a top-N ranked table, NOT a sparkline — distinct from the four prior single-series-bps tools).  Standalone typed-detail endpoint at `/api/v1/rates/detail/zcis-scanner`; `ScanInflationSwapsExtremesOutput` + `ScanInflationSwapsExtremesResultRow` mirror types (snake_case preserved, `signal` Literal, `methodology_disclosure` required at row + response level).  Wire's `methodology_disclosure` threaded into Extended `MethodologyCard` "Disclosure" row + Compact footer `title=` tooltip + Monitor widget caveat tooltip (P5 honesty preserved on the load-bearing field).  Mockup deviation (`TIMEFRAME` / `RANK BY` / per-row `PCTILE` absent) documented in THESIS Q3/Q4 as schema-locked / future-scenario — builder correctly aligned to wire over aspirational mockup.  Reviewer (fresh-context Claude) emitted `APPROVED` with one non-blocking observation (unused `instrumentLabel` import — code-hygiene smell, not a typecheck failure since `noUnusedLocals` is off).  Pre-commit gate green: test:modules ✓ (7 passing per-module checks) test:build ✓ typecheck ✓. |
| v12 | 2026-05-29 | **Frontend factory tool 5 / 9 — `calculate_inflation_swap_butterfly_tool` dual-view shipped (APPROVED-AFTER-FIX).**  §4.4 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (InflationSwapButterfly widget)`.  Same-curve 3-point ZCIS butterfly (USD 2-5-10, EUR 2-5-10, GBP 2-5-10, etc.).  Standalone typed-detail endpoint at `/api/v1/rates/detail/zcis-butterfly`; `InflationSwapButterflyOutput` mirror type.  Reviewer (fresh-context Claude) emitted `CHANGES REQUIRED` with 2 concrete + actionable + bounded findings against `surfaces/BuildExtended.tsx`: (a) identity primary used raw `curveFamily` instead of space-separated market label (deviation from mockup); (b) inlined `bucketForPercentile` logic bypassed the existing shared helper.  Both fixed in Dispatch 3 (single-file fix; touched only `surfaces/BuildExtended.tsx`).  Pre-commit gate green after fix: test:modules ✓ test:build ✓ typecheck ✓.  First initial-reviewer-output failure of the wake (1-byte log on first dispatch — transient; succeeded on retry without any prompt change), confirming Case F-style transient failures may simply be retried before escalating to `waiting_quota`. |
| v11 | 2026-05-29 | **Frontend factory tool 4 / 9 — `calculate_cross_market_inflation_swap_spread_tool` dual-view shipped.**  §4.4 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (CrossMarketZcis widget)`.  First inflation_swaps tool brought under the dual-view + standalone-bridge contracts.  Standalone typed-detail endpoint at `/api/v1/rates/detail/cross-market-zcis`; `CrossMarketInflationSwapSpreadOutput` mirror type (snake_case, includes `index_family_caveat` + `methodology_label`).  Catalog design guardrail satisfied: the "CPI-U vs HICPxT vs RPI not fungible" caveat is sourced from `current_metrics.index_family_caveat` (wire-honest, NOT a TSX literal) and surfaced on the compact view's caveat footer, the Monitor widget's bottom row, and the extended view's top-right Index-Families card.  Reviewer (fresh-context Claude) emitted `APPROVED` with no findings.  Pre-commit gate green: test:modules ✓ test:build ✓ typecheck ✓. |
| v10 | 2026-05-29 | **Frontend factory tool 3 / 9 — `calculate_real_yield_butterfly_tool` dual-view shipped.**  §4.3 row updated: `build_archetype: generic-builder → typed-result-renderer`, `build_status: scaffolded → dual-view-built`, `monitor_elig: not-required → required`, `monitor_status: n/a → built (RealYieldButterfly widget)`.  Shipped per `BUILD_GUIDE.md` Stages 4-6 + `rendering_density.md §1` dual-view contract + `methodology_exposure.md §5` standalone-bridge contract.  Files: `module.ts` (tiers=[generic_runnable, custom_build_surface, monitor_surface], typedView=null), `THESIS.md` (5-question contract incl. Mockup-conformance subsection documenting the "TIPS"→"linkers" caveat generalization), `surfaces/{BuildExtended,BuildCompact,realYieldButterflyShared,monitor/RealYieldButterflyWidget}.tsx`, `__tests__/module.spec.ts` (round-trip + dual-view + standalone-bridge + mockup-folder checks), standalone typed-detail endpoint at `/api/v1/rates/detail/real-yield-butterfly`, `fetchDetailRealYieldButterfly` service helper, `RealYieldButterflyOutput` mirror type.  Reviewer (independent Claude, fresh context) emitted `APPROVED` with no findings.  Pre-commit gate green: `npm run test:modules` ✓, `npm run test:build` ✓, `npm run typecheck` ✓.  No row added to §10.3 (pilot-pinned).  Tool 2 of the factory's batch (`calculate_breakeven_curve_spread_tool`) is `blocked` on misfiled mockup PNGs (byte-identical to tool 1's mockups — see `automation/frontend_automation/frontend_tool_catalog.yaml` block_reason for full evidence); requires human re-author of the two PNGs before it can ship.  Tool 1 (`calculate_breakeven_butterfly_tool`, commit fdac7d2) shipped without a §11 entry — its §4.3 row remains stale (scaffolded/not-required) pending a separate update wake. |
| v9 | 2026-05-28 | **Phase-1 pilots Stage B + C — `calculate_breakeven_inflation_simple_tool` + `calculate_real_yield_curve_spread_tool` brought to full parity with `get_real_yield_level_tool`.**  §4.3 rows updated: both → `build_archetype: typed-result-renderer`, `build_status: dual-view-built`, `monitor_status: built` (BreakevenInflation + RealYieldCurveSpread widgets); curve_spread `output_kind` corrected `single-series-bps → single-series-percent` (the spread is in PERCENT; changes in bps); curve_spread `monitor_elig` flipped `not-required → required`.  §10.3 gained a `calculate_real_yield_curve_spread_tool` row and the breakeven row was fully populated.  Backend (Stage 1-3): `exposure:` blocks added to every convention (13 breakeven / 15 curve_spread); 3 rolling-z-score Input overrides + `field_name` exposed (mirror of real_yield_level); `_conventions_from_config(config, params)` override resolver; MCP wrappers extended with integer-sentinel z-kwargs; manifest `one_liner` rewritten PM-facing + `pm_overridable` derived; `TestInputOverrides` + `TestExposureBlockContract` added (170 compute tests green; parity + lint green); curated-metadata migrations written (`database/migrations/2026-05-28_phase1_*_curated.sql`) — NOT applied to the live DB (off-limits this PR), so DB-backed axes (1/5/7) are `in-progress` pending apply.  Frontend (Stage 4-6): per-tool surfaces (`BuildExtended` / `BuildCompact` / `monitor/*Widget` / `*Shared.ts`) + module.ts (dual-view + monitor) + THESIS (Q1-Q5) + dual-view spec; standalone typed-detail endpoints `/detail/breakeven` + `/detail/real_yield_curve_spread` + service helpers + frontend types.  Shared infra fix: `__test-utils.ts` + `loaderPresence.test.ts` surface→tier maps + the FM8 file-existence check now recognise `buildExtended` / `buildCompact` (was a latent gap that also affected `get_real_yield_level_tool`).  `shared_mockups/README.md` documents the multi-tool DAG mockup + the open Stage-D design questions.  Stage 8 (`source_material_verified`) stays `in-progress` for both. |
