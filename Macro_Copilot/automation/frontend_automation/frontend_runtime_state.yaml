version: '1.0'
branch: frontend_automation
backend: claude-cli
orchestrator_status: processing
current_tool: policy_futures__calendar_spread
current_tool_backend_name: policy_futures_get_futures_calendar_spread_tool
current_step: builder_dispatched
dispatch_round: 1
last_dispatch_at: "2026-06-07T19:56:00Z"
last_successful_tool: policy_futures__futures_butterfly
last_successful_commit: fcc5381
last_reviewer_heading: APPROVED
last_gate_result: pass
last_stop_reason: batch_3_addendum_swap_spread_unblocked
last_error: null
last_updated: "2026-06-07T16:50:00Z"
done: 19
blocked: 0
remaining: 10
quota_state: probe_unavailable
quota_snapshot: null
quota_snapshot_at: '2026-05-29T15:52:00Z'
quota_snapshot_fresh: false
quota_note: |
  `claude /status` is not available in non-interactive automation
  ("/status isn't available in this environment").  Per
  QUOTA_AND_RESUME_POLICY.md §3 the orchestrator proceeds in
  runtime-enforced mode: dispatch normally, stop only if the
  actual provider returns a real quota signal (detected by
  parse_reviewer_log.py setting reviewer_quota_exhausted: true).
reviewer_mode: claude_only
reviewer_mode_history:
  - at: '2026-05-29T00:00:00Z'
    note: |
      This factory uses Claude for BOTH builder and reviewer (single-engine
      design).  There is NO fallback engine.  See DESIGN_PRINCIPLES.md §10
      and SINGLE_REVIEW_ROUND_POLICY.md.  Quota exhaustion on either
      dispatch triggers a clean waiting_quota stop; the next cron wake
      retries.
catalog_exhausted_at: null
human_required: null
status_notes:
  - at: "2026-06-07T19:56:00Z"
    kind: tool_20_round1_builder_dispatched
    text: |
      Tool 19 (`policy_futures__futures_butterfly`) shipped at fcc5381
      (+ automation backfill 39958bd) at 19:55 UTC.  Catalog state:
      19 done / 0 blocked / 8 remaining.

      Advanced to next eligible tool per selection-order rule:
      build_order 20 `policy_futures__calendar_spread` (no in_progress
      / changes_required / waiting_quota entries ahead).

      Tool: `policy_futures__calendar_spread`
        - backend_tool_name: `policy_futures_get_futures_calendar_spread_tool`
        - MCP function: `get_futures_calendar_spread_tool` (unprefixed)
        - endpoint_slug: `policy-futures-calendar`
        - reference: `calculate_ois_curve_spread_tool` (same 2-leg
          spread KPI shape; different curve family)
        - STIR calendar spread between two quarterly contracts (e.g.
          SFR1-SFR2), surfacing front-end policy-path slope

      Pre-flight backend audit — ALL PASS:
        - backend folder: rates_agent/policy_futures/tools/futures_calendar_spread/
          (canonical 4-file shape, __init__/compute/config/schemas).
        - MCP wrapper: get_futures_calendar_spread_tool defined at
          rates_agent/policy_futures/mcp_server.py:577.
        - curated migration: 2026-06-07_phase1_policy_futures_get_futures_calendar_spread_curated.sql
          present (2206B).  DB probe path not run under runtime-enforced
          quota mode; migration file satisfies the OR condition per
          PRE_FLIGHT_BACKEND_AUDIT.md.
        - mockups: Compact.png (1313733B) + Extended.png (1720704B)
          present at the canonical primitives/<tool>/mockups/ path.

      Notable wire-shape detail captured in builder prompt:
        - Backend Output ships `methodology_disclosure: str` field on
          `FuturesCalendarSpreadOutput` (NOT `methodology_label` — same
          sibling pattern as tool 19's `FuturesButterflySimpleOutput`).
          Builder instructed to source disclosure from
          `data.methodology_disclosure` directly (no TS literal).

      Builder prompt written to
      `/tmp/builder_prompt_policy_futures__calendar_spread_round1.md`
      (full catalog entry + pre-flight + mockup paths + reference module
      + sibling policy-futures naming-divergence note + methodology
      disclosure threading note + Option (c) shell-density precedent).

      Builder dispatched at 19:56 UTC via
        `nohup bash automation/frontend_automation/run_claude_builder.sh
           /tmp/builder_prompt_policy_futures__calendar_spread_round1.md
           /tmp/claude_frontend_builder_run_policy_futures__calendar_spread_round1.log`
        (PID 4027472).

      Plan for the rest of this wake:
        - Wait for builder to complete (~10-20 min typical).
        - Dispatch reviewer (round 2) via run_claude_reviewer.sh.
        - parse_reviewer_log.py → adjudicate per Case A-F.
        - On APPROVED/APPROVED-AFTER-FIX: pre-commit gate +
          mirrors update + commit + automation backfill.
        - Advance to next eligible tool (build_order 21:
          policy_futures__cross_market_spread).
        - Repeat until quota signal, blocker, or catalog
          exhaustion.

  - at: "2026-06-07T19:55:00Z"
    kind: tool_19_approved_gate_passed_committed
    text: |
      Cron-fired wake at 19:52 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784) resumed tool 19
      (`policy_futures__futures_butterfly` /
      `get_futures_butterfly_simple_tool`).  On entry, the round-2
      reviewer log
      `/tmp/claude_frontend_reviewer_run_policy_futures__futures_butterfly_round2.log`
      was complete (5598 bytes, mtime 19:48 UTC) but unparsed.  No
      live claude builder/reviewer subprocess.

      `parse_reviewer_log.py` returned `{ok: true, heading:
      APPROVED, environment_failure: false,
      reviewer_quota_exhausted: false}` — Case C clean path
      (no fix-pass needed; SINGLE_REVIEW_ROUND_POLICY.md §3
      one-round contract satisfied).  Reviewer's verification
      scorecard (verbatim from log) was green across:
        - Verification ledger: test:modules / test:build /
          typecheck all PASS (reviewer ran in its own sandbox).
        - A) Dual-view contract — module.ts:50 declares all 3
          tiers; module.ts:71-75 populates both buildExtended +
          buildCompact; both surface files exist; Compact has no
          controls strip + calls onExpand + methodology disclosure
          + tone cues + size prop; Extended has full canvas +
          controls + KPI strip + methodology card + lineage.
        - B) Mockup conformance — Compact matches tightly (SFR
          1-2-3 WHITES identity, USA flag, STIR pill, SOFR Strip
          Butterfly subtitle, FLY/1D/Z KPI order, sparkline with
          z-bands, caveat footer); Extended matches structurally
          with two documented honest deviations (FOMC card →
          STRIP CONTEXT card because backend lacks FOMC metadata;
          5D / 1M change cells absent because backend lacks
          weekly/monthly change wire fields — both noted in
          THESIS §4).
        - C) Standalone bridge contract — module.ts:69
          typedView=null; endpoint mounted at
          /api/v1/rates/detail/policy-futures-butterfly
          (api/routes/rates/detail.py:2989); service helper
          fetchDetailPolicyFuturesButterfly added to
          services/ratesApi.ts:802; type mirror
          FuturesButterflySimpleOutput (+ Current + TimeSeriesRow)
          added to types/rates.ts:1768-1862; all three surfaces
          consume usePolicyFuturesButterfly →
          fetchDetailPolicyFuturesButterfly.
        - D) FM1/FM3/FM7/FM8/FM10/FM11/FM12 all check.
        - E) Methodology honesty (PR10 / P5) —
          buildMethodologyRows in futuresButterflySimpleShared.ts
          consumes `data.methodology_disclosure || '—'`
          (wire-sourced, not a TSX literal; grep confirms no
          TSX literal of the disclosure prose).
        - F) Shared cross-surface helper —
          surfaces/futuresButterflySimpleShared.ts holds
          CURVE_REGISTRY / BUTTERFLY_TRIPLETS_BY_CURVE /
          usePolicyFuturesButterfly / KPI builders /
          buildMethodologyRows / sanitiseButterflySeries /
          buildReferenceBands / pctToBps; consumed by Extended +
          Compact + Monitor widget; pctToBps is the SINGLE
          PERCENT-POINTS → bps conversion point across the three
          surfaces (no drift risk).
        - G) Naming-divergence alias — lib/toolNames.ts:79-89
          adds `get_futures_butterfly_simple_tool →
          policy_futures_get_futures_butterfly_simple_tool`,
          mirroring the policy_futures_get_futures_price_level_tool
          precedent.

      Pre-commit gate re-run in orchestrator session (per
      ORCHESTRATOR_PROMPT.md Case C; the gate is the ONLY safety
      net after Dispatch 1 in the no-fix path):
        - `npm run test:modules` ✓ exit 0
          (`policy_futures_get_futures_butterfly_simple_tool:
          8 passed, 0 failed`; all 60-module registry pass).
        - `npm run test:build` ✓ exit 0 (all build-folder
          tests passed including dashboard contract / registry /
          resolveWorkflowArtifacts / artifact-payload / persisted
          adapters / RichModelWidget / widget contract / widget
          format).
        - `npm run typecheck` ✓ exit 0
          (`tsc -p tsconfig.app.json --noEmit` clean).

      Mirrors update (Case C step / ORCHESTRATOR_PROMPT
      Mirrors+commit §1-2):
        - surface_contract.md §4.5 row 246 updated:
          `output_kind: single-series-bps` (unchanged)
          `build_archetype: generic-builder → typed-result-renderer`
          `build_status: scaffolded → dual-view-built`
          `monitor_elig: not-required → required`
          `monitor_status: n/a → built (PolicyFuturesButterfly widget)`.
        - surface_contract.md §11 changelog v27 entry appended
          (full reviewer scorecard + deviation rationale +
          alias-bridge + shared-helper note + gate proof).
        - §10 untouched (Phase-1 pilot rows only; policy-futures
          butterfly is not part of pilot per §10.2 policy).
        - `graphify update .` skipped: not on PATH on this VM
          (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
          no API cost, AST-only — will catch up on a future wake
          if a human installs it).

      Commit pending on frontend_automation with the
      `frontend(futures_butterfly_simple)` template message +
      `Reviewer: APPROVED` disclosure.  Commit includes module +
      surfaces (BuildExtended + BuildCompact + Shared +
      monitor/PolicyFuturesButterflyWidget) + tests + THESIS +
      module.ts + types + service helper + typed-detail endpoint
      + toolNames alias + surface_contract.md mirrors.
      Automation YAMLs (catalog + runtime) deliberately excluded
      from this commit; backfilled in a follow-up
      `automation: backfill` commit per the established
      tool-13..18 pattern.

      Catalog state after commit: 19 done / 0 blocked / 8 remaining.
      Next eligible tool per selection-order rule: build_order 20
      `policy_futures__calendar_spread` (no in_progress /
      changes_required / waiting_quota entries ahead of it).
      Orchestrator continues into pre-flight for tool 20.

  - at: "2026-06-07T19:26:00Z"
    kind: tool_19_round1_builder_dispatched
    text: |
      Cron-fired wake at 19:22 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  Catalog had been
      extended from the original 18-tool batch with 9 additional
      todo tools (build_order 19-27) sometime after the 14:53 UTC
      catalog-exhausted note; entry-point did not gate this wake,
      so orchestrator picked up the first todo per selection-order
      rule.

      Selected tool: `policy_futures__futures_butterfly` (build_order
      19, backend_tool_name `policy_futures_get_futures_butterfly_simple_tool`,
      MCP function `get_futures_butterfly_simple_tool`).  STIR
      3-point butterfly between three quarterly policy-futures
      contracts (e.g. SFR 1-2-3 OIS Butterfly); surfaces curvature
      of the policy-futures strip.  Reference frontend:
      `calculate_ois_butterfly_tool` (OIS butterfly cousin, same
      KPI shape).

      Pre-flight backend audit — ALL PASS:
        - backend folder: rates_agent/policy_futures/tools/futures_butterfly_simple/
          (canonical 4-file shape, __init__/compute/config/schemas).
        - MCP wrapper: get_futures_butterfly_simple_tool defined at
          rates_agent/policy_futures/mcp_server.py:795.
        - curated migration: 2026-06-07_phase1_policy_futures_get_futures_butterfly_simple_curated.sql
          present (2212B).  DB probe path returned auth artifact
          `role "root" does not exist` from non-interactive
          automation — this is the runtime-enforced-quota
          companion artifact, NOT a missing-artifact signal.
          Migration file satisfies the OR condition per
          PRE_FLIGHT_BACKEND_AUDIT.md.
        - mockups: Compact.png (1306999B) + Extended.png (1726281B)
          present at the canonical primitives/<tool>/mockups/ path.

      Builder prompt at /tmp/builder_prompt_policy_futures__futures_butterfly_round1.md
      (236 lines, assembled by an earlier wake on 2026-06-07 at
      15:35 UTC but not dispatched then; content verified to target
      the correct tool with full catalog entry + pre-flight + mockup
      paths + reference module reference).

      Builder dispatched at 19:26 UTC via
        `nohup bash automation/frontend_automation/run_claude_builder.sh
           /tmp/builder_prompt_policy_futures__futures_butterfly_round1.md
           /tmp/claude_frontend_builder_run_policy_futures__futures_butterfly_round1.log`
        (PID 4014137).

      Plan for the rest of this wake:
        - Wait for builder to complete (~10-20 min typical).
        - Dispatch reviewer (round 2) via run_claude_reviewer.sh.
        - parse_reviewer_log.py → adjudicate per Case A-F.
        - On APPROVED/APPROVED-AFTER-FIX: run pre-commit gate +
          mirrors update + commit.
        - Advance to next eligible tool (build_order 20:
          policy_futures__calendar_spread).
        - Repeat until quota signal, blocker, or catalog
          re-exhaustion.

  - at: "2026-06-07T14:53:00Z"
    kind: tool_18_round3_approved_after_fix_committed_catalog_exhausted
    text: |
      Cron-fired wake at 14:37 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784) finished the round-3
      fix-pass shipping path on tool 18
      (`policy_futures__scan_extremes` /
      `get_scan_policy_futures_extremes_tool`) and exhausted the
      initial 18-tool catalog.

      Round 3 fix-pass builder (PID 3924326, dispatched 14:38 UTC
      via run_claude_builder.sh) finished at 14:43 UTC (~5m for a
      single-file targeted fix).  Log:
      /tmp/claude_frontend_builder_run_policy_futures__scan_extremes_round3.log
      (2990 bytes).  BUILDER REPORT confirmed:
        - Files updated: surfaces/BuildCompact.tsx ONLY.
        - Compact header grid widened 6 → 7 columns
          (`grid-cols-[20px_1fr_auto_auto_auto_auto] →
           grid-cols-[20px_1fr_auto_auto_auto_auto_auto]`) at
          header (lines 151-160), data-row (lines 174-230), and
          CompactSkeleton (lines 325-343).
        - Dedicated `1D Δ` cell inserted between Value and Z
          with per-metric rendering rules:
            * OI rows: `${oneDay} ${narrative}` (delta + narrative).
            * VOL rows where backend exposes no 1d Δ: narrative alone.
            * IR rows: bp delta with narrative qualifier.
        - Inline concat stripped from Value sub-line; Value cell
          now renders nativeValue over nativeUnits cleanly.
        - Files NOT touched (per "Apply these findings; do NOT
          redesign" instruction): module.ts, BuildExtended.tsx,
          surfaces/monitor/*, policyFuturesScannerShared.ts,
          types/rates.ts, services/ratesApi.ts, detail.py,
          THESIS.md, __tests__/module.spec.ts.
        - Two observation-only notes from the reviewer (IR+Δ+V+O
          literal kicker; Monitor widget helper duplication) left
          intact per explicit "do NOT dispatch a fix".

      Pre-commit gate re-run in orchestrator session (Case D
      step 5; gate is the ONLY safety net per
      SINGLE_REVIEW_ROUND_POLICY.md §3 — no re-review, no
      Dispatch 4):
        - `npm run test:modules` ✓ exit 0
          (`get_scan_policy_futures_extremes_tool: 7 passed, 0
          failed`; all module + registry tests passed).
        - `npm run test:build` ✓ exit 0
          (all build-folder tests passed).
        - `npm run typecheck` ✓ exit 0
          (`tsc -p tsconfig.app.json --noEmit` clean).

      Mirrors update (Case D step 6 / ORCHESTRATOR_PROMPT
      Mirrors+commit §1-2):
        - surface_contract.md §4.5 row 248 updated:
          `output_kind: scanner-result → scanner-rankedlist`,
          `build_archetype: generic-builder → typed-result-renderer`,
          `build_status: scaffolded → dual-view-built`,
          `monitor_status: deferred → built (PolicyFuturesScanner widget)`.
        - surface_contract.md §11 changelog v26 entry appended
          (catalog-exhaustion + reviewer trail).
        - §10 untouched (Phase-1 pilot rows only; scanner tools
          not part of pilot per §10.2 policy).
        - `graphify update .` skipped: not on PATH on this VM;
          recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback.

      Commit (ba01354) landed on frontend_automation at 14:53 UTC
      with the `frontend(scan_policy_futures_extremes)` template
      message + `Reviewer: APPROVED-AFTER-FIX` disclosure (honest
      acknowledgment that Dispatch 3 bypassed re-review per
      SINGLE_REVIEW_ROUND_POLICY.md).  Commit includes module +
      mockups + surfaces + tests + THESIS + module.ts + types +
      service helper + typed-detail endpoint + surface_contract.md
      mirrors.  Automation YAMLs (catalog + runtime) deliberately
      excluded from this commit; backfilled in a follow-up
      `automation: backfill` commit per the established
      tool-13..17 pattern.

      Catalog state: 18 done / 0 blocked / 0 remaining.
      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule:
        - orchestrator_status → catalog_exhausted.
        - last_stop_reason → catalog_exhausted.
        - catalog_exhausted_at → 2026-06-07T14:53:00Z.
        - current_* nulled (no active tool).
        - last_successful_tool / last_successful_commit advanced
          to tool 18 / ba01354.
        - last_reviewer_heading → APPROVED-AFTER-FIX.
        - last_gate_result → pass.
      Next cron wake will hit the entry-point guard
      (BACKGROUND_EXECUTION_POLICY §8) and exit without invoking
      the orchestrator until the catalog is extended.

  - at: '2026-06-07T14:38:00Z'
    kind: tool_18_round2_changes_required_round3_fix_dispatched
    text: |
      Cron-fired wake at 14:37 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, tool 18
      (`policy_futures__scan_extremes`) was in_progress with
      current_step: reviewer_dispatched / dispatch_round: 2 from the
      14:28 UTC reviewer dispatch earlier this wake-window.  No live
      claude builder/reviewer process; reviewer log
      `/tmp/claude_frontend_reviewer_run_policy_futures__scan_extremes_round2.log`
      complete (3886 bytes; mtime 14:35 UTC).

      parse_reviewer_log.py returned
      {"ok": true, "heading": "CHANGES REQUIRED",
       "environment_failure": false,
       "reviewer_quota_exhausted": false} — Case D.

      Reviewer adjudication per SINGLE_REVIEW_ROUND_POLICY.md §3 +
      ORCHESTRATOR_PROMPT.md Case D step 1:

        Finding 1 (CONCRETE) — surfaces/BuildCompact.tsx:152-159 +
          174-229 + 325-343 — Compact mockup is 7 columns
          (`# | CONTRACT | PACK | SCOPE | VALUE (NATIVE) | 1D Δ |
          Z-SCORE (252D)`) but implementation collapses `1D Δ` +
          narrative into a sub-line of the Value cell producing a
          6-column grid.  Reviewer specifies exact grid template
          delta (`grid-cols-[20px_1fr_auto_auto_auto_auto]` →
          `grid-cols-[20px_1fr_auto_auto_auto_auto_auto]`), new
          column header insert location, where to remove the
          inline concat from the Value sub-line, the per-metric
          rendering rules for OI rows (narrative rides delta) +
          VOL rows (narrative alone when delta is em-dash), and
          requires the CompactSkeleton to be mirrored.  Rule
          violated: rendering_density.md §2.2 + mockup conformance.

        Reviewer also surfaced TWO observation-only notes
        (IR+Δ+V+O hardcoded literal; Monitor widget re-implements
        the data-fetch boilerplate) with explicit "do NOT dispatch
        a fix" — preserved as-is per reviewer intent.

      Finding hygiene check (all pass):
        - No STRUCTURAL: prefix.
        - No vague language ("consider"/"could be cleaner"/
          "perhaps"/"refactor"); exact code change shown.
        - Single file inside the module folder (well under the
          4-file blast-radius rule).
        - Does NOT contradict the mockup — finding IS to bring
          the implementation back into mockup conformance.
        - References real files only (BuildCompact.tsx exists at
          the cited path; lines exist).

      All checks pass → proceed to Dispatch 3 (fix pass) per
      ORCHESTRATOR_PROMPT.md Case D step 2-4.

      Fix-pass prompt assembled at
      /tmp/builder_prompt_policy_futures__scan_extremes_round3.md
      (round 1 prompt seed + verbatim CONCRETE finding + explicit
      "do NOT redesign / do NOT touch other surfaces / do NOT
      address the observation-only notes" instruction).

      Builder re-dispatched at 14:38 UTC via
        `nohup bash automation/frontend_automation/run_claude_builder.sh
           /tmp/builder_prompt_policy_futures__scan_extremes_round3.md
           /tmp/claude_frontend_builder_run_policy_futures__scan_extremes_round3.log`
        (PID 3924326).

      Plan for the rest of this wake:
        - Monitor fix-pass builder to completion (typical
          ~3-10m runtime for a single-file targeted fix).
        - Run pre-commit gate (npm run test:modules +
          test:build + typecheck) — ONLY safety net per
          SINGLE_REVIEW_ROUND_POLICY.md §3 (NO re-review,
          NO Dispatch 4).
        - On gate green: update surface_contract.md mirrors
          (§4 row + §10 status flip + §11 changelog) + run
          `graphify update .` + commit on frontend_automation
          with `last_reviewer_heading: APPROVED-AFTER-FIX`
          honest disclosure.
        - On gate red: mark `human_required` reason
          `pre_commit_gate_failed_after_fix`, capture gate
          output, DO NOT commit.
        - Catalog post-tool-18 (success path): done=18 /
          blocked=0 / remaining=0 → flip orchestrator_status
          to `catalog_exhausted` per ORCHESTRATOR_PROMPT.md
          §Catalog-exhaustion rule.

  - at: '2026-06-07T14:10:00Z'
    kind: tool_18_builder_redispatched_after_quota_reset
    text: |
      Cron-fired wake at 14:07 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, tool 18
      (`policy_futures__scan_extremes`) was in_progress with
      current_step: builder_dispatched from the 10:13 UTC dispatch
      earlier this morning.

      Inspected the prior builder log
      (/tmp/claude_frontend_builder_run_policy_futures__scan_extremes_round1.log;
      45 bytes; mtime 10:15 UTC).  Content: a single line
      `You've hit your limit · resets 1:30pm (UTC)` — Claude
      runtime quota signal.  Per QUOTA_AND_RESUME_POLICY.md §3 +
      §6 the appropriate action is to retry the builder dispatch
      once quota has recovered (the prior wake should have set
      `waiting_quota` and stopped cleanly, but did not flip the
      flag — runtime state stayed as `processing /
      builder_dispatched`).  Reset time was 13:30 UTC; current
      wake at 14:07 UTC is past reset.

      Pre-flight backend audit re-run for tool 18 — ALL PASS
      (same as the 10:13 UTC dispatch):
        Check 1 (backend folder): rates_agent/policy_futures/tools/
          scan_policy_futures_extremes/ (4-file shape; __init__.py
          2293B + compute.py 47574B + config.yaml 29664B +
          schemas.py 32816B; unchanged).
        Check 2 (MCP wrapper): registered at
          rates_agent/policy_futures/mcp_server.py:1669
          (`def get_scan_policy_futures_extremes_tool`).
        Check 3b (curated migration): present at
          database/migrations/2026-05-30_phase1_policy_futures_
          get_scan_policy_futures_extremes_curated.sql (2242B).
        Check 4 (mockups): both Compact.png (1256102B) +
          Extended.png (1687731B) present at the target module's
          mockups/ folder.

      Working tree clean on entry; HEAD at 06f7fdf (tool 17/18
      catalog backfill commit).  Branch gate green
      (`check_branch.sh` OK on frontend_automation).

      Builder re-dispatched at 14:09 UTC via
        `nohup bash automation/frontend_automation/run_claude_builder.sh
           /tmp/builder_prompt_policy_futures__scan_extremes_round1.md
           /tmp/claude_frontend_builder_run_policy_futures__scan_extremes_round1.log`
        (wrapper PID 3912940; claude child PID 3912947).
      Builder prompt unchanged from the 10:12 UTC author — same
      20427-byte prompt (catalog entry verbatim + resolved mockup
      paths + reference module note + scanner-shape siblings
      `scan_inflation_swaps_extremes_tool` + `scan_inflation_linkers_extremes_tool`
      + `scan_bond_futures_extremes_tool` + tool-name aliaser
      note + pre-flight audit output).

      Quota: prior 10:15 UTC builder hit `You've hit your limit ·
      resets 1:30pm (UTC)` runtime signal — confirmed
      `quota_state: exhausted` retroactively for the prior wake.
      Reset window passed at 13:30 UTC.  This wake operates in
      runtime-enforced mode again per §3 (claude /status still
      unavailable non-interactively); will stop cleanly if a
      fresh quota signal returns from the builder or reviewer
      log this wake.

      Plan for this wake:
        - Monitor builder to completion (typical ~10-15m
          runtime for a scanner-shape build).
        - Dispatch reviewer (round 2).
        - parse_reviewer_log.py → Case C/D/E branching.
        - Pre-commit gate on Case C / D Dispatch-3 success.
        - Mirrors + commit + catalog flip → done if APPROVED.
        - Catalog post-tool-18 will have done=18 / blocked=0 /
          remaining=0 → flip orchestrator_status to
          `catalog_exhausted` per ORCHESTRATOR_PROMPT.md
          §Catalog-exhaustion rule.

  - at: '2026-06-07T10:14:00Z'
    kind: tool_17_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 10:07 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, the prior wake's
      reviewer for tool 17 (`bond_futures__scan_extremes`) had already
      finished (reviewer log mtime 09:57 UTC; ~2m runtime; 4375 bytes).
      Builder log mtime 09:50 UTC.  No live builder/reviewer processes
      on entry.

      parse_reviewer_log.py returned
      {"ok": true, "heading": "APPROVED", "environment_failure": false,
       "reviewer_quota_exhausted": false} — Case C.

      Reviewer verdict: bare `APPROVED` last token of the response;
      verification scorecard green across:
        - Dual-view contract (rendering_density.md §2.1/§2.2):
          `module.ts` tiers=[generic_runnable, custom_build_surface,
          monitor_surface]; both `surfaces.buildExtended` +
          `surfaces.buildCompact` populated; Compact has no controls
          strip, calls `onExpand`, methodology reachable via footer +
          `title=` hover; Extended has ControlsStrip + 3 summary cards +
          z-score distribution histogram + ranked detail table +
          MethodologyCard + LineageFooter.
        - Mockup conformance (both PNGs match):
          Compact — BOND FUTURES EXTREMES header, |z|/flagged/P+Δ+V+O
          kicker, 4 ranked rows with SCOPE chips (PRICE/OI/VOL/PRICE for
          TY1/RX1/JB1/G1 sequence reproducible from sorted-by-|z|),
          value-with-narrative dual-line cell, MOST EXTREME highlight
          row, "V1 monitors-only. CTD analytics not in scan." footer,
          "View all N" expand + Fresh + As of date.
          Extended — kicker, "Front-Month Bond Futures · Extremes (252d
          Z-Score)" title, three top-right cards (SCAN SUMMARY /
          DISTRIBUTION ALL METRICS / VS V1 LIMITATIONS), controls strip,
          z-score distribution histogram, three summary callouts, ranked
          detail table with documented columns + "Open in Build"
          deep-link to `futures_price_level_tool`, methodology card +
          lineage footer.
        - Standalone bridge contract (methodology_exposure.md §5):
          `MODULE.typedView: null`; `richModel: false`; endpoint at
          `/api/v1/rates/detail/bond-futures-scanner` with
          `response_model=ScanBondFuturesExtremesOutput`;
          `fetchDetailBondFuturesScanner` helper added to
          `ratesApi.ts`; `ScanBondFuturesExtremesOutput` +
          `ScanBondFuturesExtremesResultRow` + `ScanBondFuturesMetric`
          mirror types snake_case-preserved in `types/rates.ts`.
          All three surfaces (Build + Monitor) consume the same
          endpoint via the shared `useBondFuturesScanner` hook.
        - FM1–FM12: FM1 folder name = `scan_bond_futures_extremes_tool`
          exactly; FM7 pure value export; FM8 all three claimed surfaces
          exist at canonical paths; FM10 THESIS Q1–Q5 substantive with
          per-surface specifics + FM ID cites + ADR 0013;
          FM11 test calls `assertStandardModuleInvariants` AND adds
          dual-view/typedView-null/monitor/mockups checks; FM12
          alphabetical position correct at `src/modules/index.ts:92`
          (between `policy_futures_get_volume_open_interest_snapshot_tool`
          and `scan_inflation_linkers_extremes_tool`).
        - Methodology honesty: `MethodologyCard` rows include
          `data.methodology_disclosure` verbatim (not a TS literal);
          `BOND_FUTURES_SCANNER_COMPACT_CAVEAT` is the short-form
          per-tool caveat used in the compact footer ONLY; full
          disclosure remains the wire value via `title=`.  Matches
          the ZCIS/linker sibling pattern.
        - Shared cross-surface helper:
          `surfaces/bondFuturesScannerShared.ts` houses the curve-family
          registry, metric registry, `useBondFuturesScanner` hook,
          format helpers, scan-summary parser, histogram builder,
          methodology row builder, and reference chips.  All three
          surfaces import from it; no logic duplication.

      One non-blocking observation (kept as code-hygiene note, not a
      fix-pass round): `instrumentCountry` is imported in
      `BuildExtended.tsx:39` but never used (the table uses
      `meta?.countryLabel ?? row.curve_family` directly inline). The
      export exists in the shared file for parity with the per-row
      identity helper triple, so this is a tiny dead import rather
      than a contract violation. Typecheck passes; not worth a fix.

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    `scan_bond_futures_extremes_tool:
                                 7 passed, 0 failed`; all module +
                                 registry tests passed.
        npm run test:build      all build-folder tests pass
                                 (routing 48 / dashboard 11 /
                                  registry 18 / artifacts 28
                                  widget-format 53 etc.).
        npm run typecheck       clean (tsc -p tsconfig.app.json
                                 --noEmit exit 0).

      Mirrors:
        surface_contract.md §4.6 row updated:
          output_kind:     scanner-result   → scanner-rankedlist
          build_archetype: generic-builder  → typed-result-renderer
          build_status:    scaffolded       → dual-view-built
          monitor_status:  deferred         → built (BondFuturesScanner widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v25 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v17–v24).

      Commit on `frontend_automation` per template; catalog tool 17
      flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: 3cb47ac (work commit; SHA backfilled in a follow-up
      catalog/runtime/note commit, mirroring the tool 7-16 pattern).

      Counts post-commit: done=17 / blocked=0 / remaining=1.
      orchestrator_status stays `processing`.

      Same-wake advance to tool 18 (`policy_futures__scan_extremes`,
      build_order 18) per ORCHESTRATOR_PROMPT.md catalog-processing
      rule ("Do not intentionally stop after a single completed tool
      if more eligible catalog work remains"):

        Pre-flight backend audit for tool 18: ALL PASS
          Check 1 (backend folder): canonical 4-file shape at
            rates_agent/policy_futures/tools/scan_policy_futures_extremes/
            (__init__.py 2293B + compute.py 47574B +
             config.yaml 29664B + schemas.py 32816B).
          Check 2 (MCP wrapper): registered at
            rates_agent/policy_futures/mcp_server.py:1669
            (def get_scan_policy_futures_extremes_tool).
          Check 3b (curated migration): present at
            database/migrations/2026-05-30_phase1_policy_futures_get_scan_policy_futures_extremes_curated.sql
            (2242 bytes; DB schema not migrated on this VM so 3b
            is the active gate per PRE_FLIGHT_BACKEND_AUDIT.md §2).
          Check 4 (mockups): both Compact.png (1256102 bytes) +
            Extended.png (1687731 bytes) present at the target
            module's mockups/ folder (intake commit pre-dating
            this wake; folder mtime 2026-06-06T22:31Z).

        Builder dispatched at 10:13 UTC via
          `nohup bash automation/frontend_automation/run_claude_builder.sh
             /tmp/builder_prompt_policy_futures__scan_extremes_round1.md
             /tmp/claude_frontend_builder_run_policy_futures__scan_extremes_round1.log`
          (child claude PID 3842862; wrapper 3842855;
          check_branch.sh green at dispatch).  Prompt at
          /tmp/builder_prompt_*_round1.md (295 lines; catalog
          entry verbatim + resolved mockup paths + reference module
          note + scanner-shape siblings (scan_inflation_linkers /
          scan_bond_futures) + tool-name aliaser note + pre-flight
          audit output).

        Reference frontend module: `scan_inflation_swaps_extremes_tool`
        (commit 07372db) — Phase-1 scanner-shape pattern.  Two
        sibling scanner-shape modules also available as second/third
        references: `scan_inflation_linkers_extremes_tool`
        (commit 378a8b7) + `scan_bond_futures_extremes_tool`
        (commit 3cb47ac just-shipped).

        Tool-name alias note: frontend folder + MCP function =
        `get_scan_policy_futures_extremes_tool`; workflow registry
        uses `policy_futures_get_scan_policy_futures_extremes_tool`
        prefix (mirrors the `policy_futures_get_futures_price_level_tool`
        precedent).  TS aliaser at `src/lib/toolNames.ts` may need
        a KNOWN_TOOL_ALIASES entry — builder advised to check the
        existing pattern.

        Wake exits cleanly here with the builder in flight; the
        orchestrator will poll the builder to completion this same
        wake, dispatch the reviewer (round 2), parse the reviewer
        log, run the pre-commit gate on Case C, and commit.  If the
        builder runs long, the next cron wake will resume per
        QUOTA_AND_RESUME_POLICY.md §3.

        Quota: still in runtime-enforced mode per
        QUOTA_AND_RESUME_POLICY.md §3 (claude /status not
        available non-interactively).  No quota signal from
        any dispatch this wake.

  - at: '2026-06-07T09:32:00Z'
    kind: tool_16_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 09:22 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, the prior wake's
      builder for tool 16 (`inflation_indexed_bonds__scan_linkers_extremes`)
      was still in flight (PID 3818120; dispatch started 09:08 UTC; ~14m
      elapsed at wake start; in post-build self-test phase running
      `npm run test:modules`).  The orchestrator monitored to completion:
      builder finalised 09:23 UTC (~15m runtime).  BUILDER REPORT clean —
      4 new surface files (BuildExtended.tsx + BuildCompact.tsx +
      scanInflationLinkersExtremesShared.ts + monitor/LinkerScannerWidget.tsx)
      + 3 overwrites in target module folder (module.ts + THESIS.md +
      __tests__/module.spec.ts) + types/rates.ts +
      services/ratesApi.ts + api/routes/rates/detail.py; self-tests green:
      test:modules + test:build + typecheck all exit 0.  Loader entry at
      src/modules/index.ts line 94 already present from prior Stage 3
      scaffold (alphabetical between calculate_ois_cross_market_spread_tool
      and scan_inflation_swaps_extremes_tool); FM12 satisfied without
      additional edit.

      Builder documented one honest deviation: committed mockups envision
      a composite RY + BE (real-yield + breakeven) scanner with a per-row
      SCOPE column tagging each row; the current backend
      (schemas.py:ScanInflationLinkersExtremesOutput) is single-metric —
      ranks only on z_score_real_yield per the catalog's literal wording.
      The backend's config.yaml planned_extensions block notes that a
      multi-metric (RY + BE) ranking is contingent on a catalog amendment.
      Builder built faithful to the backend wire (single-metric RY scan)
      rather than invent breakeven rankings the wire doesn't produce.  The
      mockup's VALUE column with mixed bp/% units (implying BE deltas vs
      RY levels) is rendered as Real Yield (%) uniformly, and the SCOPE
      column is omitted.  THESIS §4 documents this as future-state design.

      Reviewer dispatched 09:25 UTC via run_claude_reviewer.sh
      (PID 3824225).  Reviewer log finalised 09:29 UTC (~4m, 3510 bytes).
      parse_reviewer_log.py returned
      {"ok": true, "heading": "APPROVED", "environment_failure": false,
       "reviewer_quota_exhausted": false} — Case C.

      Reviewer verdict: bare `APPROVED` last token of the response;
      verification scorecard green across dual-view contract
      (rendering_density.md §1 + §2.1 + §2.2), mockup conformance (judged
      against backend-wire shape per the reviewer policy — deviation
      explicitly judged honest: "the FM contract / dual-view / bridge
      contracts are satisfied for the as-built single-metric scanner"),
      standalone bridge contract (methodology_exposure.md §5; endpoint at
      /api/v1/rates/detail/linkers-scanner; typedView=null;
      ScanInflationLinkersExtremesOutput + ResultRow mirror types exact
      field-by-field with backend Pydantic schemas — snake_case preserved,
      signal Literal enum closed identically, methodology_disclosure
      required at both row and response level, nullable maturity_date /
      country / vendor_ticker), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-surface
      specific with FM ID cites; FM12 alphabetical loader at index.ts:94
      between calculate_ois_cross_market_spread_tool and
      scan_inflation_swaps_extremes_tool — correct ASCII position),
      methodology honesty (data.methodology_disclosure threaded into
      Extended methodology card's "Disclosure" row via buildMethodologyRows;
      Compact footer + Monitor widget surface the same wire field as the
      title= tooltip; LINKER_SCANNER_COMPACT_CAVEAT is desk-canonical
      visible text — no TSX literal replaces the wire string), shared
      scanInflationLinkersExtremesShared.ts helper consolidation
      (useLinkersScanner co-located across all three surfaces; format
      helpers, instrument label/flag, scan-summary parser, histogram
      builder, methodology rows; no duplicated logic), and per-row drill-
      down from the Extended ranked table to get_real_yield_level_tool
      matching THESIS Q2's deep-link claim.

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    `scan_inflation_linkers_extremes_tool:
                                 7 passed, 0 failed`; all module +
                                 registry tests passed.
        npm run test:build      all build-folder tests pass.
        npm run typecheck       clean (tsc -p tsconfig.app.json
                                 --noEmit exit 0).

      Mirrors:
        surface_contract.md §4.7 row updated:
          output_kind:     scanner-result   → scanner-rankedlist
          build_archetype: generic-builder  → typed-result-renderer
          build_status:    scaffolded       → dual-view-built
          monitor_status:  deferred         → built (LinkerScanner widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v24 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v17–v23).

      Commit on `frontend_automation` per template; catalog tool 16
      flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: 378a8b7.

      Counts post-commit: done=16 / blocked=0 / remaining=2.
      orchestrator_status stays `processing`.

      Same-wake advance to tool 17 (`bond_futures__scan_extremes`,
      build_order 17) per ORCHESTRATOR_PROMPT.md catalog-processing
      rule ("Do not intentionally stop after a single completed tool
      if more eligible catalog work remains").

  - at: '2026-06-07T09:15:00Z'
    kind: tool_15_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 08:52 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, the prior wake's
      builder for tool 15 (`inflation_indexed_bonds__forward_breakeven`)
      was still in flight (PID 3807861, ~9m elapsed at wake start).  The
      orchestrator monitored to completion: builder finalised 08:58 UTC
      (~13m runtime; BUILDER REPORT clean — all 4 new surface files +
      3 overwrites in target module folder + types/rates.ts +
      services/ratesApi.ts + api/routes/rates/detail.py; self-tests
      green: test:modules + test:build + typecheck all exit 0).  Loader
      entry at src/modules/index.ts:55 already present (no edit needed).

      Reviewer dispatched 08:59 via run_claude_reviewer.sh (PID 3814625).
      Reviewer log finalised 09:03 (~4m, 4487 bytes).
      parse_reviewer_log.py returned
      {"ok": true, "heading": "APPROVED", "environment_failure": false,
       "reviewer_quota_exhausted": false} — Case C.

      Reviewer verdict: bare `APPROVED` last token of the response;
      verification scorecard green across dual-view contract
      (rendering_density.md §1 + §2.1 + §2.2), mockup conformance
      (Compact + Extended both match Compact.png / Extended.png —
      US · 5Y5Y FORWARD BREAKEVEN identity, 9-cell bps KPI strip,
      forward chart with z-score bands, methodology card, compact
      3-KPI strip with sparkline + caveat footer + expand affordance),
      standalone bridge contract (methodology_exposure.md §5; endpoint
      at /api/v1/rates/detail/forward-breakeven; typedView=null;
      ForwardBreakevenSimpleOutput mirror type exact field-by-field
      with backend Pydantic schemas), FM1–FM12 (FM10 Q1/Q2/Q3/Q5 per-
      surface specific with FM ID cites; FM12 alphabetical loader at
      index.ts:55 between calculate_curve_spread_tool and
      calculate_half_life_tool — correct ASCII position), methodology
      honesty (current_metrics.methodology_label sourced via
      buildMethodologyRows in forwardBreakevenShared.ts:437 — no TSX
      literal; honest fallback string only fires when backend returns
      empty), shared forwardBreakevenShared.ts helper consolidation
      (both Build views consume useForwardBreakeven, compactKPIs/
      extendedKPIs, pairForLinker/forwardPairFor/forwardPairOptions
      ForLinker, buildReferenceBands, buildMethodologyRows,
      sanitiseForwardBreakevenSeries, FORWARD_BREAKEVEN_COMPACT_CAVEAT;
      Monitor widget uses local fetcher matching the reference
      BreakevenInflationWidget paramsKey de-dup pattern — explicitly
      acceptable per reviewer's non-blocking observation), and Pair/
      forward-pair model (single Country-Pair linker-keyed dropdown +
      same-country invariant + per-pair forward-pair filtering against
      BREAKEVEN_TENORS_BY_PAIR; defaultParams forward_pair: '5Y5Y' /
      start_tenor: '5Y' / end_tenor: '10Y' satisfies backend's
      _start_must_precede_end validator on first render).  Two non-
      blocking observations noted: (a) Extended "Inflation compensation"
      top-right card uses the longer compact caveat ("Inflation
      compensation, not expected inflation. Compounds × 2 pillars +
      forward.") while mockup small-print reads "Forward inflation
      compensation, not expected inflation" — implementation is
      strictly more informative; (b) widget local fetcher rather than
      reusing useForwardBreakeven — identical pattern to reference
      BreakevenInflationWidget.

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    `calculate_forward_breakeven_simple_tool:
                                 10 passed, 0 failed`; all module +
                                 registry tests passed.
        npm run test:build      all build-folder tests pass.
        npm run typecheck       clean (tsc -p tsconfig.app.json
                                 --noEmit exit 0).

      Mirrors:
        surface_contract.md §4.3 row updated:
          build_archetype: generic-builder → typed-result-renderer
          build_status:    scaffolded     → dual-view-built
          monitor_status:  deferred       → built (ForwardBreakeven widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v23 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v17–v22).

      Commit on `frontend_automation` per template; catalog tool 15
      flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: pending-backfill (a separate small commit will land
      the actual SHA after the work commit, mirroring the tool
      7/8/9/10/11/12/13/14 backfill pattern).

      Counts post-commit, after backfill: done=15 / blocked=0 /
      remaining=3.  orchestrator_status stays `processing`.

  - at: '2026-06-07T08:42:00Z'
    kind: tool_14_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 08:37 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, the prior
      wake's reviewer for tool 14 (`ois__cross_market_spread`) had
      already finished (reviewer log mtime 07:30 UTC; ~5m runtime;
      3483 bytes).  `parse_reviewer_log.py` returned
      {"ok": true, "heading": "APPROVED", "environment_failure": false,
       "reviewer_quota_exhausted": false} — Case C.

      Reviewer verdict: bare `APPROVED` last token of the response;
      verification scorecard green across dual-view contract
      (rendering_density.md §1 + §2.1 + §2.2), mockup conformance
      (Compact + Extended both match Compact.png / Extended.png),
      standalone bridge contract (methodology_exposure.md §5;
      endpoint at /api/v1/rates/detail/ois-cross-market-spread;
      typedView=null), FM1–FM12, methodology honesty
      (acknowledged-gap consistent with OIS sub-domain pattern —
      OISCrossMarketSpreadCurrentMetrics lacks methodology_label;
      caveat sourced from OIS_CROSS_MARKET_COMPACT_CAVEAT in
      crossMarketOisShared.ts with TODO(PR10) marker), shared
      helper consolidation, cross-curve invariant guard.

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    `calculate_ois_cross_market_spread_tool:
                                 6 passed, 0 failed`; all module +
                                 registry tests passed (58+ modules).
        npm run test:build      all build-folder tests pass
                                 (routing 48 / dashboard 11 /
                                  registry 18 / artifacts 28 /
                                  widget-format 53 etc.).
        npm run typecheck       clean (tsc -p tsconfig.app.json
                                 --noEmit exit 0).

      Mirrors:
        surface_contract.md §4.2 row updated:
          build_archetype: generic-builder → typed-result-renderer
          build_status:    scaffolded     → dual-view-built
          monitor_status:  deferred       → built (CrossMarketOis widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v22 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v17–v21).

      Commit on `frontend_automation` per template; catalog tool
      14 flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: pending-backfill (a separate small commit will
      land the actual SHA after the work commit, mirroring the
      tool 7/8/9/10/11/12/13 backfill pattern).

      Counts post-commit, after backfill: done=14 / blocked=0 /
      remaining=4.  orchestrator_status stays `processing`.

      Same-wake advance to tool 15
      (`inflation_indexed_bonds__forward_breakeven`, build_order
      15) per ORCHESTRATOR_PROMPT.md catalog-processing rule
      ("Do not intentionally stop after a single completed tool
      if more eligible catalog work remains"):

        Pre-flight backend audit for tool 15: ALL PASS
          Check 1 (backend folder): canonical 4-file shape at
            rates_agent/inflation_indexed_bonds/tools/forward_breakeven_simple/
            (__init__.py + compute.py + config.yaml + schemas.py).
          Check 2 (MCP wrapper): registered at
            rates_agent/inflation_indexed_bonds/mcp_server.py:632
            (def calculate_forward_breakeven_simple_tool).
          Check 3b (curated migration): present at
            database/migrations/2026-05-30_phase1_calculate_forward_breakeven_simple_curated.sql
            (2224 bytes; DB schema not migrated on this VM so 3b
            is the active gate per PRE_FLIGHT_BACKEND_AUDIT.md §2).
          Check 4 (mockups): both Compact.png (1292075 bytes) +
            Extended.png (1720640 bytes) present at the target
            module's mockups/ folder (intake commit pre-dating
            this wake; folder mtime 2026-06-06T22:31Z).

        Builder dispatched at 08:45 UTC via
          `bash automation/frontend_automation/run_claude_builder.sh
             /tmp/builder_prompt_inflation_indexed_bonds__forward_breakeven_round1.md
             /tmp/claude_frontend_builder_run_inflation_indexed_bonds__forward_breakeven_round1.log`
          (PID 3807861, nohup, &; check_branch.sh green
          at dispatch).  Prompt at /tmp/builder_prompt_*_round1.md
          (~7KB; catalog entry verbatim + resolved mockup paths
          + reference module note + pre-flight audit output +
          no-alias-bridge note).

        Reference frontend module: Phase-1 pilot
        `calculate_breakeven_inflation_simple_tool` (breakeven
        family pattern).  Closest shape sibling for forward
        two-pillar implied:  `calculate_ois_forward_rate_tool`
        (commit 031f71d) — its v20 deviation-handling pattern is
        precedent for wire-vs-mockup gaps on the forward KPI strip.

        Wake exits cleanly here with the builder in flight; the
        next cron fire will:
          (a) poll builder to completion (per prior wake patterns
              for tool 11/12/13/14 the builder typically takes
              10-20m to complete);
          (b) dispatch the reviewer (round 2) via
              run_claude_reviewer.sh with a fresh-context prompt
              wrapping CLAUDE_REVIEWER_PROMPT.md + the catalog
              entry + the resolved mockup paths + the builder's
              `git diff` and newly-untracked surface files;
          (c) parse the reviewer log via parse_reviewer_log.py
              and apply the Case A–F adjudication;
          (d) on Case C: run the pre-commit gate + mirrors +
              commit.

        Quota: still in runtime-enforced mode per
        QUOTA_AND_RESUME_POLICY.md §3 (claude /status not
        available non-interactively).  No quota signal from
        any dispatch this wake.

  - at: '2026-06-07T06:10:00Z'
    kind: tool_13_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 05:58 UTC on 2026-06-07 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, the prior
      wake's builder for tool 13 (`inflation_swaps__curve_spread`)
      had already finished (log mtime 05:02 UTC; ~10m runtime;
      builder self-tests green; BUILDER REPORT clean — module.ts +
      THESIS.md + __tests__/module.spec.ts overwrites + 4 new
      surface files BuildExtended/BuildCompact/
      inflationSwapCurveSpreadShared/monitor widget + types/rates.ts
      + services/ratesApi.ts + api/routes/rates/detail.py).  No
      edit to src/modules/index.ts (loader entry already present);
      no tool-name alias needed.

      Reviewer dispatched 05:59 via run_claude_reviewer.sh
      (PID 3756826).  Prompt at
      /tmp/reviewer_prompt_inflation_swaps__curve_spread_round2.md
      (1997 lines — catalog entry + mockup paths + 626-line tracked
      diff + 4 new untracked surface files appended verbatim).
      Reviewer log finalised 06:06 (~6m, 3801 bytes).

      Reviewer verdict: `# APPROVED` (first heading of response,
      followed by full verification scorecard + "No findings.").
      parse_reviewer_log.py returned `heading: null` — its regex
      requires bare `APPROVED` on a line by itself, not the
      markdown heading `# APPROVED`.  The reviewer DID emit the
      heading exactly as instructed by the reviewer prompt
      ("Emit EXACTLY ONE of these headings as the first heading
      of your response") — this is a parser quirk, not a reviewer
      ambiguity.  Visual inspection of the log confirmed the
      unambiguous APPROVED verdict (zero blocking findings;
      verification scorecard green across dual-view contract,
      compact/extended views, standalone bridge, methodology
      honesty, shared helper, FM11, FM12, pre-commit gates,
      THESIS conformance, and mockup conformance).  Treated as
      Case C per ORCHESTRATOR_PROMPT.md; documented honestly in
      the §11 changelog entry for v21.

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    `calculate_inflation_swap_curve_spread_tool:
                                 6 passed, 0 failed`; all 58+ modules +
                                 registry pass.
        npm run test:build      all build-folder tests pass
                                 (routing 48 / dashboard 11 /
                                  registry 18 / artifacts 28 etc.).
        npm run typecheck       clean (tsc -p tsconfig.app.json
                                 --noEmit exit 0).

      Mirrors:
        surface_contract.md §4.4 row updated:
          build_archetype: generic-builder → typed-result-renderer
          build_status:    scaffolded     → dual-view-built
          monitor_status:  deferred       → built (InflationSwapCurveSpread widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v21 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v20, v19, v18, v17, v16, v15, etc.).

      Commit on `frontend_automation` per template; catalog tool
      13 flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: pending-backfill (a separate small commit will
      land the actual SHA after the work commit, mirroring the
      tool 7/8/9/10/11/12 backfill pattern).

      Counts on exit (post-commit, after backfill): done=13 /
      blocked=0 / remaining=5.  orchestrator_status stays
      `processing` and the wake exits cleanly here to let the
      next cron fire pick up the next eligible tool — tool 14
      `ois__cross_market_spread`, build_order 14, status `todo`.
      Quota note unchanged: still in runtime-enforced mode per
      QUOTA_AND_RESUME_POLICY.md §3.

  - at: '2026-06-06T23:30:00Z'
    kind: tool_11_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 23:07 UTC on 2026-06-06 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry the prior
      wake's builder for tool 11 (`inflation_swaps__rate_level`)
      was running (PID 3627372, started 23:06).  Polled to
      completion at 23:20 (~14m runtime, exit 0; 3.5KB BUILDER
      REPORT; builder-side self-tests green).

      Builder produced module.ts + THESIS.md +
      __tests__/module.spec.ts overwrites + 4 new surface files
      (BuildExtended.tsx / BuildCompact.tsx /
      inflationSwapRateLevelShared.ts /
      monitor/InflationSwapRateLevelWidget.tsx) + types/rates.ts +
      services/ratesApi.ts + api/routes/rates/detail.py additions.
      No edit to src/modules/index.ts (loader entry already
      present from Stage-3 scaffold); no alias needed in
      src/lib/toolNames.ts (frontend folder name matches MCP
      function + workflow registry name).

      Reviewer dispatched 23:22 via run_claude_reviewer.sh
      (PID 3633400).  Prompt at
      /tmp/reviewer_prompt_inflation_swaps__rate_level_round2.md
      (1896 lines).  Reviewer log finalised 23:26 (~5m, 4130
      bytes).  parse_reviewer_log.py returned
      {"ok": true, "heading": "APPROVED",
      "environment_failure": false,
      "reviewer_quota_exhausted": false} — Case C.

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    all module + registry tests
                                 passed (including the new
                                 module's spec).
        npm run test:build      all build-folder tests passed
                                 (routing 48 / dashboard 11 /
                                  registry 18 / artifacts 28
                                  etc.).
        npm run typecheck       clean
                                 (tsc -p tsconfig.app.json
                                  --noEmit exit 0).

      Mirrors:
        surface_contract.md §4.4 row updated:
          build_archetype: generic-builder → typed-result-renderer
          build_status:    scaffolded     → dual-view-built
          monitor_status:  deferred       → built (InflationSwapRateLevel widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v19 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v18, v17, v16, v15, v14, v13, etc.).

      Commit on `frontend_automation` per template; catalog tool
      11 flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: pending-backfill (a separate small commit will
      land the actual SHA after the work commit, mirroring the
      tool 7/8/9/10 backfill pattern).

      Counts on exit (post-commit, after backfill): done=11 /
      blocked=0 / remaining=7.  orchestrator_status stays
      `processing` and advances to the next eligible tool in the
      same wake (tool 12 `ois__forward_rate`, build_order 12).

      Quota: still in runtime-enforced mode per
      QUOTA_AND_RESUME_POLICY.md §3 (claude /status not
      available non-interactively).  No quota signal from
      the reviewer log.

  - at: '2026-06-06T23:03:00Z'
    kind: tool_10_shipped_batch_2_advancing
    text: |
      Cron-fired wake at 22:52 UTC on 2026-06-06 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  On entry, the prior
      wake's builder for tool 10 (`ois__rate_level`) was still
      running (PID 3617629, started 22:41).  Waited for builder to
      exit (22:55, ~14m runtime, exit 0; 3.5KB BUILDER REPORT;
      builder-side self-tests green).

      Builder produced module.ts + THESIS.md + __tests__/module.spec.ts
      overwrites + 4 new surface files
      (BuildExtended.tsx 291L / BuildCompact.tsx 96L /
       oisRateLevelShared.ts 568L / monitor/OisRateLevelWidget.tsx 207L)
      + types/rates.ts + services/ratesApi.ts + api/routes/rates/detail.py
      additions.  No edit to src/modules/index.ts or
      src/lib/toolNames.ts (loader entry + alias already present).

      Reviewer dispatched 22:56 via run_claude_reviewer.sh
      (background id b6s9v2h61).  Prompt at
      /tmp/reviewer_prompt_ois__rate_level_round2.md (1856 lines —
      catalog entry + mockup paths + 1775-line diff bundle).
      Reviewer log /tmp/claude_frontend_reviewer_run_ois__rate_level_round2.log
      finalized 23:01 (~5m, 3480 bytes).  parse_reviewer_log.py
      returned {"ok": true, "heading": "APPROVED",
      "environment_failure": false, "reviewer_quota_exhausted":
      false} — Case C.  Reviewer cited zero blocking findings;
      confirmed dual-view contract, standalone bridge,
      FM1-FM12, mockup conformance (Compact 3 KPIs + extended
      9-cell strip + per-family caveat), and OIS sub-domain
      methodology-label carve-out (TODO(PR10) anchored to
      FAMILY_REGISTRY, matching OIS curve_spread / butterfly
      siblings).

      Pre-commit gate (orchestrator-run from
      UI/macro-copilot-dashboard-polished/):
        npm run test:modules    `get_ois_rate_level_tool: 9 passed,
                                  0 failed`; all 58+ modules +
                                  registry pass
        npm run test:build      all build-folder tests pass
                                  (routing 48 / dashboard 11 /
                                   registry 18 / artifacts 28
                                   etc.)
        npm run typecheck       clean (tsc -p tsconfig.app.json
                                  --noEmit exit 0)

      Mirrors:
        surface_contract.md §4.2 row updated:
          build_archetype: generic-builder → typed-result-renderer
          build_status:    scaffolded      → dual-view-built
          monitor_status:  deferred        → built (OisRateLevel widget)
          (monitor_elig already `required`)
        surface_contract.md §11 added v18 entry covering this ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v17, v16, v15, v14, v13, etc.).

      Commit on `frontend_automation` per template; catalog tool 10
      flipped → `status: done`, `last_review_heading: APPROVED`,
      `last_gate_result: pass`, `active_step: committed`.
      commit_sha: pending-backfill (a separate 2nd small commit
      will land the actual SHA after the work commit, mirroring
      the tool 7 / tool 8 / tool 9 backfill pattern).

      Counts on exit (post-commit, after backfill): done=10 /
      blocked=0 / remaining=8.  orchestrator_status stays
      `processing` and advances to the next eligible
      tool in the same wake (tool 11 `inflation_swaps__rate_level`,
      build_order 11).

      Quota: still in runtime-enforced mode per
      QUOTA_AND_RESUME_POLICY.md §3 (claude /status not
      available non-interactively).  No quota signal from the
      reviewer log.  Will continue advancing until the actual
      provider returns a real quota signal.

  - at: '2026-06-06T22:40:00Z'
    kind: wake_start_batch_2_tool_10_dispatched
    text: |
      Cron-fired wake at 22:39 UTC on 2026-06-06 (cron id
      450aa21c-625d-4ad7-b3da-ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md
      + the full automation package + frontend_tool_catalog.yaml +
      frontend_runtime_state.yaml.  Branch check passed (on
      `frontend_automation`, HEAD 2b76b5e "feat(factory-batch-2):
      queue 9 new tools for frontend factory").  Tree clean on entry.

      Catalog state on entry:
        done=9, blocked=0, remaining=9 — batch-2 queued by the user
        with 9 new `todo` entries (build_order 10-18).  No
        in_progress / changes_required / waiting_quota entries.
        Selection order picks first todo by build_order ⇒ tool 10
        (`ois__rate_level`).

      Pre-flight backend audit for tool 10 (read-only):
        Check 1  rates_agent/ois/tools/rate_level/ 4-file shape   PASS
        Check 2  rates_agent/ois/mcp_server.py:217
                 `def calculate_ois_rate_level_tool(...)`         PASS
        Check 3a docker exec macro-tsdb psql tool_metadata row    N/A
                 (relation macro_data.tool_metadata does not exist
                 on this VM's local Postgres — DB schema not
                 migrated; per PRE_FLIGHT_BACKEND_AUDIT.md §2 the
                 OR-clause path b satisfies the gate)
        Check 3b database/migrations/2026-05-30_phase1_calculate_ois_rate_level_curated.sql
                 (2024 bytes)                                     PASS
        Check 4  mockups Compact.png (1398102 B) + Extended.png
                 (1598127 B) at
                 UI/macro-copilot-dashboard-polished/src/modules/primitives/get_ois_rate_level_tool/mockups/
                                                                  PASS

      Builder dispatched at 22:40 UTC via run_claude_builder.sh with
      prompt /tmp/builder_prompt_ois__rate_level_round1.md, log
      /tmp/claude_frontend_builder_run_ois__rate_level_round1.log.
      Background pid tracked by OpenClaw exec (background id
      b4fghoasm).  Runtime state: current_tool=ois__rate_level,
      current_step=builder_dispatched, dispatch_round=1.

      Quota: claude /status path not usable from non-interactive
      automation (same as prior wakes); proceeding in
      runtime-enforced quota mode per QUOTA_AND_RESUME_POLICY.md §3.
      Will detect quota exhaustion via parse_reviewer_log.py's
      reviewer_quota_exhausted signal.

  - at: '2026-05-30T02:07:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 02:07 UTC on 2026-05-30 (cron id 450aa21c-
      625d-4ad7-b3da-ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md +
      frontend_tool_catalog.yaml + frontend_runtime_state.yaml.
      Branch check passed (on `frontend_automation`).  Tree clean
      against HEAD 5cdfd4c on entry.  Catalog state unchanged from
      prior wake @01:52: done=9, blocked=0, remaining=0, no
      eligible work under the selection order
      (in_progress / changes_required / waiting_quota / todo all
      empty).  Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule +
      BACKGROUND_EXECUTION_POLICY §8, this wake is a clean no-op:
      orchestrator_status + last_stop_reason remain
      `catalog_exhausted`, last_updated bumped to 02:07, no
      dispatches, no source code changes.  Second consecutive no-op
      wake since tool 9 shipped.  The cron entry-point short-circuit
      (BACKGROUND_EXECUTION_POLICY §8.2) is still not engaging — the
      cron continues to dispatch the orchestrator prompt even with
      `orchestrator_status: catalog_exhausted` set.  That is a cron
      entry-point bug, not an orchestrator bug; tracked via the
      recurring pattern in this status_notes log.  Resumption
      requires a human to either (a) append a new entry to
      `frontend_tool_catalog.yaml` with non-terminal status, or
      (b) set `orchestrator_status: idle` in this file per
      BACKGROUND_EXECUTION_POLICY §8.3.  Wake exits cleanly.

  - at: '2026-05-30T01:52:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 01:52 UTC on 2026-05-30 (cron id 450aa21c-
      625d-4ad7-b3da-ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md +
      frontend_tool_catalog.yaml + frontend_runtime_state.yaml.
      Branch check passed (on `frontend_automation`).  Tree clean
      against HEAD cbd5613 on entry.  Catalog state unchanged from
      prior wake @01:38 (tool 9 shipped that wake; nothing has
      mutated the catalog since): done=9, blocked=0, remaining=0,
      no eligible work under the selection order
      (in_progress / changes_required / waiting_quota / todo all
      empty).  Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule +
      BACKGROUND_EXECUTION_POLICY §8, this wake is a clean no-op:
      orchestrator_status + last_stop_reason remain
      `catalog_exhausted`, last_updated bumped to 01:52, no
      dispatches, no source code changes.  First no-op wake since
      tool 9 shipped (the prior consecutive no-op streak under the
      pre-resolution `done=7 / blocked=2` state ended when tools 2
      and 9 were unblocked + shipped @00:48 → 01:38).  The cron
      entry-point short-circuit (BACKGROUND_EXECUTION_POLICY §8.2)
      is still not engaging — the cron continues to dispatch the
      orchestrator prompt even with `orchestrator_status:
      catalog_exhausted` set.  That is a cron entry-point bug, not
      an orchestrator bug; tracked via the recurring pattern in
      this status_notes log.  Resumption requires a human to either
      (a) append a new entry to `frontend_tool_catalog.yaml` with
      non-terminal status, or (b) set `orchestrator_status: idle`
      in this file per BACKGROUND_EXECUTION_POLICY §8.3.  Wake
      exits cleanly.

  - at: '2026-05-30T01:38:00Z'
    kind: tool_shipped_catalog_exhausted
    text: |
      Cron-fired wake at 01:37 UTC (cron id 450aa21c-625d-4ad7-b3da-
      ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md + frontend_tool_catalog.yaml +
      frontend_runtime_state.yaml.  Branch check passed (on `frontend_automation`,
      HEAD 134b4b6).  On entry the tree carried an in-flight dispatch for tool 9
      (`policy_futures__futures_price_level`): builder finished @01:21, reviewer
      dispatched @01:23 with the builder's diff sitting unstaged.  Reviewer log
      finalized @01:30 (`/tmp/claude_frontend_reviewer_run_policy_futures_price_level_round2.log`,
      2362 bytes).  `parse_reviewer_log.py` returned
      `{"ok": true, "heading": "APPROVED", "environment_failure": false,
        "reviewer_quota_exhausted": false}` — Case C.  Reviewer confirmed all
      three round-1 findings addressed (the two double-`·` identity bugs +
      the structural shell-density mismatch resolved per Option (c) acceptance,
      documented in THESIS `## Mockup conformance`).

      Pre-commit gate (orchestrator-run, all from
      UI/macro-copilot-dashboard-polished/):
        npm run typecheck         clean
        npm run test:modules      `policy_futures_get_futures_price_level_tool:
                                    8 passed, 0 failed`; all 58 modules + registry pass
        npm run test:build        all build-folder tests pass

      Mirrors:
        surface_contract.md §4.5 row updated:
          build_archetype: generic-builder → typed-result-renderer
          build_status:    scaffolded      → dual-view-built
          monitor_status:  deferred        → built (PolicyFuturesPriceLevel widget)
          (monitor_elig was already `required`)
        surface_contract.md §11 added v17 entry covering the ship.
        §10 not edited (pilot-pinned).
        `graphify update` — skipped, not on PATH on this VM
        (recorded per ORCHESTRATOR_PROMPT Mirrors §2 fallback;
         same precedent as v16, v15, v14, v13, etc.).

      Commit on `frontend_automation` per the template; catalog tool 9 flipped
      → `status: done`, `last_review_heading: APPROVED`, `last_gate_result: pass`,
      `active_step: committed`.  Tool 2's already-uncommitted commit_sha backfill
      (`commit_sha: 134b4b6` line for `inflation_indexed_bonds__breakeven_curve_spread`)
      is bundled into the same work commit so the tree returns clean.

      Counts on exit: done=9 / blocked=0 / remaining=0 — catalog fully shipped.
      orchestrator_status flips → `catalog_exhausted`, last_stop_reason →
      `catalog_exhausted`, catalog_exhausted_at → 01:38.
      last_successful_tool → `policy_futures__futures_price_level`,
      last_successful_commit → `pending-backfill` (a second tiny automation
      commit will land the actual SHA after the work commit, mirroring the
      tool 7 / tool 8 pattern at 1a41454 + 2a22a5e).

      Next cron wake: the entry-point script
      (BACKGROUND_EXECUTION_POLICY §8) should short-circuit on
      `orchestrator_status: catalog_exhausted` and exit before invoking
      this prompt.  If it fires anyway, the wake should be a clean no-op
      (no eligible work under the selection order; no dispatches).

  - at: '2026-05-30T00:07:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 00:07 UTC on 2026-05-30 (cron id 450aa21c-
      625d-4ad7-b3da-ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md +
      frontend_tool_catalog.yaml + frontend_runtime_state.yaml.
      Branch check passed (on `frontend_automation`).  Tree clean
      against HEAD b689cae on entry.  Catalog state unchanged from
      prior wake @23:52 (only HEAD advance was 23:52's own no-op
      commit): done=7, blocked=2 (tools 2 + 9, both
      human_action_required: true), remaining=0, no eligible work
      under the selection order.  Per ORCHESTRATOR_PROMPT.md
      Catalog-exhaustion rule, this wake is a clean no-op:
      orchestrator_status + last_stop_reason remain `catalog_exhausted`,
      last_updated bumped to 00:07, no dispatches, no source code
      changes.  6th consecutive no-op wake; the cron entry-point
      short-circuit (BACKGROUND_EXECUTION_POLICY §8) is not engaging.
      Resumption gated on the human action documented in the
      `human_required` block + tool 2's catalog block_reason; see the
      @23:37 status note for the full resumption checklist (not
      repeated here to keep the file's size sane).  Wake exits cleanly.

  - at: '2026-05-29T23:52:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 23:52 UTC (cron id 450aa21c-625d-4ad7-b3da-
      ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md +
      frontend_tool_catalog.yaml + frontend_runtime_state.yaml as
      required.  Branch check passed
      (`automation/frontend_automation/check_branch.sh` → OK on
      `frontend_automation`).

      Catalog state on entry (unchanged from prior wake @23:37):
        done:      7   (tools 1, 3, 4, 5, 6, 7, 8)
        blocked:   2   (tool 2 mockup_content_mismatch + tool 9
                        reviewer_findings_structural_no_fix_attempt;
                        both have human_action_required: true)
        remaining: 0
        eligible work under selection order (in_progress →
        changes_required → waiting_quota → todo): NONE.

      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule, this wake is
      a clean no-op:
        - orchestrator_status remains `catalog_exhausted` (set @22:28)
        - last_stop_reason remains `catalog_exhausted` (durable value
          per the rule; the structural-finding reason that originally
          triggered exhaustion is preserved in tool 9's catalog
          block_reason + the human_required block + the @22:28 +
          @22:52 + @23:07 + @23:22 + @23:37 status notes)
        - last_updated bumped to 23:52
        - no dispatches, no commits to source code, no tree changes
        - tree clean against HEAD d31543a on entry (verified
          `git status --short` empty)

      The cron entry-point script (BACKGROUND_EXECUTION_POLICY §8) is
      expected to short-circuit future wakes by reading
      `orchestrator_status: catalog_exhausted` and exiting before the
      orchestrator prompt is invoked.  This wake (like @22:52 + @23:07
      + @23:22 + @23:37) fired the prompt anyway, so it repeated the
      no-op pattern.  All such wakes will continue to repeat until a
      human flips tool 2 or tool 9 back to `todo`.

      Resumption requires one of:
        Tool 2 — human re-authors
          UI/.../calculate_breakeven_curve_spread_tool/mockups/
          {Compact,Extended}.png to depict a 2-leg curve spread
          (template: calculate_real_yield_curve_spread_tool/mockups
          adapted from PERCENT → BPS), then flips
          catalog status: blocked → todo + clears
          human_action_required: true → false.
        Tool 9 — human decides between (a) extending
          BuildCompactShell + BuildCompactShellProps with `subtitle`
          + `secondaryKpis` slots (touches 10+ shipped compact views;
          outside the 4-file fix-pass blast-radius rule) OR
          (b) revising
          UI/.../policy_futures_get_futures_price_level_tool/
          mockups/Compact.png down to the shell's standard density
          (3 KPIs, single identity line).  Either path can be combined
          with hand-applying findings #1 + #2 (1-line double-"·" fixes
          per the catalog block_reason) on the re-dispatch.

      Until one of those happens, the factory has nothing to do.
      Wake exits cleanly with no additional dispatches.

  - at: '2026-05-29T23:37:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 23:37 UTC (cron id 450aa21c-625d-4ad7-b3da-
      ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md +
      frontend_tool_catalog.yaml + frontend_runtime_state.yaml as
      required.  Branch check passed
      (`automation/frontend_automation/check_branch.sh` → OK on
      `frontend_automation`).

      Catalog state on entry (unchanged from prior wake @23:22):
        done:      7   (tools 1, 3, 4, 5, 6, 7, 8)
        blocked:   2   (tool 2 mockup_content_mismatch + tool 9
                        reviewer_findings_structural_no_fix_attempt;
                        both have human_action_required: true)
        remaining: 0
        eligible work under selection order (in_progress →
        changes_required → waiting_quota → todo): NONE.

      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule, this wake is
      a clean no-op:
        - orchestrator_status remains `catalog_exhausted` (set @22:28)
        - last_stop_reason remains `catalog_exhausted` (durable value
          per the rule; the structural-finding reason that originally
          triggered exhaustion is preserved in tool 9's catalog
          block_reason + the human_required block + the @22:28 +
          @22:52 + @23:07 + @23:22 status notes)
        - last_updated bumped to 23:37
        - no dispatches, no commits to source code, no tree changes
        - tree clean against HEAD dd09b48 on entry (verified
          `git status --short` empty)

      The cron entry-point script (BACKGROUND_EXECUTION_POLICY §8) is
      expected to short-circuit future wakes by reading
      `orchestrator_status: catalog_exhausted` and exiting before the
      orchestrator prompt is invoked.  This wake (like @22:52 + @23:07
      + @23:22) fired the prompt anyway, so it repeated the no-op
      pattern.  All such wakes will continue to repeat until a human
      flips tool 2 or tool 9 back to `todo`.

      Resumption requires one of:
        Tool 2 — human re-authors
          UI/.../calculate_breakeven_curve_spread_tool/mockups/
          {Compact,Extended}.png to depict a 2-leg curve spread
          (template: calculate_real_yield_curve_spread_tool/mockups
          adapted from PERCENT → BPS), then flips
          catalog status: blocked → todo + clears
          human_action_required: true → false.
        Tool 9 — human decides between (a) extending
          BuildCompactShell + BuildCompactShellProps with `subtitle`
          + `secondaryKpis` slots (touches 10+ shipped compact views;
          outside the 4-file fix-pass blast-radius rule) OR
          (b) revising
          UI/.../policy_futures_get_futures_price_level_tool/
          mockups/Compact.png down to the shell's standard density
          (3 KPIs, single identity line).  Either path can be combined
          with hand-applying findings #1 + #2 (1-line double-"·" fixes
          per the catalog block_reason) on the re-dispatch.

      Until one of those happens, the factory has nothing to do.
      Wake exits cleanly with no additional dispatches.

  - at: '2026-05-29T23:22:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 23:22 UTC (cron id 450aa21c-625d-4ad7-b3da-
      ef39a6ba7784).  Read ORCHESTRATOR_PROMPT.md +
      frontend_tool_catalog.yaml + frontend_runtime_state.yaml as
      required.  Branch check passed
      (`automation/frontend_automation/check_branch.sh` → OK on
      `frontend_automation`).

      Catalog state on entry (unchanged from prior wake @23:07):
        done:      7   (tools 1, 3, 4, 5, 6, 7, 8)
        blocked:   2   (tool 2 mockup_content_mismatch + tool 9
                        reviewer_findings_structural_no_fix_attempt;
                        both have human_action_required: true)
        remaining: 0
        eligible work under selection order (in_progress →
        changes_required → waiting_quota → todo): NONE.

      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule, this wake is
      a clean no-op:
        - orchestrator_status remains `catalog_exhausted` (set @22:28)
        - last_stop_reason remains `catalog_exhausted` (durable value
          per the rule; the structural-finding reason that originally
          triggered exhaustion is preserved in tool 9's catalog
          block_reason + the human_required block + the @22:28 +
          @22:52 + @23:07 status notes)
        - last_updated bumped to 23:22
        - no dispatches, no commits to source code, no tree changes
        - tree clean against HEAD 43969c6 on entry (verified
          `git status --short` empty)

      The cron entry-point script (BACKGROUND_EXECUTION_POLICY §8) is
      expected to short-circuit future wakes by reading
      `orchestrator_status: catalog_exhausted` and exiting before the
      orchestrator prompt is invoked.  This wake (like @22:52 + @23:07)
      fired the prompt anyway, so it repeated the no-op pattern.  All
      such wakes will continue to repeat until a human flips tool 2
      or tool 9 back to `todo`.

      Resumption requires one of:
        Tool 2 — human re-authors
          UI/.../calculate_breakeven_curve_spread_tool/mockups/
          {Compact,Extended}.png to depict a 2-leg curve spread
          (template: calculate_real_yield_curve_spread_tool/mockups
          adapted from PERCENT → BPS), then flips
          catalog status: blocked → todo + clears
          human_action_required: true → false.
        Tool 9 — human decides between (a) extending
          BuildCompactShell + BuildCompactShellProps with `subtitle`
          + `secondaryKpis` slots (touches 10+ shipped compact views;
          outside the 4-file fix-pass blast-radius rule) OR
          (b) revising
          UI/.../policy_futures_get_futures_price_level_tool/
          mockups/Compact.png down to the shell's standard density
          (3 KPIs, single identity line).  Either path can be combined
          with hand-applying findings #1 + #2 (1-line double-"·" fixes
          per the catalog block_reason) on the re-dispatch.

      Until one of those happens, the factory has nothing to do.
      Wake exits cleanly with no additional dispatches.

  - at: '2026-05-29T23:07:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 23:07 UTC (cron id 450aa21c-…).  Read
      ORCHESTRATOR_PROMPT.md + frontend_tool_catalog.yaml +
      frontend_runtime_state.yaml as required.  Branch check passed
      (`automation/frontend_automation/check_branch.sh` → OK on
      `frontend_automation`).

      Catalog state on entry (unchanged from prior wake @22:52):
        done:      7   (tools 1, 3, 4, 5, 6, 7, 8)
        blocked:   2   (tool 2 mockup_content_mismatch + tool 9
                        reviewer_findings_structural_no_fix_attempt;
                        both have human_action_required: true)
        remaining: 0
        eligible work under selection order (in_progress →
        changes_required → waiting_quota → todo): NONE.

      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule, this wake is
      a clean no-op:
        - orchestrator_status remains `catalog_exhausted` (set @22:28)
        - last_stop_reason remains `catalog_exhausted` (durable value
          per the rule; the structural-finding reason that originally
          triggered exhaustion is preserved in tool 9's catalog
          block_reason + the human_required block + the @22:28 +
          @22:52 status notes)
        - last_updated bumped to 23:07
        - no dispatches, no commits to source code, no tree changes
        - tree clean against HEAD 95b7b9a on entry (verified
          `git status --short` empty)

      The cron entry-point script (BACKGROUND_EXECUTION_POLICY §8) is
      expected to short-circuit future wakes by reading
      `orchestrator_status: catalog_exhausted` and exiting before the
      orchestrator prompt is invoked.  Each wake that fires the prompt
      anyway (as this one did, like @22:52) will repeat this no-op
      pattern until a human flips tool 2 or tool 9 back to `todo`.

      Resumption requires one of:
        Tool 2 — human re-authors
          UI/.../calculate_breakeven_curve_spread_tool/mockups/
          {Compact,Extended}.png to depict a 2-leg curve spread
          (template: calculate_real_yield_curve_spread_tool/mockups
          adapted from PERCENT → BPS), then flips
          catalog status: blocked → todo + clears
          human_action_required: true → false.
        Tool 9 — human decides between (a) extending
          BuildCompactShell + BuildCompactShellProps with `subtitle`
          + `secondaryKpis` slots (touches 10+ shipped compact views;
          outside the 4-file fix-pass blast-radius rule) OR
          (b) revising
          UI/.../policy_futures_get_futures_price_level_tool/
          mockups/Compact.png down to the shell's standard density
          (3 KPIs, single identity line).  Either path can be combined
          with hand-applying findings #1 + #2 (1-line double-"·" fixes
          per the catalog block_reason) on the re-dispatch.

      Until one of those happens, the factory has nothing to do.
      Wake exits cleanly with no additional dispatches.

  - at: '2026-05-29T22:52:00Z'
    kind: cron_wake_noop_catalog_exhausted
    text: |
      Cron-fired wake at 22:52 UTC (cron id 450aa21c-…).  Read
      ORCHESTRATOR_PROMPT.md + frontend_tool_catalog.yaml +
      frontend_runtime_state.yaml as required.  Branch check passed
      (`automation/frontend_automation/check_branch.sh` → OK on
      `frontend_automation`).

      Catalog state on entry (unchanged from prior wake @22:28):
        done:      7   (tools 1, 3, 4, 5, 6, 7, 8)
        blocked:   2   (tool 2 mockup_content_mismatch + tool 9
                        reviewer_findings_structural_no_fix_attempt;
                        both have human_action_required: true)
        remaining: 0
        eligible work under selection order (in_progress →
        changes_required → waiting_quota → todo): NONE.

      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule, this wake is
      a clean no-op:
        - orchestrator_status remains `catalog_exhausted` (set @22:28)
        - last_stop_reason advanced from
          `reviewer_findings_structural_no_fix_attempt` →
          `catalog_exhausted` (the literal rule-mandated value once the
          state is durable; the structural-finding reason is preserved
          in tool 9's catalog block_reason + the human_required block
          + the @22:28 status note)
        - last_updated bumped to 22:52
        - no dispatches, no commits, no tree changes

      The cron entry-point script (BACKGROUND_EXECUTION_POLICY §8) is
      expected to short-circuit future wakes by reading
      `orchestrator_status: catalog_exhausted` and exiting before the
      orchestrator prompt is invoked.  If wakes continue to fire the
      prompt anyway (as this one did), each wake will repeat this
      no-op pattern until a human flips tool 2 or tool 9 back to
      `todo`.

      Resumption requires one of:
        Tool 2 — human re-authors
          UI/.../calculate_breakeven_curve_spread_tool/mockups/
          {Compact,Extended}.png to depict a 2-leg curve spread
          (template: calculate_real_yield_curve_spread_tool/mockups
          adapted from PERCENT → BPS), then flips
          catalog status: blocked → todo + clears
          human_action_required: true → false.
        Tool 9 — human decides between (a) extending
          BuildCompactShell + BuildCompactShellProps with `subtitle`
          + `secondaryKpis` slots (touches 10+ shipped compact views;
          outside the 4-file fix-pass blast-radius rule) OR
          (b) revising
          UI/.../policy_futures_get_futures_price_level_tool/
          mockups/Compact.png down to the shell's standard density
          (3 KPIs, single identity line).  Either path can be combined
          with hand-applying findings #1 + #2 (1-line double-"·" fixes
          per the catalog block_reason) on the re-dispatch.

      Until one of those happens, the factory has nothing to do.
      Wake exits cleanly with no additional dispatches.

  - at: '2026-05-29T22:28:00Z'
    kind: tool_9_blocked_catalog_exhausted
    text: |
      Tool 9 (policy_futures__futures_price_level) Dispatch 2 reviewer
      verdict: CHANGES REQUIRED with 3 findings, 1 prefixed `STRUCTURAL:`.
      Reviewer log
      /tmp/claude_frontend_reviewer_run_policy_futures_price_level_round2.log
      (5576 bytes) parsed: heading=CHANGES REQUIRED, environment_failure=
      false, reviewer_quota_exhausted=false.

      Findings (verbatim summary, full text in catalog block_reason):
        #1 CONCRETE  surfaces/BuildCompact.tsx:67 — double "·" separator
                     (BuildCompactShell already inserts the glyph).  1-line
                     fix.
        #2 CONCRETE  surfaces/BuildExtended.tsx:243 — same double "·" bug
                     in extended identity.  1-line fix.
        #3 STRUCTURAL Compact.png mockup demands density (2-line identity
                      + 6 secondary KPIs) the shared BuildCompactShell
                      cannot deliver without API changes touching 10+
                      shipped compact views.  Human design call:
                      extend shell OR revise mockup down.

      Adjudication per SINGLE_REVIEW_ROUND_POLICY.md §3 + ORCHESTRATOR_PROMPT
      Case D step 1:
        ANY STRUCTURAL: prefix → mark human_required, SKIP Dispatch 3.
      Decision: NO Dispatch 3.  Tool 9 → status: blocked +
      human_action_required: true.  Builder diff discarded per default
      blocked-and-discardable rule (no explicit human preservation
      directive on entry).  Tree clean against HEAD 2a22a5e after
      `git restore` (THESIS.md, module.ts, __tests__/module.spec.ts,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) + `git clean -fd` (surfaces/ folder).

      Pipeline summary (this wake):
        Entry state                            tool 8 builder_dispatched
                                                @21:33; this wake @21:52
                                                reconstructed
        Dispatch 1 reviewer (tool 8)           parsed builder log
                                                @21:50 (full BUILDER
                                                REPORT, ~17m runtime)
        Dispatch 2 reviewer (tool 8)           APPROVED @21:59 (~5m)
        Pre-commit gate (tool 8)               npm run test:modules ✓
                                                test:build ✓ typecheck ✓
        Mirrors update (tool 8)                surface_contract.md §4.2
                                                row updated; v15 §11
                                                entry added.  graphify
                                                SKIPPED (not on PATH).
        Commit (tool 8)                        a0364d4 frontend(curve_spread)
        Backfill + advance commit              2a22a5e chore(automation)
        Dispatch 1 builder (tool 9)            exit 0 @22:19 (~13m, full
                                                BUILDER REPORT;
                                                self-tests green;
                                                documented Extended.png
                                                vol/OI deviation per
                                                NO_GO_RULES)
        Dispatch 2 reviewer (tool 9)           CHANGES REQUIRED @22:26
                                                (~6m, 5576-byte review)
        Adjudication                           STRUCTURAL → human_required;
                                                NO Dispatch 3
        Blocked-diff disposition               discarded (default rule)
        Catalog state                          done: 7, blocked: 2,
                                                remaining: 0

      Catalog exhaustion rule triggered: every entry is `done` or
      `blocked` (tool 2 mockup_content_mismatch + tool 9 structural
      findings).  Setting orchestrator_status: catalog_exhausted,
      last_stop_reason: reviewer_findings_structural_no_fix_attempt
      (the proximate cause for THIS wake's stop; the catalog-exhausted
      state is the durable downstream consequence).

      Per ORCHESTRATOR_PROMPT.md Catalog-exhaustion rule + the cron
      entry-point script (BACKGROUND_EXECUTION_POLICY §8), future cron
      wakes will read `orchestrator_status: catalog_exhausted` at the
      top and exit without invoking this prompt.  Resumption requires
      human action on either tool 2 (re-author mockups) or tool 9
      (resolve structural mismatch + flip status: blocked → todo).

      Wake exits cleanly with no additional dispatches.

  - at: '2026-05-29T22:03:00Z'
    kind: wake_continued_tool_9_dispatched
    text: |
      Same wake as tool 8 commit.  Advancing to last eligible tool per
      BACKGROUND_EXECUTION_POLICY §3 selection order: tool 9
      (policy_futures__futures_price_level, build_order 9).
      Categorically distinct from tools 1-8 — SNAPSHOT shape, not a
      butterfly/spread chart.  Reference is get_real_yield_level_tool
      (Phase-1 snapshot pilot).

      Pre-flight 4-check audit:
        Check 1 (backend folder 4-file shape)  PASS
          rates_agent/policy_futures/tools/futures_price_level/
          {__init__.py, config.yaml, schemas.py, compute.py}
        Check 2 (MCP wrapper grep)             PASS
          rates_agent/policy_futures/mcp_server.py:200
          def get_futures_price_level_tool(
          (matches catalog's pre_flight_backend_audit.mcp_tool_name
           which differs from backend_tool_name policy_futures_get_*_tool
           — wrapper unprefixed inside its own domain namespace; workflow
           registry at workflows/__init__.py L1147 disambiguates from the
           bond_futures sibling at L1079)
        Check 3 (curated migration file)       PASS via 3(b)
          database/migrations/2026-05-29_phase1_policy_futures_get_futures_price_level_curated.sql
        Check 4 (mockups Compact+Extended)     PASS
          sha256 C=39c4e5f… E=ecad482… (unique in catalog from
          tool 2's catalog-wide audit; no byte-collision with any
          other tool)

      Defensive vision sanity check (post-tool-2 lesson): viewed both
      PNGs as images via the Read tool.  Confirmed content matches the
      catalog's tool 9 SNAPSHOT concept:
        - Compact: "Policy Futures Price Level · SNAPSHOT" header,
          "SFR1 · SOFR Whites Pack" identity, "WHITES · SOFR 1st
          Quarterly" sub-tag, 3 KPIs (IMPLIED RATE 4.25% (Price 95.75) /
          1D CHANGE -4.5 bp (-1.05%) / Z-SCORE 252D +1.36 Elevated),
          implied-rate sparkline with ±2σ / ±1.5σ bands, range strip
          (5D / 1M change + 252D PCTL/HIGH/LOW/OBSERVATIONS), caveat
          footer "100-minus-rate; UP price = DOWN implied rate (dovish)"
          + "Rolling-generic strip read at position 1." + flag chip +
          freshness indicator + expand affordance.
        - Extended: same identity + KPI strip (PRICE 95.75 / 1D / 5D /
          IMPLIED-RATE z-score / 252D pctl) + volume & open-interest
          side panel + implied-rate chart with bands + methodology card
          + lineage footer + controls strip (FAMILY / TENOR / CONTRACT
          / LAST_PRICE / etc.).
        - Snapshot shape per catalog design guardrails: "Mirror
          get_real_yield_level_tool's snapshot shape (level + change +
          z-score + range strip)" + "Quote convention: 100-minus-rate;
          note in methodology disclosure that an UP price = DOWN implied
          rate (dovish)".  Both honored on the mockup.

      Catalog entry flipped: status: todo → in_progress.  Runtime state:
      orchestrator_status: processing, current_tool:
      policy_futures__futures_price_level, dispatch_round: 1,
      current_step: builder_dispatched.

      Dispatching Dispatch 1 (builder) via run_claude_builder.sh on
      `frontend_automation` branch.  Prompt:
      /tmp/builder_prompt_policy_futures_price_level_round1.md.  Log:
      /tmp/claude_frontend_builder_run_policy_futures_price_level_round1.log.
      Quota probe still unavailable; runtime-enforced.

  - at: '2026-05-29T22:00:00Z'
    kind: tool_8_committed_advance
    text: |
      Tool 8 (ois__curve_spread) APPROVED on first review.  Reviewer log
      /tmp/claude_frontend_reviewer_run_ois_curve_spread_round2.log
      (6194 bytes) parsed: heading=APPROVED, environment_failure=false,
      reviewer_quota_exhausted=false.

      Wake reconstructed state on entry: prior wake's Dispatch 1 builder
      (dispatched 21:33 UTC) had completed by ~21:50 UTC.  Builder log
      /tmp/claude_frontend_builder_run_ois_curve_spread_round1.log
      (4363 bytes) reports all 4 surfaces files created + 3 Stage-3
      stubs overwritten + shared infra updated (src/types/rates.ts +
      src/services/ratesApi.ts + api/routes/rates/detail.py route ADDED
      not replaced) + self-tests green.  No retry needed.

      Pipeline summary:
        Dispatch 1 (builder)                   exit 0 ~21:50 (~17m, full
                                               BUILDER REPORT in log)
        Dispatch 2 (reviewer)                  APPROVED ~21:59 (~5m,
                                               6194-byte review)
        Pre-commit gate (orchestrator):
          npm run test:modules ✓
            calculate_ois_curve_spread_tool: 6 passed, 0 failed
            all module + registry tests passed
          npm run test:build   ✓ (all build-folder tests passed)
          npm run typecheck    ✓ (tsc --noEmit clean)
        Mirrors update:
          surface_contract.md §4.2 row updated in place
            (build_archetype generic-builder → typed-result-renderer,
             build_status scaffolded → dual-view-built,
             monitor_status deferred → built (OisCurveSpread widget);
             monitor_elig was already required).
          §11 v15 changelog entry added.
          §10.3 untouched (pilot-pinned to original 3 Phase-1 pilots).
          graphify update: SKIPPED — not on PATH on this VM (recorded
            per ORCHESTRATOR_PROMPT Mirrors §2 fallback).

      Reviewer notes (verbatim from log):
        A. Dual-view contract — tiers + surfaces + size prop + tone all
          honored.  Compact has no controls strip, calls onExpand,
          surfaces caveat in footer.  Extended has full controls, KPI
          strip, decomposition row, chart with z-bands, methodology
          card, lineage footer.
        B. Mockup conformance — accepted defensible deviations
          (bull/bear flattening regime substituted with steeper/flatter
          cues because bull/bear requires rate-direction history not in
          the Output; top-right card #3 substituted with identity +
          caveat; decomposition row folded into headline strip),
          following just-approved OIS butterfly precedent.
        C. Standalone bridge — typedView: null, /api/v1/rates/detail/
          ois-curve-spread endpoint, fetchDetailOisCurveSpread added,
          OisCurveSpreadOutput mirror type field-by-field match to
          backend (snake_case preserved, no fabricated methodology_label
          / percentile_252d / high_252d_bps).  Both build surfaces +
          Monitor widget consume the SAME bridge via the shared
          useOisCurveSpread hook.
        D. FM1-FM12 — all satisfied; alphabetical loader entry at
          index.ts:64 + :137 between calculate_ois_cross_market_spread
          and calculate_ois_forward_rate.
        E. Methodology honesty — Documented carve-out: OIS sub-domain
          Output does NOT carry methodology_label (sub-domain consistent
          with tool 7), so disclosure is sourced from
          OIS_CURVE_SPREAD_COMPACT_CAVEAT in the shared helper with a
          TODO(PR10) marker.  Honest disclosure honored.  All other
          methodology rows sourced from wire fields (current_spread_bps,
          daily_change_bps, current_z_score, rolling_window_days,
          curve_family, spread_label).
        F. Shared cross-surface helper — oisCurveSpreadShared.ts houses
          family registry + curve options + tenor options + pair options
          + data hook + KPI builders + reference bands + stretch context
          + methodology rows + sanitiser + caveat constant.  Extended +
          Compact + Monitor all import from it.  No drift.
        G. Pre-commit gates — all three pass.

      State: done: 7, blocked: 1, remaining: 1.  Factory advancing to
      tool 9 (policy_futures__futures_price_level, build_order 9 —
      SNAPSHOT shape mirroring get_real_yield_level_tool, NOT a
      butterfly/spread chart).  Wake remaining budget should permit a
      fresh tool dispatch; will probe eligibility on next entry per
      BACKGROUND_EXECUTION_POLICY §3 selection order.

  - at: '2026-05-29T21:31:00Z'
    kind: tool_7_committed_advance
    text: |
      Tool 7 (ois__calculate_ois_butterfly) APPROVED on first review.
      Reviewer log /tmp/claude_frontend_reviewer_run_ois_butterfly_round2.log
      (3787 bytes) parsed: heading=APPROVED, environment_failure=false,
      reviewer_quota_exhausted=false.

      Wake reconstructed state on entry: prior wake's Dispatch 1 builder
      (dispatched 20:55 UTC) had completed by ~21:10 UTC.  Builder stdout
      log was 1 byte (transient claude --print buffering — same pattern
      as tool 5 reviewer Dispatch 2 first attempt at 20:05 UTC), but ALL
      8 frontend artifact slots were populated on-disk: module.ts +
      THESIS.md + __tests__/module.spec.ts (overwritten Stage-3 scaffold)
      + surfaces/{BuildExtended,BuildCompact,oisButterflyShared,monitor/
      OisButterflyWidget}.tsx (new) + shared infra (types/rates.ts +
      services/ratesApi.ts + api/routes/rates/detail.py).  No retry
      needed since the diff was complete and the reviewer doesn't see
      the builder's stdout.

      Pipeline summary:
        Dispatch 1 (builder)                   exit 0 ~21:10 (~15m,
                                               1-byte stdout but full diff)
        Dispatch 2 (reviewer)                  APPROVED ~21:30 (~6m)
        Pre-commit gate (orchestrator):
          npm run test:modules ✓
            calculate_ois_butterfly_tool: 6 passed, 0 failed
            all module + registry tests passed
          npm run test:build   ✓ (all build-folder tests passed,
            including widget-format 53 / widget-contract 11 / dashboard-
            registry 18 / routing 48)
          npm run typecheck    ✓ (tsc --noEmit clean)
        Mirrors update:
          surface_contract.md §4.2 row updated in place
            (build_archetype generic-builder → typed-result-renderer,
             build_status scaffolded → dual-view-built,
             monitor_elig not-required → required,
             monitor_status n/a → built (OisButterfly widget)).
          §11 v14 changelog entry added.
          §10.3 untouched (pilot-pinned to original 3 Phase-1 pilots).
          graphify update: SKIPPED — not on PATH on this VM (recorded
            per ORCHESTRATOR_PROMPT Mirrors §2 fallback).

      Reviewer notes (verbatim from log):
        A. Dual-view contract — tiers + surfaces + size prop + tone all
          honored.  Compact has no controls strip, calls onExpand,
          surfaces caveat in footer.  Extended has full controls, KPI
          strip, decomposition row, chart with z-bands, methodology
          card, lineage footer.
        B. Mockup conformance — Compact matches mockup beat-for-beat
          (identity "SOFR · 2-5-10 OIS FLY", USD flag, 3 KPIs with the
          mockup's "Elevated Cheap" caption, sparkline with ±2σ/±1.5σ
          bands, caveat + flag chip footer).  Extended reproduces the
          mockup layout per the sibling factory pattern.
        C. Standalone bridge — typedView: null, /api/v1/rates/detail/
          ois-butterfly endpoint, fetchDetailOisButterfly added, type
          mirror matches Pydantic field-by-field, both views share the
          same useOisButterfly hook.
        D. FM1-FM12 — all satisfied; alphabetical loader entry at
          index.ts:62.
        E. Methodology honesty — Documented carve-out: OIS sub-domain
          Output does NOT carry methodology_label (distinct from the
          linker/ZCIS sibling Outputs), so disclosure is sourced from
          OIS_BUTTERFLY_COMPACT_CAVEAT in the shared helper with a
          TODO(PR10) marker.  Honest disclosure honored.
        F. Shared cross-surface helper — oisButterflyShared.ts houses
          family registry + triplet registry + KPI builders + reference
          bands + stretch context + methodology rows + sanitiser + hook.
          Extended + Compact + Monitor all import from it.  No drift.
        G. Pre-commit gates — all three pass.

      Non-blocking observations (NOT findings):
        - THESIS "Mockup conformance" sub-section mentions 2 cards
          while Q2 enumerates all 3 — minor prose self-inconsistency
          in THESIS only; implementation matches mockup.  Not worth a
          fix pass.
        - surfaces.build = BuildExtended retained for the legacy
          VirtualPrimitiveCanvas dispatcher; consistent with the
          sibling pattern.
        - Family registry honestly uses short-form curve_family ids
          (USD_SOFR / EUR_ESTR / GBP_SONIA / JPY_OIS / AUD_OIS /
          CAD_OIS) while labelling the overnight index (SOFR / ESTR /
          SONIA / TONA / AONIA / CORRA) — matches backend schema's
          "honest disclosure" comment.

      State: done: 6, blocked: 1, remaining: 2.  Factory advancing to
      tool 8 (ois__curve_spread, build_order 8).  Wake remaining budget
      should permit a fresh tool dispatch; will probe eligibility on
      next entry per BACKGROUND_EXECUTION_POLICY §3 selection order.

  - at: '2026-05-29T20:53:00Z'
    kind: tool_6_committed_advance
    text: |
      Tool 6 (inflation_swaps__scan_inflation_swaps_extremes) APPROVED on
      first review.  Reviewer log
      /tmp/claude_frontend_reviewer_run_zcis_scanner_round2.log (4663
      bytes) parsed: heading=APPROVED, environment_failure=false,
      reviewer_quota_exhausted=false.

      Wake reconstructed state on entry: prior wake's reviewer dispatch
      at 20:38 UTC (state showed current_step=reviewer_dispatched,
      dispatch_round=2) completed at 20:45 UTC on the slug
      `zcis_scanner` (NOT `scan_inflation_swaps_extremes` — the wrappers
      use the short tool_slug stem so logs/prompts are at
      /tmp/{builder,reviewer}_*_zcis_scanner_round*.{md,log}).  No
      claude / wrapper processes still in flight.

      Reviewer affirmed dual-view (A1-A5), mockup conformance (B6-B8),
      standalone bridge (C9-C13), FM1-FM12, methodology honesty (E21-E22),
      shared helper (F23-F27).  One non-blocking observation
      (BuildExtended.tsx:43 imports `instrumentLabel` without referencing
      it — code-hygiene smell, not a typecheck failure; not worth a fix
      pass).  Documented mockup-vs-wire delta: mockup's TIMEFRAME /
      RANK BY / per-row PCTILE absent in impl because backend
      ScanInflationSwapsExtremesInput schema YAML-locks lookback_days,
      single-metric ranking, and does not emit a percentile column.
      THESIS Q3 ("single-metric per catalog") + Q4 ("Multi-metric scan
      adoption" as future scenario) document this — builder aligned to
      wire over aspirational mockup.

      Pre-commit gate (orchestrator):
        npm run test:modules ✓
          scan_inflation_swaps_extremes_tool: 7 passed, 0 failed
          all module + registry tests passed
        npm run test:build  ✓ (all build-folder tests passed)
        npm run typecheck   ✓ (tsc --noEmit clean)

      Mirrors update:
        surface_contract.md §4.4 row (line 235) updated in place
          (output_kind scanner-result → scanner-rankedlist,
           build_archetype generic-builder → typed-result-renderer,
           build_status scaffolded → dual-view-built,
           monitor_status deferred → built (ZcisScanner widget)).
        Redundant §4.7a "Inflation-swap scanner (shipped this wake)"
          sub-section the builder added removed — consolidated into the
          §4.4 row in place, matching the v10-v12 pattern.
        §11 v13 changelog entry added.
        §10.3 untouched (pilot-pinned to original 3 Phase-1 pilots).
        graphify update: SKIPPED — not on PATH on this VM (recorded per
          ORCHESTRATOR_PROMPT Mirrors §2 fallback).

      State: done: 5, blocked: 1, remaining: 3.  Factory advancing to
      tool 7 (ois__calculate_ois_butterfly, build_order 7).  Wake
      remaining budget should permit a fresh tool dispatch; will probe
      eligibility on next entry per BACKGROUND_EXECUTION_POLICY §3
      selection order.

  - at: '2026-05-29T20:16:00Z'
    kind: tool_5_committed_after_fix_advance
    text: |
      Tool 5 (inflation_swaps__inflation_swap_butterfly) committed as
      8e95e1cce67ba47a84af5d164f6dfce2e07fcf68 with
      last_reviewer_heading: APPROVED-AFTER-FIX.

      Pipeline summary:
        Pre-flight (4 checks + vision sanity)  PASS  at 19:47
        Dispatch 1 (builder)                   exit 0 at 20:00 (~13m)
        Dispatch 2 (reviewer) — FIRST ATTEMPT  empty 1-byte log at 20:05
          The reviewer wrapper exited cleanly with the branch-check
          line in wrapper.out but claude --print produced 0 bytes of
          stdout to the tee log.  Parser returned heading: null,
          environment_failure: false, reviewer_quota_exhausted: false
          (no marker substrings in the 1-byte log).  No quota / env
          signals — looks like a transient claude --print failure
          (network blip, buffer issue, or one-off process glitch).
        Dispatch 2 (reviewer) — RETRY            CHANGES REQUIRED at 20:11 (~5m)
          Re-ran with the same prompt verbatim; produced a substantive
          3150-byte review.  Verdict: CHANGES REQUIRED.  2 findings,
          both concrete + actionable + bounded + within the module
          folder (surfaces/BuildExtended.tsx only):
            #1 identity primary deviates from mockup (rendering_density
                §2.1 + THESIS Mockup-conformance) — exact fix provided
            #2 inlined bucketForPercentile bypasses shared helper
                (BUILD_GUIDE §6B + FM contract) — exact fix provided;
                bucketForPercentile verified to exist at
                lib/tone.ts:92-99 and already imported in the shared
                helper.
        Dispatch 3 (fix pass)                  exit 0 at 20:14 (~2m)
          Both findings applied verbatim to surfaces/BuildExtended.tsx
          ONLY (no other files touched).  Self-tests green.
        Pre-commit gate (orchestrator)         all 3 exit 0 at 20:15
        Mirrors update                         at 20:15
          surface_contract.md §4.4 row updated; §11 v12 entry added
        Commit                                 8e95e1c at 20:15
          11 files changed, 1827 insertions(+), 31 deletions(-)
          last_reviewer_heading recorded as APPROVED-AFTER-FIX (honest
          disclosure that Dispatch 3 bypassed re-review per
          SINGLE_REVIEW_ROUND_POLICY §6 / §1).

      Lesson learned (for future wakes): a 1-byte reviewer log with no
      parser markers can be a transient claude --print failure — a
      single retry can disambiguate before escalating to Case F /
      waiting_quota.  Recommend codifying this in
      QUOTA_AND_RESUME_POLICY.md as "Case F handling protocol: try
      ONE retry before marking human_required, log the retry decision
      to status_notes for audit".  Out of scope for this wake — a
      separate human-authored policy edit.

      State: done: 4, blocked: 1, remaining: 4.  Factory advancing to
      tool 6 (inflation_swaps__scan_inflation_swaps_extremes,
      build_order 6, SCANNER shape — compact view is a top-N ranked
      table, NOT a sparkline).

  - at: '2026-05-29T19:46:00Z'
    kind: tool_4_committed_advance
    text: |
      Tool 4 (inflation_swaps__cross_market_inflation_swap_spread)
      committed as 302a388fac7d2cbad86c57aaab673cbafac62798.

      Pipeline summary:
        Pre-flight (4 checks + vision sanity)  PASS  at 19:22
        Dispatch 1 (builder)                   exit 0 at 19:38 (~16m)
          all 8 frontend artifact slots populated; index.ts already had
          alphabetical entry; self-tests green
        Dispatch 2 (reviewer)                  APPROVED at 19:43 (~4m)
          fresh-context Claude; no findings; explicitly confirmed
          methodology honesty (index_family_caveat sourced from wire,
          not hardcoded)
        Pre-commit gate                        all 3 exit 0 at 19:44
        Mirrors update                         at 19:45
          surface_contract.md §4.4 row updated; §11 v11 entry added
        Commit                                 302a388 at 19:45
          11 files changed, 1597 insertions(+), 36 deletions(-)

      State: done: 3, blocked: 1, remaining: 5.  Factory advancing to
      tool 5 (inflation_swaps__inflation_swap_butterfly, build_order 5).

  - at: '2026-05-29T19:19:00Z'
    kind: tool_3_committed_advance
    text: |
      Tool 3 (inflation_indexed_bonds__real_yield_butterfly) committed
      as 48e4230ae68c75f78fd1f2237b8b71ce3fb85fd6.

      Pipeline summary:
        Pre-flight (4 checks + vision sanity)  PASS  at 18:55
        Dispatch 1 (builder)                   exit 0 at 19:11 (~16m)
          builder report: all 8 frontend artifact slots populated,
          self-reported npm test:modules / test:build / typecheck all 0
        Dispatch 2 (reviewer)                  APPROVED at 19:16 (~5m)
          fresh-context Claude; no findings; documented the THESIS
          Mockup-conformance "TIPS"→"linkers" generalization rationale
          as the lone known deviation
        Pre-commit gate                        all 3 exit 0 at 19:17
          npm run test:modules ✓ (per-module 6 checks pass; full registry
                                   passes; loader-presence test enforces
                                   dual-view for this module)
          npm run test:build   ✓ (all build-folder tests pass — DAG,
                                   build-empty-state, parameters,
                                   slot-controls, routing, dashboard,
                                   widget contracts, etc.)
          npm run typecheck    ✓ (tsc --noEmit clean, no NEW errors)
        Mirrors update                         at 19:18
          surface_contract.md §4.3 row: build_archetype generic-builder
            → typed-result-renderer; build_status scaffolded →
            dual-view-built; monitor_elig not-required → required;
            monitor_status n/a → built (RealYieldButterfly widget)
          surface_contract.md §11: v10 changelog entry added
          §10.3 skipped (pilot-pinned to original 3 Phase-1 pilots)
          graphify: SKIPPED — not on PATH (recorded per
            ORCHESTRATOR_PROMPT Case C Mirrors §2 fallback)
        Commit                                 48e4230 at 19:18
          11 files changed, 1753 insertions(+), 31 deletions(-)
          on `frontend_automation`; never touched main / revamp /
          primitive_automation

      State reset: orchestrator_status: processing, current_tool: null,
      current_step: ready_to_dispatch, dispatch_round: 0, done: 2,
      blocked: 1, remaining: 6.  Factory advancing to next eligible
      tool per BACKGROUND_EXECUTION_POLICY §3 selection order: tool 4
      (inflation_swaps__cross_market_inflation_swap_spread,
      build_order: 4).

      Note: tool 1's §4.3 row in surface_contract.md remains stale
      (scaffolded / not-required) — tool 1's commit (fdac7d2) shipped
      without the mirror update.  This is a separately-trackable docs
      drift but not a blocker.  Future wake could batch-fix it; current
      wake stays focused on `current_tool` per NO_GO_RULES §16.
  - at: '2026-05-29T18:55:00Z'
    kind: wake_start_real_yield_butterfly_round1
    text: |
      After tool 2 blocked on misfiled mockups (see prior status_notes
      entry), advanced to next eligible `todo` per
      BACKGROUND_EXECUTION_POLICY §3:

        inflation_indexed_bonds__real_yield_butterfly
        (backend: calculate_real_yield_butterfly_tool;
         endpoint slug: real-yield-butterfly;
         build_order: 3)

      Pre-flight 4-check audit:
        Check 1 (backend folder 4-file shape)  PASS
          rates_agent/inflation_indexed_bonds/tools/real_yield_butterfly/
        Check 2 (MCP wrapper grep)             PASS
          rates_agent/inflation_indexed_bonds/mcp_server.py:1743
          def calculate_real_yield_butterfly_tool(
        Check 3 (curated migration file)       PASS via 3(b)
          database/migrations/2026-05-29_phase1_calculate_real_yield_butterfly_curated.sql
        Check 4 (mockups Compact+Extended)     PASS
          sha256 C=81187a08… E=68f7e114… (unique in catalog; no
          byte-collision with any other tool)

      Defensive vision sanity check (post-tool-2 lesson): viewed both
      PNGs as images via the Read tool.  Confirmed content matches the
      catalog's tool 3 concept:
        - Compact: "Real Yield Butterfly · SNAPSHOT", "USD_TIPS 5-10-30
          RY FLY", 3 KPIs (FLY -14 bp Belly Rich · 1D CHANGE -1.8 bp ·
          Z-SCORE -2.07 Extreme Rich), sparkline w/ ±2σ z-score bands,
          identity chip "USD_TIPS Linker curve", expand affordance
          top-right.
        - Extended: "USD_TIPS 5-10-30 REAL YIELD BUTTERFLY", FLY KPI
          strip + z-score + percentile + history range, time-series
          chart, FLY-vs-decomposition panel (belly vs wings), real-yield
          curve snapshot panel (3 points), methodology card on right,
          lineage footer.
        - Matches the catalog's design guardrail "Single linker curve
          (no nominal pair) — input is one curve_family + three tenors"
          (single USD_TIPS curve, 5Y / 10Y / 30Y).
        - No byte-collision with any other tool's mockups (verified by
          catalog-wide sha256 audit).

      Catalog entry flipped: status: todo → in_progress.  Runtime state:
      orchestrator_status: processing, current_tool:
      inflation_indexed_bonds__real_yield_butterfly, dispatch_round: 1,
      current_step: builder_dispatched.

      Dispatching Dispatch 1 (builder) via run_claude_builder.sh on
      `frontend_automation` branch.  Prompt:
      /tmp/builder_prompt_real_yield_butterfly_round1.md.  Log:
      /tmp/claude_frontend_builder_run_real_yield_butterfly_round1.log.
      Quota probe still unavailable; runtime-enforced.
  - at: '2026-05-29T18:54:00Z'
    kind: tool_2_builder_refused_mockup_content_mismatch
    text: |
      Tool 2 (inflation_indexed_bonds__breakeven_curve_spread) was
      dispatched to Dispatch 1 (Claude builder, --print --model opus
      --effort xhigh) at 18:48 UTC after a CLEAN pre-flight (all four
      checks PASS including Check 4 path-existence of the mockup PNGs).

      The builder ran for ~5 minutes and REFUSED the build per the
      builder-prompt "blocker" policy.  Builder evidence (verbatim,
      from /tmp/claude_frontend_builder_run_breakeven_curve_spread_round1.log):

        - The two mockup PNGs at
          UI/.../calculate_breakeven_curve_spread_tool/mockups/{Compact,
          Extended}.png are byte-identical to the corresponding files
          under calculate_breakeven_butterfly_tool/mockups/ (sha256
          a47e9746c90537c22183c6d8f7ec7cec6fee6cdf7a3cd97892ccd5836af97fda
          + adf8ad5d4a3711731eed42fb165f1ea201050303c24a7687f3ff2c2301631e5e).
        - The PNGs' visual content depicts a 3-leg BREAKEVEN BUTTERFLY
          (title "US 2-5-10 BE FLY", three tenors UST/USD_TIPS 2Y/5Y/10Y,
          headline KPI "FLY -18 bp", 3-leg caveat footer), NOT a 2-leg
          curve spread.
        - The backend tool's schemas
          (rates_agent/inflation_indexed_bonds/tools/breakeven_curve_spread/schemas.py)
          unambiguously model a 2-leg spread:
          BreakevenCurveSpreadInput exposes only short_tenor + long_tenor;
          BreakevenCurveSpreadCurrentMetrics exposes current_spread_bps +
          short_breakeven_bps + long_breakeven_bps (NO belly_bps /
          fly_bps); _short_must_precede_long validator enforces
          short_years < long_years; docstring spread_label examples are
          "UST/USD_TIPS 2s10s breakeven", "FR_OAT/EUR_FR_LINKER 5s10s
          breakeven".  No third leg anywhere.
        - The mockup's headline `FLY -18 bp` KPI has no backing field in
          BreakevenCurveSpreadOutput.  Faking it (e.g. relabelling
          current_spread_bps as "FLY") would mis-shape the module to
          PMs and violate P5 honest disclosure (the methodology_label
          threaded from the backend YAML would visibly contradict the
          headline).
        - Both pairs were committed in `5ea7ecc "changes"` together —
          almost certainly a cp/mv into the wrong folder during
          mockup intake.  No alternate mockups exist elsewhere in the
          repo for this tool.

      Catalog-wide mockup audit (sha256 of all 9 tools' Compact + Extended
      PNGs) confirms tool 2 is the ONLY misfile in the catalog.  Tools
      3-9 each have unique mockup hashes — none collide with each other
      or with tool 1.  Hashes:
        tool 1 breakeven_butterfly:                C=a47e974… E=adf8ad5…  ← reference
        tool 2 breakeven_curve_spread:             C=a47e974… E=adf8ad5…  ← DUPLICATE (blocker)
        tool 3 real_yield_butterfly:               C=81187a0… E=68f7e11…
        tool 4 cross_market_inflation_swap_spread: C=3069d05… E=e1fc747…
        tool 5 inflation_swap_butterfly:           C=7c75285… E=15b3fa6…
        tool 6 scan_inflation_swaps_extremes:      C=513299b… E=b705a94…
        tool 7 ois_butterfly:                      C=0c90439… E=1f74c86…
        tool 8 ois_curve_spread:                   C=a7c2853… E=d49faec…
        tool 9 policy_futures_price_level:         C=39c4e5f… E=ecad482…

      Disposition per ORCHESTRATOR_PROMPT "Blocked-diff disposition rule"
      + NO_GO_RULES §1 (functional mockups-missing) + PRE_FLIGHT §4
      (read-only — orchestrator MUST NOT manufacture mockups):
        - Tool 2 → status: blocked, block_reason: mockup_content_mismatch
          (verbatim in catalog entry).
        - Tree is already clean: builder wrote 0 files (refusal exited
          before any edit), so blocked-and-discardable disposition is
          a no-op; HEAD remains 9c6c30c.
        - Counts: done:1, blocked:1, remaining:7.
        - Per BACKGROUND_EXECUTION_POLICY §3 selection order and the
          ORCHESTRATOR_PROMPT loop rule ("Continue to the next eligible
          tool — one missing artifact blocks only its dependent tool"),
          advance to next eligible: tool 3
          (inflation_indexed_bonds__real_yield_butterfly, build_order 3).

      Remediation (for human): re-author the two PNGs at
      UI/.../calculate_breakeven_curve_spread_tool/mockups/ to depict a
      2-leg breakeven curve spread (current spread bps + endpoint
      decomposition + 252d z-score + sparkline + percentile), e.g.
      "UST/USD_TIPS 2s10s breakeven".  Natural template:
      calculate_real_yield_curve_spread_tool/mockups/ adapted PERCENT →
      BPS (matches the catalog entry's existing design guardrail).  Then
      flip catalog status: blocked → todo and the factory will pick it
      up on the next eligible-selection pass.

      Optional gate-extension (recommended, per builder's third
      remediation point): extend PRE_FLIGHT Check 4 with a sha256
      collision check across all catalog tools' mockup folders — would
      have caught this misfile at audit time rather than burning ~5min
      of Claude builder quota.  Out of scope for this wake; would be a
      separate human-authored PR.
  - at: '2026-05-29T18:48:00Z'
    kind: wake_start_breakeven_curve_spread_round1
    text: |
      New cron wake at 18:47 UTC.  Tree clean at HEAD 9c6c30c (tool 1
      committed at fdac7d2; state-reset commit at 9c6c30c).  Selected
      first eligible `todo` tool per BACKGROUND_EXECUTION_POLICY §3
      selection order (no in_progress / changes_required / waiting_quota
      entries; tool 1 done; tool 2 is first todo):

        inflation_indexed_bonds__breakeven_curve_spread
        (backend: calculate_breakeven_curve_spread_tool;
         endpoint slug: breakeven-curve-spread;
         build_order: 2)

      Pre-flight 4-check audit:
        Check 1 (backend folder 4-file shape)  PASS
          rates_agent/inflation_indexed_bonds/tools/breakeven_curve_spread/
          {__init__.py, config.yaml, schemas.py, compute.py}
        Check 2 (MCP wrapper grep)             PASS
          rates_agent/inflation_indexed_bonds/mcp_server.py:831
          def calculate_breakeven_curve_spread_tool(
        Check 3 (curated migration file)       PASS via Check 3(b)
          database/migrations/2026-05-29_phase1_calculate_breakeven_curve_spread_curated.sql
          (Check 3(a) DB row probe returned `relation "macro_data.tool_metadata"
          does not exist` — table not created on this VM; migration file
          presence satisfies the gate per PRE_FLIGHT_BACKEND_AUDIT.md §2)
        Check 4 (mockups Compact+Extended)     PASS
          UI/.../calculate_breakeven_curve_spread_tool/mockups/{Compact,Extended}.png
          (1.3 MB + 1.6 MB committed)

      Catalog entry flipped: status: todo → in_progress.  Runtime state:
      orchestrator_status: processing, current_tool:
      inflation_indexed_bonds__breakeven_curve_spread, dispatch_round: 1,
      current_step: builder_dispatched.

      Dispatching Dispatch 1 (builder) via
      run_claude_builder.sh on `frontend_automation` branch.  Prompt:
      /tmp/builder_prompt_breakeven_curve_spread_round1.md (309 lines —
      catalog entry verbatim + pre-flight output + resolved paths +
      CLAUDE_BUILDER_PROMPT.md body).  Log:
      /tmp/claude_frontend_builder_run_breakeven_curve_spread_round1.log.
      Quota probe unavailable; proceeding runtime-enforced.
  - at: '2026-05-29T18:37:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 18:37 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, SINGLE_REVIEW_ROUND_POLICY,
      frontend_tool_catalog.yaml, and frontend_runtime_state.yaml.
      Ran check_branch.sh: OK on `frontend_automation`.

      Observed state (unchanged from 18:22 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 18:22 wake.  This is the 10th consecutive NO-OP wake on
      the same blocker (16:22 → 16:37 → 16:52 → 17:07 → 17:22 → 17:37
      → 17:52 → 18:07 → 18:22 → 18:37).

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T18:22:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 18:22 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, SINGLE_REVIEW_ROUND_POLICY,
      frontend_tool_catalog.yaml, and frontend_runtime_state.yaml.
      Ran check_branch.sh: OK on `frontend_automation`.

      Observed state (unchanged from 18:07 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 18:07 wake.  This is the 9th consecutive NO-OP wake on
      the same blocker (16:22 → 16:37 → 16:52 → 17:07 → 17:22 → 17:37
      → 17:52 → 18:07 → 18:22).

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T18:07:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 18:07 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, frontend_tool_catalog.yaml, and
      frontend_runtime_state.yaml.  Ran check_branch.sh: OK on
      `frontend_automation`.

      Observed state (unchanged from 17:52 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 17:52 wake.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T17:52:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 17:52 UTC.  Read ORCHESTRATOR_PROMPT,
      frontend_tool_catalog.yaml, and frontend_runtime_state.yaml.
      Ran check_branch.sh: OK on `frontend_automation`.

      Observed state (unchanged from 17:37 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 17:37 wake.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T17:37:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 17:37 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, SINGLE_REVIEW_ROUND_POLICY,
      frontend_tool_catalog.yaml, and frontend_runtime_state.yaml.
      Ran check_branch.sh: OK on `frontend_automation`.

      Observed state (unchanged from 17:22 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 17:22 wake.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T17:22:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 17:22 UTC.  Read ORCHESTRATOR_PROMPT,
      frontend_tool_catalog.yaml, and frontend_runtime_state.yaml.
      Ran check_branch.sh: OK on `frontend_automation`.

      Observed state (unchanged from 17:07 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 17:07 wake.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T17:07:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 17:07 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, SINGLE_REVIEW_ROUND_POLICY,
      DISCORD_STATUS_POLICY, frontend_tool_catalog.yaml, and
      frontend_runtime_state.yaml.  Ran check_branch.sh: OK on
      `frontend_automation`.

      Observed state (unchanged from 16:52 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  HEAD is unchanged at 6972c5d.  No human action
      since 16:52 wake.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T14:30:00Z'
    kind: factory_armed_after_rename_revert
    text: |
      Previous block (`pre_commit_gate_test_build_pre_existing_baseline_drift`)
      remediated by reverting the two bad rename commits (2b8b36b +
      cd4aa0d) and adding `node` to tsconfig.app.json types.

      The test:build failure root cause: the bad rename removed
      `policy_futures_get_futures_price_level_tool` from src/modules/
      index.ts entirely (treating it as a duplicate of
      `get_futures_price_level_tool`, but they are distinct tools —
      L1079 vs L1147 in rates_agent/workflows/__init__.py).  The build
      harness test asserts KNOWN_BACKEND_TOOLS contains 58 names;
      after the deletion it was 57, hence the failure.  The revert
      restores the 58th tool.

      The 8 pre-existing typecheck baseline errors (`node:fs/promises`,
      `NodeJS.*`, `process` references in pilot module specs and
      shared compact components) are now fixed by the `node` types
      addition.

      The `zToneClass` error in RealYieldLevelWidget.tsx:176 remains
      as pre-existing baseline (single error; predates the factory).
      The orchestrator's gate logic compares AFTER ≤ BEFORE count, so
      this single residual won't block.

      All 9 tools are back to status `todo` — including the previously
      blocked breakeven_butterfly which the orchestrator had flipped
      after the failed gate.

      On this wake: select the first eligible tool, run pre-flight,
      dispatch the builder.
  - at: '2026-05-29T15:52:00Z'
    kind: wake_start_breakeven_butterfly_round1
    text: |
      Wake start.  Discarded stale builder diff sitting in working
      tree from prior wake (THESIS.md / module.ts / __tests__/module.spec.ts
      modifications + surfaces/ folder untracked + ratesApi.ts /
      types/rates.ts / detail.py mods) per default
      blocked-and-discardable rule (no human preservation
      directive).  Tree is clean against HEAD.

      Selected first eligible `todo` tool:
      inflation_indexed_bonds__breakeven_butterfly
      (backend: calculate_breakeven_butterfly_tool).

      Pre-flight 4-check audit:
        Check 1 (backend folder 4-file shape)  PASS
        Check 2 (MCP wrapper grep)             PASS
        Check 3 (curated migration file)       PASS
        Check 4 (mockups Compact+Extended)     PASS

      Dispatching Dispatch 1 (builder) via
      run_claude_builder.sh on `frontend_automation` branch.
      Quota probe unavailable; proceeding runtime-enforced.
  - at: '2026-05-29T16:07:30Z'
    kind: wake_detected_inflight_builder
    text: |
      New cron wake at 16:07 UTC.  Prior wake's Dispatch 1 builder
      (PID 200737 wrapper / 200744 claude CLI; tee log
      /tmp/claude_frontend_builder_run_breakeven_butterfly_round1.log)
      still in flight after ~12m runtime; `claude --print` buffers
      stdout so the tee log is currently 0 bytes (expected).

      Per BACKGROUND_EXECUTION_POLICY §7 no-overlap rule, will NOT
      dispatch a parallel builder.  Per §1 / §6, will wait
      synchronously (bounded ~20m) for the in-flight builder to
      complete, then continue the single-round loop on this same
      wake (reviewer dispatch → parse → commit or fix → next tool).
      If the wait times out, this wake will exit cleanly leaving
      `current_step: builder_dispatched` for the next cron wake.
  - at: '2026-05-29T16:09:00Z'
    kind: builder_completed_round1
    text: |
      Dispatch 1 builder exited cleanly at ~16:08.  BUILDER REPORT
      reports all 8 frontend artifact slots populated (module.ts +
      THESIS.md + surfaces/BuildExtended.tsx + surfaces/BuildCompact.tsx
      + surfaces/breakevenButterflyShared.ts + surfaces/monitor/
      BreakevenButterflyWidget.tsx + __tests__/module.spec.ts +
      mockups/{Compact,Extended}.png input).  Shared infra updates:
      src/types/rates.ts + src/services/ratesApi.ts + api/routes/
      rates/detail.py (route ADDED, not replaced).  FM12 loader entry
      already present from prior Stage-3 import; no edit needed.
      Builder self-reports `npm run test:modules` + `test:build` +
      `typecheck` all exit 0 (NOT independently verified by
      orchestrator yet — gate runs post-reviewer per protocol).
      Dispatching Dispatch 2 reviewer via run_claude_reviewer.sh.
  - at: '2026-05-29T16:17:33Z'
    kind: reviewer_changes_required_structural
    text: |
      Dispatch 2 reviewer completed in ~6m.  Verdict: CHANGES REQUIRED
      with 5 findings, of which two are explicitly prefixed
      `STRUCTURAL:`.  Parser output:
        {ok:true, heading:"CHANGES REQUIRED",
         environment_failure:false, reviewer_quota_exhausted:false}

      Adjudication per SINGLE_REVIEW_ROUND_POLICY §3 + ORCHESTRATOR_PROMPT
      Case D:
        ANY STRUCTURAL: prefix → mark human_required, SKIP Dispatch 3.

      Decision: NO Dispatch 3.  Tool → human_required.  Wake stops
      cleanly.  Full finding text + working-tree disposition recorded
      under the runtime-state `human_required` block above.

      Reviewer notes findings #3 (type-mirror nullability) + #4
      (Monitor triplet options) are concrete + actionable in
      isolation, but cannot be applied without first resolving #1
      (mockup default unreachable in USD_TIPS playbook) and #2
      (identity-string drift coupled to #1).  A piecemeal fix-pass
      that addressed only #3/#4 would not satisfy the reviewer's
      structural concerns and would risk needing to be re-done after
      the human design call lands.
  - at: '2026-05-29T16:52:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 16:52 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, DISCORD_STATUS_POLICY,
      frontend_tool_catalog.yaml, and frontend_runtime_state.yaml.
      Ran check_branch.sh: OK on `frontend_automation`.

      Observed state (unchanged from 16:37 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per explicit prior-wake
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  No human action since 16:37 wake.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions + SINGLE_REVIEW_ROUND_POLICY §3:
        - cannot advance to next eligible tool
          (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
          a dirty tree (§4a Disallowed clause);
        - cannot discard the preserved diff (non-default preservation
          was the explicit prior-wake choice);
        - cannot re-dispatch builder/reviewer for breakeven_butterfly
          (STRUCTURAL findings skip Dispatch 3 + NO Dispatch 4 / NO
          re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T16:37:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 16:37 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, frontend_tool_catalog.yaml, and
      frontend_runtime_state.yaml.  Ran check_branch.sh: OK on
      `frontend_automation`.

      Observed state (unchanged from 16:22 wake, ~15m delta):
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per prior wake's explicit
                             non-default preservation directive)
        done:0  blocked:1  remaining:8

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications.  No human action yet.

      Per BACKGROUND_EXECUTION_POLICY §4a + ORCHESTRATOR_PROMPT Stop
      conditions: cannot advance to next eligible tool
      (`inflation_indexed_bonds__breakeven_curve_spread`) on top of a
      dirty tree; cannot discard the preserved diff (non-default
      preservation was the explicit prior-wake choice); cannot
      re-dispatch builder/reviewer for breakeven_butterfly because the
      structural findings require a human design call (per
      SINGLE_REVIEW_ROUND_POLICY §3, NO Dispatch 4 / NO re-review).

      Decision: NO-OP wake.  Factory remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`) and
            resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended USD_TIPS playbook OR THESIS Mockup-conformance
            section), re-stages + commits + sets
            orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.

  - at: '2026-05-29T16:22:30Z'
    kind: wake_noop_dirty_tree_preserved
    text: |
      New cron wake at 16:22 UTC.  Read ORCHESTRATOR_PROMPT,
      BACKGROUND_EXECUTION_POLICY, SINGLE_REVIEW_ROUND_POLICY, and
      DISCORD_STATUS_POLICY.  Ran check_branch.sh: OK on
      `frontend_automation`.

      Observed state:
        orchestrator_status: human_required
        current_tool:        inflation_indexed_bonds__breakeven_butterfly (blocked)
        working_tree:        dirty_preserved (per prior wake's explicit
                             non-default preservation directive)

      `git status --short` confirms the preserved breakeven_butterfly
      diff is still in the tree (THESIS.md, module.ts, __tests__/
      module.spec.ts, surfaces/{BuildCompact,BuildExtended,
      breakevenButterflyShared,monitor/BreakevenButterflyWidget}.tsx,
      src/types/rates.ts, src/services/ratesApi.ts, api/routes/rates/
      detail.py) plus the runtime-state + catalog state-flip
      modifications from the prior wake.

      Per BACKGROUND_EXECUTION_POLICY §4a:
        "if a human explicitly wants the blocked scaffold preserved,
         stop and surface that preservation requirement as the reason
         for the halt"
        "Disallowed: leaving blocked generated code in the tree and
         starting the next tool on top of it"

      Decision: NO-OP wake.  Do NOT discard the preserved diff
      (non-default preservation was an explicit prior-wake choice).
      Do NOT advance to next eligible tool
      (`inflation_indexed_bonds__breakeven_curve_spread`) on top of
      a dirty tree — that would violate §4a.  Do NOT re-dispatch
      builder/reviewer for breakeven_butterfly — the structural
      findings (#1 mockup default unreachable from USD_TIPS playbook;
      #2 identity-string drift coupled to #1) require a human design
      call, not another automated round.

      Re-surfacing the existing human_required block.  The factory
      remains halted pending one of:
        (a) human clears the tree (`git restore . && git clean -fd
            UI/.../calculate_breakeven_butterfly_tool/surfaces/`)
            and resolves the structural design call, then sets
            orchestrator_status: idle to re-arm; OR
        (b) human hand-applies the resolution (revised mockup OR
            extended playbook OR THESIS Mockup-conformance section),
            then re-stages + commits + sets orchestrator_status: idle.

      Wake exits cleanly with no dispatches and no commits.  Next cron
      wake will again read state, observe human_required + dirty
      tree, and again no-op until the human acts.
  - at: '2026-05-29T18:45:59Z'
    kind: tool_1_advanced_via_human_resolution
    text: |
      Tool 1 (inflation_indexed_bonds__breakeven_butterfly) committed as
      fdac7d2821fc61ecdab7b122b8ede4d18d1c3374.  Reviewer's 2 STRUCTURAL findings resolved via
      Option (c): accept USD_TIPS data-driven deviation + document in
      THESIS Mockup-conformance subsection.  Findings #3 (over-permissive
      types — time_series_butterfly + time_series_zscore tightened to
      REQUIRED to mirror Pydantic Output) and #4 (single-pair
      BUTTERFLY_TRIPLET_OPTIONS — widened to union across all four linker
      pairs) addressed alongside.  Loading-state stub in BuildExtended.tsx
      extended to satisfy the now-required TimeSeries fields.

      Gate green on the VM (npm run test:modules + test:build + typecheck
      all exit 0).  Tool commit pushed to origin/frontend_automation.

      State reset: orchestrator_status: processing, current_tool: null,
      current_step: ready_to_dispatch, dispatch_round: 0, done: 1,
      blocked: 0, remaining: 8.  Factory advancing to tool 2
      (inflation_indexed_bonds__breakeven_curve_spread) on next cron
      wake.
  - at: "2026-05-30T00:35:00Z"
    kind: tool_9_design_call_applied_advance
    text: |
      Catalog reset: tool 9 (policy_futures__futures_price_level)
      flipped status: blocked -> todo with two new design_guardrails
      added per Option (c) precedent from tool 1 (breakeven_butterfly,
      commit fdac7d2):

        guardrail (5): keep shell-standard density on Compact +
        Extended; do NOT extend BuildCompactShell / BuildExtendedShell
        to add subtitle / secondaryKpis slots (10+ tool blast radius);
        add a `## Mockup conformance` subsection to THESIS.md.

        guardrail (6): do NOT prepend `· ` to the secondary string
        on either Build view (IdentityBlock already inserts the glyph).
        Reference twin: get_real_yield_level_tool/surfaces/BuildCompact.tsx:72-76.
        Avoiding this prevents review findings #1 + #2 from recurring.

      Tool 2 (inflation_indexed_bonds__breakeven_curve_spread) REMAINS
      blocked — its mockups are byte-identical to breakeven_butterfly`s
      mockups (depicting a 3-leg butterfly, not a 2-leg curve spread).
      Human must author new Compact.png + Extended.png for this tool
      depicting a 2-leg breakeven curve spread (template:
      calculate_real_yield_curve_spread_tool/mockups/ adapted from % -> bps),
      then flip status back to todo.  No safe path from the orchestrator
      side; PRE_FLIGHT Check 4 is path-existence only.

      Counters: done:7  blocked:1  remaining:1.  Cron is currently
      DISABLED (`openclaw cron disable 450aa21c-...` at 00:30Z).  User
      must re-enable to dispatch tool 9, then optionally disable again
      after tool 9 settles to avoid post-exhaustion no-op commit spam
      until tool 2 mockups are authored.
  - at: "2026-05-30T00:48:00Z"
    kind: tool_2_mockups_remediated_unblocked
    text: |
      Tool 2 (inflation_indexed_bonds__breakeven_curve_spread) unblocked.
      Human (sreeramandra) authored new mockup PNGs depicting the canonical
      2-leg breakeven curve spread (US 2s10s, UST/USD_TIPS, 3 KPIs, single
      identity line — shell-density compatible).  Old butterfly-duplicate
      mockups (sha256 a47e9746 + adf8ad5d) replaced with new ones (sha256
      f6f21f1c + 117558a3) in commit 0b1eb86 on frontend_automation.

      Catalog tool 2 entry:
        status: blocked -> todo
        human_action_required: true -> false
        block_reason: <multi-line> -> null  (preserved as previous_block_reason)
        last_resolution_applied: mockups_remediated_user_authored_2leg_spread

      Counters: done:7  blocked:1  remaining:1  ->  done:7  blocked:0  remaining:2
      Both tool 2 and tool 9 are now eligible for dispatch on next wake.
      Cron must be re-enabled to dispatch.

  - at: "2026-05-30T03:30:00Z"
    kind: batch_2_queued
    text: |
      Batch 2 queued: 9 new tool entries appended to catalog
      (build_order 10-18).  Mockups intake committed separately;
      9 stub migration files added under database/migrations/
      to satisfy PRE_FLIGHT Check 3(b).

      The 9 tools: get_ois_rate_level_tool,
      calculate_inflation_swap_rate_level_tool,
      calculate_ois_forward_rate_tool,
      calculate_inflation_swap_curve_spread_tool,
      calculate_ois_cross_market_spread_tool,
      calculate_forward_breakeven_simple_tool,
      scan_inflation_linkers_extremes_tool,
      scan_bond_futures_extremes_tool,
      get_scan_policy_futures_extremes_tool.

      All structural twins are from Batch 1 or Phase-1 pilot.
      Three tools carry catalog notes about backend_tool_name vs
      mcp_tool_name vs frontend folder name divergences (per the
      TS KNOWN_TOOL_ALIASES + workflow-registry naming history).

      Counters: done:9 blocked:0 remaining:9.

  - at: "2026-06-07T15:30:00Z"
    kind: batch_3_queued
    text: |
      Batch 3 queued: 9 new tool entries appended (build_order
      19-27).  9 of the 10 originally recommended — calculate_swap_spread_tool
      DEFERRED because its Compact.png and Extended.png mockups
      are byte-identical (sha256 87437a22... for both); user must
      author the missing view before this tool can be queued.

      The 9 shipped: policy_futures_get_futures_butterfly_simple_tool,
      policy_futures_get_futures_calendar_spread_tool,
      policy_futures_get_futures_cross_market_spread_tool,
      policy_futures_get_futures_pack_average_simple_tool,
      get_futures_price_level_tool (bond_futures domain),
      calculate_inflation_swap_forward_tool,
      calculate_swap_breakeven_basis_simple_tool,
      calculate_cross_country_breakeven_spread_simple_tool,
      compute_financing_rate_tool.

      4 policy_futures tools carry name-divergence guardrails
      (frontend folder + workflow registry use policy_futures_ prefix;
      MCP function inside policy_futures/mcp_server.py uses unprefixed
      get_futures_<thing>_tool — same pattern as Batch 1 d8e8233).

      Counters: done:18 blocked:0 remaining:9.

  - at: "2026-06-07T16:50:00Z"
    kind: batch_3_addendum_swap_spread_added
    text: |
      calculate_swap_spread_tool added to catalog as build_order 28
      (the 10th tool from the Batch 3 recommendation, originally
      deferred because Compact.png and Extended.png were byte-
      identical at sha256 87437a22...).  Human re-authored Extended.png
      with distinct content (now sha256 8cd57fe0...); mockups landed
      on VM and committed alongside the catalog entry.

      Counters: done:18 blocked:0 remaining: 9 -> 10.  Orchestrator
      will pick this up AFTER tools 19-27 ship (build_order 28 is
      lowest priority among current todos).
